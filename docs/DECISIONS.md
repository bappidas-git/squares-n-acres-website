# Decisions — Squares N Acres website

Architecture decision records, newest last. Format:
`- yyyy-mm-dd — Dxx/Pnn — <decision> — <reason>`

`Dxx` references a numbered decision of `prompts/00_MASTER_CONTEXT.md` §13; `Pnn` is the prompt
that took or applied the decision. Decisions taken by an executor (not pre-decided in the master
context) carry only a `Pnn` tag.

---

- 2026-09-15 — D17/P01 — `scripts/check-traces.js` implements the §13 D17 pattern set as 23 named case-insensitive regexes plus a hex-literal scan, scans `src public mock-server scripts docs db.json README.md package.json .env*`, skips `node_modules build coverage prompts .git dist docs/archive` and binary extensions, and excludes itself from the scan — otherwise the file that defines the patterns would always report findings.
- 2026-09-15 — D17/P01 — The trace allow-list is applied by expanding each match to the whole surrounding word (`[\w-]` on both sides) and comparing against `home, homes, homepage, home-loan, home loan, hometown`, plus a phrase check for the two-word entries — a plain substring allow-list would also swallow real findings such as the boilerplate's SweetAlert2 popup class.
- 2026-09-15 — D17/P01 — Hex literals are reported everywhere except `src/assets/styles/global.css`, `src/theme.js`, `src/seo/data/*` and `public/brand/*`; `&#8377;`-style entities and `#`/`?` fragments inside string literals that begin with `http(s)://` are not counted as colours.
- 2026-09-15 — D36/P01 — ESLint stays in `package.json` → `eslintConfig` (CRA reads it during `start`/`build`) with `extends: ["react-app", "react-app/jest", "prettier"]` and the four project rules: `no-console` (allow `warn`/`error`), `no-unused-vars` (error, `^_` escape hatch, `ignoreRestSiblings`), `react-hooks/exhaustive-deps` as an **error**, and `no-restricted-imports` for `axios` and `@mui/icons-material`. Overrides exempt `src/services/api.js` / `src/services/http.js` from the axios ban and give `scripts/**` + `mock-server/**` the Node env with `console` allowed. — One config, no flat-config migration, and CRA's dev server enforces exactly what CI enforces.
- 2026-09-15 — D37/P01 — Prettier `{ singleQuote, semi, printWidth: 100, trailingComma: "es5", endOfLine: "lf" }` with `eslint-config-prettier` last in `extends`, so formatting is never an ESLint concern.
- 2026-09-15 — P01 — **Whole-tree formatting is deferred to prompt 03.** Every file in `src/` is currently unformatted, so `prettier --write` on the files touched here would have rewritten ~8 000 lines across 18 large files (`FinanceGuide.jsx`, `PropertyDetails.jsx`, `api.js`, …) and buried the behaviour-preserving lint fixes in noise, immediately before the rebrand rewrites the same files. Only `scripts/check-traces.js` (new in this prompt) is Prettier-formatted; `npm run format:check` therefore fails until prompt 03 runs `npm run format` on the whole tree. — Prompt 01 §4.6 ("do not run `prettier --write` on the whole tree … Prompt 03 formats the whole tree after the purge").
- 2026-09-15 — D6/P01 — `.nvmrc` pins `20` while `engines` accepts `>=18.18` / npm `>=9`. — The target machine runs Node 20 LTS; the looser `engines` range keeps the repository installable on the 18.18+ machines the master context mentions.
- 2026-09-15 — P01 — `test:ci` carries `--passWithNoTests`. — `react-scripts test --watchAll=false` with `CI=true` exits **1** when no test file exists (verified: "No tests found, exiting with code 1"); the flag is the escape hatch prompt 01 §7 anticipated and becomes a no-op once prompt 35 adds the first tests.
- 2026-09-15 — P01 — Unused `catch (err)` bindings were converted to the optional catch binding `catch {` rather than renamed to `_err`. — 16 sites, all of which already set UI state in the block; the optional binding compiles under CRA's Babel and states plainly that the error object is not used.
- 2026-09-15 — P01 — Dead code that ESLint reported was deleted rather than silenced with a `_` prefix: `extractPaginationMeta` (`api.js`), `handleOpenLeadForm` (`PropertyDetails.jsx`), `capitalize` (`seoGenerator.js`), `resultRef` (`FinanceGuide.jsx`), `isMobile` + its `useTheme`/`useMediaQuery` imports (`LeadDetail.js`), `inList` (`ArticleDetail.js`), the unused `as: Tag` prop (`AnimatedSection.jsx`) and the unused imports listed in the prompt. None of it was reachable, so behaviour is unchanged; the **product** defects behind them (the unreachable enquiry modal, the dead `visitService`/`adminService`/`PropertyDetail.js`) stay open and are listed in `docs/PROJECT_STATE.md`.
- 2026-09-15 — P01 — `HeroSection`'s search input gained `aria-controls="hero-search-suggestions"` (and the suggestion list the matching `id`) instead of disabling `jsx-a11y/role-has-required-aria-props`. — A `role="combobox"` must name the listbox it controls; the attribute is inert until the dropdown opens, so nothing about the behaviour changes.
- 2026-09-15 — P01 — `GalleryTab`'s cover-seeding effect was restructured to depend on `[formData.title, formData.gallery, updateField]` with the placeholder URL hoisted to `PLACEHOLDER_COVER_PREFIX` and an equality guard (`currentCover === defaultUrl → return`) before `updateField`. — Removes the repository's only `eslint-disable` comment; the guard makes the extra dependencies loop-safe even though `updateField` is a stable `useCallback`.
- 2026-09-15 — P01 — `console.error` in `Dashboard.js` was replaced by a one-line comment rather than left as a bare empty block. — The master context forbids swallowing errors silently; the surrounding `finally` already clears the loading state and the UI renders its empty state, and the codebase already uses this comment style in its other `catch {}` blocks.
- 2026-09-15 — P01 — `.gitignore` gained `.env` and `.env.production` even though `.env` is still tracked by git. — Ignoring an already-tracked file is a no-op; prompt 02 removes the committed `.env` (with the Cloudways URL) from the index, at which point the rule starts protecting it.
- 2026-09-15 — D5/P02 — The monogram (`BRAND.iconUrl`, the S–square–A mark) is used as-is for every derived icon: the three PNG favicons, the 180 px Apple touch icon and the 192/512/512-maskable PWA icons are Cloudinary `c_pad,b_white` renders of it, and the maskable variant is the same padded render as `icon-512.png`. — The mark occupies ~75 % of the square canvas, which already satisfies the maskable safe zone, so a second crop would only shrink it; no `favicon.ico` is generated because encoding ICO would need a dependency outside the allow-list.
- 2026-09-15 — D31/P02 — `SITE.url` reads `REACT_APP_SITE_URL` and falls back to the placeholder domain `https://www.squaresnacres.com`, stripping trailing slashes; `.env.development` sets `http://localhost:3000` and `.env.production.example` the placeholder domain. — Canonical/OG URLs must never contain a double slash, and the runtime `seoSettings.siteUrl` (prompt 35) will win over both.
- 2026-09-15 — D48/P02 — `src/services/api.js` reads `REACT_APP_API_URL` with **no** fallback and throws `REACT_APP_API_URL is not set. Copy .env.example to .env and set it.` at module load. Verified that `npm run build:ci` still succeeds without `.env.production`: CRA inlines `undefined` and the throw only fires in the browser, so the build never depends on a deployment secret. — The backend is chosen by one variable; a silent fallback would hide a misconfigured deployment (and was how the Cloudways host survived in the boilerplate).
- 2026-09-15 — D49/P02 — Inter and Manrope are loaded by a single Google Fonts `<link>` in `public/index.html` with `preconnect` to `fonts.googleapis.com` and `fonts.gstatic.com` and `display=swap`. — A `<link>` in the document head starts the request during HTML parsing, while the boilerplate's CSS `@import` in `global.css` (removed in prompt 03) delays it until the stylesheet is parsed.
- 2026-09-15 — D50/P02 — `src/assets/images/logo.png` is deleted and the header, mobile header and footer render `BRAND.logoUrl` with explicit `width`/`height` attributes in the wordmark's 2.34:1 ratio (112×48 where the CSS renders 48 px, 94×40 where it renders 40 px). — The attributes give the browser the intrinsic ratio before the image arrives, so the `width: auto; height: <n>px` CSS reserves the right box and nothing shifts; reading `siteSettings.general.logoUrl` first is added with the settings context in a later prompt.
- 2026-09-15 — P02 — The footer logo gets a temporary white rounded container (a white background, 8 px of padding, an 8 px radius and `object-fit: contain`) instead of the removed `filter: brightness(0) invert(1)`. — Master context §2.2 rule 1 forbids recolouring the logo and rule 5 requires a light container on a dark surface; the footer is still the boilerplate navy until prompt 04 moves it to `--color-surface`, which also replaces these three literals with tokens. Because `box-sizing: border-box` is global, the container keeps the existing 48 px footprint and the wordmark renders 32 px tall inside it.
- 2026-09-15 — P02 — `package-lock.json` was regenerated with `npm install --package-lock-only` so its `name`/`version` match the renamed package. — The lockfile is generated, the diff is 8 lines, and leaving the boilerplate's package name in it would have kept a trace in a file every `npm ci` reads.
- 2026-09-15 — P02 — The Cloudways URL quoted as evidence in `docs/CODEBASE_INVENTORY.md` was replaced by a description of the fallback. — Master context §3.5 requires that the host exist nowhere in the repository; moving the inventory to `docs/archive/` in prompt 03 would have hidden it from the scanner without removing it. The last occurrence of the word is the regex inside `scripts/check-traces.js`, which is the definition of the pattern and is excluded from its own scan.
- 2026-09-15 — P02 — `public/index.html` (`theme-color`) and `public/manifest.json` (`theme_color`, `background_color`) keep literal hex colours and are recorded under "Pending rewrites" for prompt 03. — A meta tag and a web-app manifest cannot reference a CSS variable; the alternative was to widen the scanner's hex allow-list from this prompt, whose scope does not include `scripts/check-traces.js`, so prompt 03 (which owns making the strict scan a gate) allows the three literals instead of the patterns being weakened.
- 2026-09-15 — D17/P03 — `scripts/check-traces.allow.json` carries three keys: `files` (whole-file exemptions, currently only `src/theme.js`), `lines` (per-line exemptions, currently the eleven `:root` palette and font declarations of `src/assets/styles/global.css`) and `colourLiterals`. Prompt 04 replaces the whole file with `{ "files": [], "lines": {} }`, which drops every exemption **and** — because `colourLiterals` then defaults to `block` — turns the colour scan into a gate in the same edit. — The prompt-04 instruction is "empty the allow file and make `check:traces` pass"; putting the colour switch in the same file means that one edit does both, and no later prompt has to touch `scripts/check-traces.js` again.
- 2026-09-15 — D17/P03 — Line exemptions store the SHA-256 of the line's trimmed text, not the text itself, and a finding on an allow-listed line is reported (with `allow-listed line no longer matches its recorded content`) as soon as the hash stops matching. — Storing the text would put the very literals being purged into a scanned file; hashing satisfies the "match by content as well" requirement without reintroducing the trace. Verified by inserting a line into `global.css`: the check goes back to exit 1 and names the shifted lines.
- 2026-09-15 — P03 — The ~1 500 hex literals in JS/JSX and CSS modules, and the eight boilerplate palette-hex trace patterns, are reported but do **not** fail `npm run check:traces` in this prompt (`colourLiterals: "report"`). — Prompt 03 §10 states that hex literals remain and belong to prompt 04, and §12 forbids touching component styling here; making them blocking now would make the acceptance criterion "`check:traces` exits 0" unsatisfiable. Brand strings, domains, fonts, hosts, storage keys and CSS class prefixes — the 446 findings this prompt owns — are all blocking and all cleared.
- 2026-09-15 — P03 — `docs/CODEBASE_INVENTORY.md` moved to `docs/archive/CODEBASE_INVENTORY.md` and the trace literals quoted in `docs/PROJECT_STATE.md` and `docs/DECISIONS.md` were rewritten as descriptions ("the dotted brand string", "the placeholder-image host") rather than skip-listed. — The move was already booked as a prompt-03 pending rewrite and is where `00_MASTER_CONTEXT.md` §4.1 puts the finished file; neutralising the two live state files keeps `docs/**` inside the D17 scan, so a brand string reintroduced in a future prompt's report is still caught. Every later prompt must describe traces instead of quoting them.
- 2026-09-15 — P03 — `docs/QA/**` is skip-listed in `scripts/check-traces.js`. — QA artefacts are verbatim tool output (`03-traces-before.txt` is the scanner's own report and necessarily contains its findings); neutralising them would falsify the evidence, and the prompts from 44 onward write more of them.
- 2026-09-15 — P03 — `global.css` gained `--font-serp` (`arial, sans-serif`) and `--font-mono`, and the Google-SERP preview mocks in `AdminSeo.js`/`ArticleForm.jsx` plus the code/slug fields now read them. — `00_MASTER_CONTEXT.md` §4.3 forbids inline `fontFamily` anywhere, but the SERP preview must keep rendering in Arial to be an honest preview and monospace has no existing token; two named non-brand tokens satisfy both, and no palette value was touched.
- 2026-09-15 — P03 — The property draft key is `sna_property_draft`, without the `:<id|new>` suffix of `00_MASTER_CONTEXT.md` §4.2. — Prompt 03 is a rename-only pass ("behaviour is otherwise unchanged"); per-property drafts are a behaviour change that belongs to the property-form prompts (18–21), which own the key's final shape.
- 2026-09-15 — P03 — The boilerplate marketing copy in `db.json` (`siteSettings.companyDescription`, `tagline`) was replaced with the same neutral placeholders as the `Footer.jsx` defaults, and the FAQ answers' contact details with the §14 placeholders. — The code defaults are only a fallback; leaving the seed as-is would have kept the old copy on screen, which §6 of the prompt forbids. The seed is rewritten wholesale by prompt 10.
- 2026-09-15 — P03 — `@mui/icons-material` was uninstalled but stays named in the `no-restricted-imports` rule in `package.json`. — The rule is required by `00_MASTER_CONTEXT.md` §3.4/D36 and is what stops the package coming back; the acceptance criterion is about the dependency list, and `npm ls @mui/icons-material` reports it absent.
- 2026-09-15 — D2/P04 — `sweetalert2`, `react-slick`, `slick-carousel`, `react-countup` and `react-intersection-observer` were uninstalled in one step after every call site had been rewritten, and `date-fns@4.4.0` pinned exactly (not `^4.4.0`) like the rest of the §3.3 allow-list. — The five removals are mechanical once the replacements exist (`ui/Carousel`, `useCountUp`, `useInView`, `ConfirmDialog` with `variant="alert"`), and an exact pin is what "exact pins, nothing else may be added" means.
- 2026-09-15 — D2/P04 — `useCountUp`'s `enabled: false` holds the value at `start`, not at `end`. — The one caller (`BuilderOverview`) passes `enabled={inView}`, so "not enabled yet" must mean "has not counted yet"; returning the final value first would make the counter jump final → 0 → final the moment the section scrolls in. Reduced motion still jumps straight to `end` once enabled, which is what the prompt's edge case asks for.
- 2026-09-15 — D3/P04 — The footer's temporary white logo container (prompt 02) is gone, not replaced: the footer surface is now `--color-surface`, so the wordmark sits on it directly. Verified in a headless Chromium: `footer` computes to `rgb(247, 247, 248)` with `rgb(31, 31, 31)` text, and the wordmark's wrapper computes to `rgba(0, 0, 0, 0)`.
- 2026-09-15 — D4/P04 — The old-token compatibility layer described by prompt 04 §4.1 was never added. — The migration is script-driven and rewrites every CSS module in one pass, so the layer would have existed only between two commands inside this session and the prompt requires it deleted before committing anyway. The end state is the one it prescribes: `grep` for `--color-bg-surface`, `--color-secondary*`, `--spacing-*`, `--font-number`, `--color-text-primary`, `--color-bg-default`, `--color-bg-paper`, `--color-text-light`, `--shadow-xl`, `--z-fixed`, `--z-modal-backdrop` over `src/` returns 0.
- 2026-09-15 — D4/P04 — Mapping rules applied to the boilerplate palette: navy (`--color-primary` and its literals) → `--color-charcoal` for headings, dark sections, tooltips and table headers, and → `--color-primary` (red) for buttons, active states and badges; gold (`--color-secondary*`) → `--color-primary` for fills/borders/icons and `--color-primary-dark` for every text colour (§2.4: red text on a surface is 4.44:1 and fails AA, the dark variant is 5.76:1); the nine sites where navy was a large dark surface are listed by file and line in the migration script rather than guessed by regex. Purple, teal, indigo and pink accents fold into the red and info families; the six real bank brand colours were dropped with the `DEFAULT_BANKS` colour field.
- 2026-09-15 — P04 — `global.css` carries eleven tokens beyond §2.4: the three social colours the prompt asks for plus `--color-instagram` / `--color-youtube` (the settings form edits five social links, not three), three Google SERP colours (`--color-serp-title/-url/-text`, the colour counterpart of prompt 03's `--font-serp` — a preview is only honest if it looks like the real result), and a translucent group (`--color-overlay-strong`, `--color-scrim`, `--color-on-dark`, `--color-on-dark-subtle`, `--color-on-dark-border`, `--color-glass`, `--color-primary-tint`, `--color-primary-ring`). — The ~120 `rgba()` values the CSS modules carried needed somewhere to live: `color-mix()` is not safe against this browserslist, and alpha values in component CSS would be exactly the literal the scan forbids. `src/theme.test.js` asserts the complete token-name set, so a future addition has to be deliberate.
- 2026-09-15 — P04 — `scripts/check-traces.js` gained `public/index.html` and `public/manifest.json` to `HEX_ALLOWED_FILES` instead of an entry in the allow file. — The prompt requires `scripts/check-traces.allow.json` to be `{ "files": [], "lines": {} }`; a `theme-color` meta tag and a PWA manifest cannot reference a CSS variable, so their literals are structural (like `public/brand/*`, already exempt) rather than a styling decision that could be migrated. This closes the prompt-02 pending rewrite that prompt 03 deferred.
- 2026-09-15 — P04 — `src/theme.test.js` asserts the palette by _shape_ — the `--color-*` token names are exactly the documented set, every hex in `global.css` sits inside a token declaration, and the twenty mirrored values equal `theme.palette.*` — instead of grepping for the boilerplate hexes. — Naming them in a test file would put the very literals being purged back into `src/`, where both `check:traces` and the acceptance grep would (correctly) report them. The string check lives in `npm run check:traces`, which scans the whole repository and is the right owner.
- 2026-09-15 — P04 — `scripts/contrast-check.js` gates the pairs that must reach 4.5:1 (body text) or 3:1 (icons, borders, the focus ring) and prints three pairs as informational: `--color-warning` on white (2.68:1 — §2.4 says it is never text), and the two border tokens (decorative boundaries, not control state indicators). The focus ring is measured against the page backgrounds, never against the control it surrounds, because `outline-offset: 2px` puts it outside the control. — 29 gated pairs, all passing; a hard gate on documented-by-design values would make the script unsatisfiable without changing tokens the master context fixes.
- 2026-09-15 — D51/P04 — Every CSS media query in `src/` is now one of `599.98/600` or `899.98/900`; the boilerplate's 960/961, 768 and 1024 queries are gone, and `MainLayout` switches header variants through `useBreakpoint()` (MUI `md`). Verified in a headless Chromium at 899, 900, 930, 959 and 960 px: exactly one `header` is visible at each width (65 px below 900, 73 px at and above it) and `scrollWidth === innerWidth`. This closes ADD-05.
- 2026-09-15 — D52/P04 — `Header` and `MobileHeader` no longer hide on scroll-down; they keep a sticky position and gain `--shadow-sm` once `scrollY > 10`. Only `BottomNav` hides, via `useScrollDirection`. Verified at 390 px: the nav's transform goes to `translateY(65px)` after scrolling to 900 and back to `none` after scrolling up, while the header keeps its shadow throughout.
- 2026-09-15 — D53/P04 — `ui/Section` defaults to `background="none" spacing="none" container="flush"`. — The thirteen pages it replaces already style their own sections, so neutral defaults make the swap a pure import change and keep page structure identical; a design-system band opts in explicitly (`<Section background="surface" spacing="lg" container="default">`), which is what the new sections of prompts 26–27 will do.
- 2026-09-15 — D54/P04 — The single `ToastProvider` replaced **thirteen** local MUI `Snackbar`s, not the two the prompt's context names (`AdminLayout` and `UserManagement`): eleven admin pages carried their own `snackbar` state as well. The acceptance criterion ("`grep -rn "Snackbar" src` shows only `ToastProvider.jsx`") only holds if all of them go, and the rewritten provider uses no `Snackbar` at all, so the grep now returns nothing.
- 2026-09-15 — D33/P04 — `formatDate` post-processes `Intl.DateTimeFormat`'s month part to three letters. — Node's ICU renders September as "Sept" under `en-IN`, while §2.4/D22 specifies `05 Sep 2026`; slicing the `month` part of `formatToParts` keeps the IST timezone and the locale's ordering while fixing the abbreviation.
- 2026-09-15 — P04 — Data records carry a `tone` name instead of a colour: `LEAD_STATUS_CONFIG`, `ARTICLE_CATEGORY_TONES`, `FAQ_CATEGORY_TONES`, `TAG_OPTIONS`, `UserManagement.ROLES`, the `FinanceGuide` score bands and credit-score options, the `ConstructionStatus` statuses and the `InteriorDesigning` room swatches. `toneStyles(tone)` in `src/components/ui/tones.js` resolves a tone to `{ color, background, border }` of `var()` references. `DEFAULT_BANKS` lost its `color` field entirely — the six values were real bank brand colours, which §14 forbids.
- 2026-09-15 — P04 — The `<select>` chevrons that were `data:image/svg+xml` URIs with a `%236B7280` fill are now two CSS gradients coloured by `--color-text-muted`. — `%23xxxxxx` is a URL-encoded hex literal; only `%23ffffff` is tolerated (§4 task 10), and a gradient chevron needs no encoded colour at all.
- 2026-09-15 — P04 — `src/test-utils.jsx` holds the shared render helper rather than living in `src/components/ui/__tests__/`. — CRA's Jest `testMatch` treats every file under `__tests__/` as a test file, so a helper there fails the run with "Your test suite must contain at least one test"; the root-level name matches neither `testMatch` pattern and is imported only by tests, so it never reaches the bundle.
- 2026-09-15 — P04 — `LazyImage` sets its ratio through a `--ratio` custom property rather than an inline `aspect-ratio`. — jsdom's CSSOM drops `aspect-ratio`, so the "no CLS" assertion could not be tested; a custom property survives, and the CSS module applies `aspect-ratio: var(--ratio, 4 / 3)`.
- 2026-09-15 — P04 — The `Dashboard` donut and bar charts read their colours through `useCssVars` instead of passing `var()` into SVG presentation attributes. — `stroke="var(--color-primary)"` is an attribute, not a CSS declaration, and its resolution is unreliable; `getComputedStyle(document.documentElement).getPropertyValue(...)` gives the chart a concrete value while the palette stays in `global.css`.
- 2026-09-15 — D36b/P05 — `src/config/enums.js`, `src/services/endpoints.js`, `src/services/schemas/*.js`, `mock-server/schemas/models.js` **and `src/config/rbac.js`** are CommonJS. — The master context names the first four; `rbac.js` joins them because prompt 05 §9 verifies the sidebar with `node -e "require('./src/config/rbac').getNavItemsForRole('sales')"`, which an ESM file cannot answer. Verified that `import { ROLES } from '../config/rbac'` still compiles: webpack and Babel interop the named exports, and the three existing consumers (`AdminLayout`, `AdminAuthContext`, `routes/index.js`) were not touched.
- 2026-09-15 — P05 — `LEAD_SOURCES` carries a 29th value, `other` "Other", beyond the 28 the master context §6.17 enumerates. — The prompt's own verification pins the count at 29 (`LEAD_SOURCES.values.length` → 29) while §6.17 lists 28, so one value is missing from the list rather than from the count. `other` is the value the model actually needs: it is where an unmapped legacy source, an imported lead and a future form land, and adding it does not change the meaning of any of the 28.
- 2026-09-15 — D20/P05 — `LEGACY_LEAD_SOURCE_MAP` has 24 entries: the 23 §6.17 lists plus `child_form → property-enquiry`. — §11 BUG-09 says the boilerplate has "24 form values incl. `child_form`, `website`" and `PropertyDetails.jsx:449` still writes `child_form` for the in-page enquiry form, so a 23-entry map would silently drop those leads in the prompt-10 conversion. `enums.test.js` asserts all 24 old values resolve to a canonical source.
- 2026-09-15 — P05 — `BLOCK_TYPES` has **25** types, not the 23 §6.17 names. — §6.10's table is the definitive list and enumerates 25 rows, and D27 says five of them (`jobs`, `facts`, `expandableCards`, `packages`, `gallery`) are additions to the master list of 20 — 20 + 5 = 25. The "23" is a miscount of the same table; implementing 23 would mean dropping two CMS blocks the seed pages need. `enums.test.js` pins the exact list in §6.10's order.
- 2026-09-15 — P05 — The master context's tone names `muted` (lead priority `low`, article status `draft`, timeline `upcoming`, SEO band `none`) and `charcoal` (badge colour) are spelled `neutral` in `enums.js`. — Prompt 04 fixed the tone vocabulary at `TONES = ['neutral','primary','success','warning','error','info']` in `src/components/ui/tones.js`, and `toneStyles()` silently falls back to `neutral` for anything else, so a `muted` record would render correctly while naming a tone that does not exist. §12 of this prompt forbids touching UI components, so the enum adapts to the vocabulary rather than the other way round; `enums.test.js` asserts every tone in every enum is a member of `TONES`.
- 2026-09-15 — P05 — Six enums exist beyond §6.17: `BADGE_TONES`, `NEWSLETTER_STATUS`, `BULK_ACTIONS`, `SITEMAP_CHANGEFREQ`, `SEO_TEST_STATUS` and the derived `LEAD_PIPELINE`. — Each is a value set the contract already fixes somewhere else (§6.4 badge colours, §6.14 subscriber status, §5.8 bulk actions, §6.14 `sitemap.changefreq`, §9.6 `analysis[].status`, §6.17's "pipeline order = this order") and each is needed by a schema descriptor written in this prompt. §6.17 says enums.js is "the only place" for enumerated values, so declaring them here is the rule, not an exception.
- 2026-09-15 — D14/P05 — `docs/DATA_MODEL.md` reserves an id range per collection (properties 1–60, localities 1–30, media 1–400, …) on top of the `max(id)+1` rule. — The seed hand-writes cross-references (`similarPropertyIds`, `lead.propertyId`, `page.blocks[].data.ids`), and a reserved ceiling is what stops a record added by a later prompt from colliding with one of them.
- 2026-09-15 — D38/P05 — §6.15 is reproduced in `docs/DATA_MODEL.md` as the "HOM → SNA field mapping" with its own introduction rather than as a bare table. — It is the contract prompt 10 executes against; naming it as such makes the doc usable without the master context beside it.
- 2026-09-15 — P05 — `mock-server/schemas/models.js` composes each collection's writable part from `src/services/schemas/*` and adds only the read-only embeds and server-managed fields. — Writing both by hand would give two independent copies of ~50 property fields that drift on the first change; composing them means the request contract and the storage contract cannot disagree. The honeypot `website` is destructured out of the lead and job-application shapes so it is validated but never stored (§5.11).
- 2026-09-15 — P05 — The field mini-language gained five keys beyond the list in prompt 05 §5: `pattern`, `accepts`, `unique`, `read`/`serverManaged`/`secret`, and `note`; the collection descriptor gained `publicRead`. — `redirect.fromPath` must start with `/`, `lead.source` must tolerate the 24 legacy values, `newsletterSubscribers.email` is unique, §6 marks many fields `(read)` or server-managed, `adminUsers.password` and `apiTokens.token` must never be returned, and §5.10 says `leads`, `media`, `adminUsers`, `apiTokens`, `jobApplications`, `newsletterSubscribers` and `propertyViews` have no public endpoint at all — `publicScope: null` only means "not scoped", which is the opposite. All six are documented in `mock-server/schemas/README.md`.
- 2026-09-15 — D23/P05 — The registry declares `perPage` on every list endpoint but no default value; the defaults (12 public, 20 admin, 6 similar, `all` on admin) live in `docs/API_CONTRACT.md` §5.6 and in the mock. — A default in the registry would be sent as an explicit parameter by the client and would then override whatever the server decides, which is exactly the coupling the registry exists to avoid.
- 2026-09-15 — P05 — Query descriptors use `enum:`/`csv:enum:` built from `src/config/enums.js` wherever §5.15's excerpt shows a loose `string` (`availability`, `areaUnit`, `furnishing`, `facing`). — The excerpt is explicitly an excerpt; deriving the descriptor from the enum means the mock's parameter validation and the generated Postman/OpenAPI artefacts stay correct when an enum changes, and `endpoints.test.js` checks every descriptor against the grammar.
- 2026-09-15 — P05 — The 21 uniform admin CRUD groups are built by an `adminResource()` factory instead of 154 hand-written literals. — §5.14 defines them as one rule ("same CRUD + bulk, + check-slug where slugged"), so expressing them as a rule is what keeps them identical; the factory emits exactly the §5.15 entry shape, and `endpoints.test.js` asserts the shape, the keys and the resulting paths against a hardcoded list, so the generated entries are verified the same way the literal ones are.
- 2026-09-15 — P05 — `/admin/media` and `/admin/users` get the full CRUD + bulk set even though §5.14's detailed rows show only a subset for media. — The generic row lists both among the resources with "same CRUD + bulk", and the detailed row is a subset of it, not a contradiction; giving the mock the complete set costs nothing and avoids a missing-route surprise in prompt 39.
- 2026-09-15 — D89/P05 — `POST /admin/leads/:id/claim` is declared `auth: 'user'` in the registry while `PERMISSIONS.leads.claim` is `[sales]`. — The two answer different questions: `auth` is the minimum the route requires (a valid token), `can()` is what the role may do once inside. Sales is the role the action exists for — an admin or manager assigns instead — so the UI hides the button for them while the route itself stays open and the API applies the matrix. `docs/RBAC.md` states this explicitly.
- 2026-09-15 — D88/P05 — The 409 of a master-data delete is documented in `docs/API_CONTRACT.md` as an `Error` body that additionally carries `data.usedBy`. — §5.14 defines the shape but §5.3 says `errors` alone; spelling out the union in the Error shape is what lets the UI render the usage list without guessing.
- 2026-09-15 — P05 — `ROUTE_PERMISSIONS` no longer has an entry for `/admin/neighborhoods`, and `NAV_ITEMS` no longer links it. — §7's route map is the final one and replaces the boilerplate's neighbourhoods screen with `/admin/master-data/localities` (D13). The old route still renders — `src/routes/index.js` guards it with a hardcoded `RoleRoute` and is out of this prompt's scope — and nothing calls `hasRouteAccess` for that path today, so the removal changes no behaviour. Prompt 12 moves the routes onto `ROUTE_PERMISSIONS`; prompt 14 replaces the screen.
- 2026-09-15 — D25/P05 — The registry's `example` values are the slugs prompt 10 must seed (`lakeview-heights-3-bhk-whitefield`, `karnataka-rera-guide-for-homebuyers`, `whitefield`, `aurelia-estates`, `about`, `real-estate-advisor-bengaluru`, plus `editorial-team` for the author route). — `scripts/smoke-api.js` and the guidelines generator resolve `:slug` from this field, so the seed and the registry have to agree; `endpoints.test.js` pins the six the prompt names so a seed rename cannot pass silently.
- 2026-09-15 — P05 — `scripts/check-endpoints.js` applies its path-literal rule only to `src/services/*.js` (top level), while the `http.`/`axios.` rule applies to all of `src/`. — Router paths are indistinguishable from API paths as string literals (`'/properties'`, `'/admin/leads'` appear 30+ times in components and are `react-router` targets), so a repository-wide literal rule would be unsatisfiable. The narrow rule still catches what it is for: an API path reappearing in a service. Both rules were verified against a deliberate violation.
- 2026-09-15 — D36/P05 — `scripts/check-endpoints.allow.json` is a flat array of file paths, and the script prints the allow-list entries that no longer produce a finding. — Prompt 11 empties it to `[]`; the "no finding left" line is what tells that prompt an entry is ready to go, so the exemption cannot outlive the file it excuses.
- 2026-09-15 — D56/P05 — `lead.meta` is typed `{ type: 'object', nullable: true }` with no shape. — It deliberately holds source-specific payloads (the financial-assessment answers and score, a bank name, a workspace size); constraining it would be the data loss D56 exists to prevent. The admin lead detail renders it as a key/value card.
- 2026-09-15 — D58–D61/P05, D76–D78/P05 — The seed-conversion decisions of §6.15 (tags → `isFeatured` + `badgeIds`, `sections` → `sectionVisibility`, `specialities` → `highlights` strings, old `floorPlans` → both `unitConfigurations` and `floorPlans`, the four article categories, FAQ `finance` → `home-loan`, partners → `category: 'developer'`) are reproduced verbatim in `docs/DATA_MODEL.md`'s mapping table and encoded in the enums they depend on (`FAQ_CATEGORIES` has `home-loan` and no `finance`; `PARTNER_CATEGORIES` has `developer`; `SECTION_VISIBILITY_KEYS` has the 18 post-mapping keys). — Prompt 10 applies the mapping; the enums are what make a wrong value fail validation rather than reach the seed.
- 2026-09-15 — D90–D91/P05 — `PRICE_BUCKETS_RENT` is documented as serving lease listings too, and `LEAD_SOURCES` keeps `careers` next to the separate `jobApplications` collection. — Both are the master context's own resolutions of a contradiction (§13.1); recording them in the enum comments is what stops a later prompt from "fixing" either one.
- 2026-09-15 — D9/P06 — JSON Server 0.17.4 runs as a library inside Express, and its router is mounted **last**, after the admin-prefix, query-translation, public-scoping, validation and timestamp middleware, with `router.render` replaced by the envelope. — The CLI cannot produce the §5.2 envelopes, the §5.3 errors or the `PUT`/`PATCH` semantics, and a custom route has to be able to win over the CRUD fallback (`mock-server/routes/index.js` is the array prompts 07–09 fill).
- 2026-09-15 — D9/P06 — The generic `DELETE` is implemented in `mock-server/db.js` (`removeRecord`) and handled in `app.js` before the JSON Server router. — JSON Server's own `destroy` additionally removes every record whose foreign key does not resolve (`lib/server/mixins.js` `getRemovable`), and `_.getById(collection, null)` is `undefined`, so a lead with `propertyId: null` counts as unresolved: deleting any record would silently take unrelated rows with it. Verified before and after: the six seed leads survive a delete.
- 2026-09-15 — D14/P06 — `PUT` re-applies the model defaults **recursively** (`mergeDefaults`), so `PUT { pricing: { price } }` keeps `pricing.currency`, and an object field that declares both a `shape` and a `default` gets the shape's defaults with the declared default on top. A declared `null` default (`lead.requirement`, `lead.utm`) still wins outright. — §5.8 says missing optional fields become their defaults; a top-level spread would have applied that to the record's first level only and wiped every nested key the form did not send.
- 2026-09-15 — P06 — `required` is enforced on `PUT` even for fields that declare a default, but not on `POST`. — The two rules the prompt states are otherwise in conflict: `PUT /admin/properties/1 { title: 'X' }` must be a 422 that names `listingType` (required, default `sale`), while `POST /leads` must succeed without `status` or `priority` (required, defaults `new` and `medium`). A create lets the server supply the §6 defaults; a replace states the whole record, so an omission there is the silent wipe §7 of the prompt calls out.
- 2026-09-15 — P06 — `publicRead: false` hides a collection from public **reads** only, and returns 404 rather than 403. — §5.10 says `leads`, `media`, `adminUsers`, `apiTokens`, `jobApplications`, `newsletterSubscribers` and `propertyViews` have no public endpoint, but §5.14 defines public `POST /leads`, `POST /newsletter/subscribe` and `POST /jobs/:id/apply`; a blanket block would have broken every lead form. 404 rather than 403 because the existence of the collection is not public information either.
- 2026-09-15 — P06 — Fields marked `secret` in the schema descriptors (`adminUsers.password`, `apiTokens.token`) are stripped from every response of the generic router. — The descriptor already says "never returned by any endpoint"; enforcing it in `renderEnvelope` means the mock cannot leak the seeded passwords while `/admin/*` is unauthenticated (prompts 06 → 07), and prompt 07's `/auth/login` builds its own envelope so it is unaffected.
- 2026-09-15 — P06 — The two singletons are merged into on `PUT` as well as on `PATCH`, one level deep per branch. — §5.14 says `PUT /admin/settings` deep-merges known keys, and a singleton's top-level keys (`general`, `hero`, `footer`…) _are_ the object fields the prompt's `PATCH` rule names, so the same rule applied at the right level. Without it a `PATCH { newsletter: { title } }` dropped `enabled`, `subtitle` and `successMessage`.
- 2026-09-15 — D19/P06 — `MOCK_PORT` 4000, `MOCK_DELAY_MS` 0, `MOCK_TOKEN_TTL_HOURS` 24, `MOCK_FRESH` off; CORS allows `http://localhost:3000`, `http://127.0.0.1:3000` and `http://localhost:5000`. — §3.5 and §5.12; all four were already documented in `.env.example` by prompt 02 and are now read by `mock-server/config.js`.
- 2026-09-15 — D21/P06 — `/sitemap.xml`, `/sitemap-*.xml`, `/robots.txt`, `/rss.xml` and `/llms.txt` are rewritten onto `/api/...` by one middleware at the top of the pipeline, so the mirror keeps working when prompt 09 implements the real routers. Until then both paths answer `501 { message: 'Not implemented until prompt 09' }`. — A 501 says "planned, not built"; a 404 would be indistinguishable from a typo in the path.
- 2026-09-15 — P06 — `scripts/validate-seed.js` treats "every field of §6 present" as a rule: a field whose descriptor is not `read` must exist on the record, `null` and `[]` included. Read fields (the embeds and the computed counters) are exempt because the API derives them. — This is what makes the starter seed complete rather than merely valid, and it is why no screen can read `undefined` off a seeded record. Verified against a deliberately broken copy: 12 injected defects, 12 reported.
- 2026-09-15 — P06 — `validate-seed.js` adds `adminUsers.email` to the uniqueness check through an explicit `EXTRA_UNIQUE` map rather than through the descriptor. — §6.14 says the e-mail address is unique, but `adminUsers` composes its fields from `masterData.user`, which is also the body shape of a `PUT` that keeps the record's own address; marking it `unique` there would be wrong for the write contract.
- 2026-09-15 — P06 — The seed and `--stats` cover **28** collections and singletons, not the 27 of prompt 06 §8. — `mock-server/schemas/models.js` (prompt 05) declares 28 and §6 enumerates 28; the prompt's own task 8 lists all 28 by name, so the acceptance line is a miscount of the same list.
- 2026-09-15 — P06 — The six starter properties reproduce the _structure_ of the six HOM records under the §6.15 mapping — one rent listing, one under construction with a timeline, one villa, plus a lease office floor and a plotted layout — but not their names. — §14 forbids real developer and project names, and every HOM record carried one (`NEW-19` in the prompt-01 audit); the mapping is about field shapes, and keeping the real brands would have failed both §14 and `check:traces`' successor checks.
- 2026-09-15 — P06 — Seed properties carry `reraNumber: null` and `reraRegistered: false`, and the developers carry `reraIds: []`. — §14 allows a labelled placeholder for the company's own RERA number (which `siteSettings.general.reraNumber` carries) but a per-project registration number is a fact about a real regulator's register; leaving it null is the honest default, and the "RERA Registered" badge stays in master data for real listings to use.
- 2026-09-15 — D19/P07 — A token is 48 characters of `crypto.randomBytes(36).toString('base64url')`, opaque, stored in `apiTokens` with `expiresAt = now + MOCK_TOKEN_TTL_HOURS`, and expired records are deleted lazily (when the token is presented, and in bulk on every login) rather than by a timer. — §6.14 pins the length and §5.4 pins the lifetime; 36 bytes are exactly 48 base64url characters, so nothing is padded or truncated away. Laravel Sanctum owns the real tokens, so nothing about the content may leak into the contract, and a mock that keeps its tokens in the runtime db is a mock a restart does not sign you out of. A timer would keep the process alive and would still have to be re-checked on use.
- 2026-09-15 — D19/P07 — `mock-server/config.js` parses `MOCK_TOKEN_TTL_HOURS` with `parseFloat` instead of `parseInt`. — §7 of prompt 07 requires `MOCK_TOKEN_TTL_HOURS=0.01` to expire a session after 36 s (prompts 12 and 45 test the client-side auto-logout with it); `parseInt('0.01')` is `0`, which would have expired every token immediately. The default (24) and the other three `MOCK_*` variables are unchanged. This is the one file outside prompt 07's declared scope that had to change, and the change is confined to that variable.
- 2026-09-15 — P07 — Authentication and the role matrix are mounted **once**, on `/api/admin` (plus `requireAuth` on `/api/auth/logout|profile|password`), before every router, instead of being applied per route. — The generic JSON Server router answers admin paths that no hand-written router declares, so a per-route guard would leave `/api/admin/<anything>` open. Mounting first also means prompts 08 and 09 inherit `req.user` without repeating the matrix.
- 2026-09-15 — P07 — `mock-server/lib/routePermissions.js` derives the permission from the path and the method (`GET`→`view`, `POST`→`create`, `PUT`/`PATCH`→`edit`, `DELETE`→`delete`), lets a path segment name the action (`bulk`, `export`, `duplicate`, `check-slug`, `notes`, `claim`), and falls back to the nearest action the area declares. — §7's matrix is keyed by area and action while HTTP speaks paths; the fallback is what makes `POST /admin/leads/7/notes` a `leads.edit` (which sales hold) rather than a `leads.create` the matrix never defines, and `GET /admin/properties/check-slug` a `properties.create`, which is the minimum role the registry declares for it.
- 2026-09-15 — P07 — An admin path with no rule in `routePermissions.js` is **denied** (403), not served. — It is the difference between "a route is protected once somebody remembers to protect it" and "a route is protected until somebody declares what it needs". It also hides the two mock-only collections: `/api/admin/adminUsers` and `/api/admin/apiTokens` answer 403 for every role, while `/admin/users` is the declared way in. Both kebab-case paths (`/admin/property-types`) and camelCase collection names (`/admin/propertyTypes`) are mapped, because the generic router answers the latter today and prompt 09 adds the former.
- 2026-09-15 — D15/P07 — The server side of the sales scope is the matrix alone: sales hold `properties.view`, `leads.view|edit|export|claim` and `dashboard.view`, so `GET /admin/properties` is 200 and `POST`/`PUT`/`PATCH`/`DELETE` are 403, master data, articles, content, SEO, media, settings and users are 403 outright, and `DELETE /admin/leads/:id` is 403 while a note on a lead is allowed. — Row-level scoping (`assignedTo == me OR null`) is a property of the lead data, not of the route, so it belongs to the leads router of prompt 08; this prompt makes sure the route-level half cannot be bypassed.
- 2026-09-15 — P07 — Every 401 says `Unauthenticated.` — missing header, wrong scheme, unknown, expired and revoked alike — with one exception, `Account is inactive.` for a deactivated account. — A response must not tell an attacker whether a token was ever valid; the deactivated account is the one case where the reader is the token's owner and needs the reason. For the same reason an inactive account's login answers `Invalid email or password.` rather than naming the account.
- 2026-09-15 — P07 — `POST /auth/login` returns the six-field `user` of §5.4 while `GET /auth/profile` and `PUT /auth/profile` return the whole record minus `password`. — §5.4 spells out the login payload (`id, name, email, role, avatarUrl, phone`) and §5.14 types the profile responses as `User`; the client stores the small one in `sna_auth_user` and re-reads the full one on every route change.
- 2026-09-15 — P07 — In `/admin/users` the last-active-admin guard runs **before** the "not yourself" guards, and a `PUT` on your own record silently keeps your role while a `PATCH` that changes it is a 422 naming `role`. — Because `users.*` is admin-only, an admin acting on somebody else always survives their own change, so the last-admin rule is only ever reachable on a self-change; checking it first is what makes it reachable at all, and both answers are a 422 with the same `errors.id` key, so only the human sentence differs. §7 of prompt 07 requires the `PUT` to keep the role (the user form posts the whole record and must not fail on a field it cannot change) and the `PATCH` to be refused (a role change is what a `PATCH { role }` is for).
- 2026-09-15 — P07 — `PRIVATE_COLLECTIONS` in `publicScope.js` answers 404 on the public prefix for every method, with an explicit `PUBLIC_WRITES` exception for `POST /leads`. — §5.10 hides the seven collections and §5.14 defines the public lead form on exactly that path; prompt 09 replaces the generic `POST /leads` with the honeypot-and-activities router, and until then removing the exception would have broken a working endpoint (prompt 06 documented it as working).
- 2026-09-15 — P07 — The mock's tests use Node's built-in `node:test`/`node:assert`, start `createApp()` on an ephemeral port over a per-test copy of the seed in `os.tmpdir()`, and run through `npm run test:mock` = `cd mock-server && node --test`. — §3.3 allows no new dependency, so Jest (which belongs to `react-scripts` and its jsdom environment) is not an option for a server suite. A copy per test keeps `db.json` and `mock-server/.runtime/db.json` untouched and gives every test a fresh rate-limit counter, which is what makes the 429 case testable. The script runs `node --test` from inside the directory rather than passing it one: Node 22 no longer expands a directory argument and Node 20 does not expand a glob, so the cwd form is the only one that works on both (the repository pins Node 20 in `.nvmrc` and accepts ≥ 18.18).
- 2026-09-15 — D68/P08 — A `bedrooms` filter matches the property's own `configuration.bedrooms` **or** the `bedrooms` of any of its **active** `unitConfigurations`, and a value of 5 or more means "that many or more". A plot or a commercial floor has neither, so it never matches. — §5.7 states the rule; the mock implements it on the stored record rather than on a denormalised column so that a project quoting 2 and 3 BHK units is found under both, which is what a buyer filtering "3 BHK" expects. The facets count a property under every bucket it can be found under for the same reason: a facet count that does not match what the filter returns is worse than no facet.
- 2026-09-15 — D95/P08 — `viewCount` and `enquiryCount` are `serverManaged` in the schema, so they are stripped from every request body; the only two places they move are `POST /properties/:id/view` and `POST /leads`. — Integrity: a counter a client can set is not a counter. `PUT` preserves them through `serverManagedValues()`, and `duplicate` resets them to 0 because the copy has not been seen by anyone.
- 2026-09-15 — P08 — `POST /properties/:id/view` counts one view per IP per property per hour, from an in-memory map that a restart forgets, and it does **not** touch `updatedAt`. — §5.14 asks for the debounce; keeping it in memory is what "mock, not analytics" means. Leaving `updatedAt` alone matters because `relevance` sorts by it: counting a view must not reshuffle the listing.
- 2026-09-15 — P08 — `possessionBy=yyyy-mm` matches a listing whose `possessionDate` falls on or before the last day of that month, **or** whose `constructionStatus` is `ready-to-move` or `resale`. — §5.7 says "or ready-to-move"; a resale flat carries no `possessionDate` either and is just as available now, so excluding it would answer "nothing is available by December" for half the seed. The simplest robust reading, logged here because the master context does not spell it out.
- 2026-09-15 — P08 — `ids=3,1` returns those listings in that order and skips sorting entirely. — §5.7 says the order is the one asked for; applying the default `relevance` sort afterwards would have silently undone it (it did, until a test caught it).
- 2026-09-15 — P08 — A `PATCH` re-derives the slug only when the body carries `slug`; a `PATCH` that renames the title leaves the URL alone. — §5.9 auto-generates from the title "when the client sends an empty slug", and a partial update that has not mentioned the slug has not sent an empty one. Silently moving a public URL because somebody fixed a typo in a title is the kind of change that costs rankings (§9).
- 2026-09-15 — P08 — `POST /admin/properties/:id/duplicate` copies the record as an inactive, unfeatured draft titled `… (Copy)` at `<slug>-copy` (then `-copy-2`), with `viewCount`, `enquiryCount` and `publishedAt` reset and `seo.score` set to `null` / `scoreBand` `none`. — §5.14; the score is an answer about a page that does not exist yet, so it is cleared rather than copied and the SEO panel recomputes it.
- 2026-09-15 — P08 — `DELETE /admin/properties/:id` also removes the id from every other listing's `similarPropertyIds`. — Nothing else would: the array is plain data, and a dangling id would make `/properties/:id/similar` quietly return fewer results than the editor chose. The same delete is why `similar` skips picks that are no longer active instead of erroring.
- 2026-09-15 — D96/P08 — The lead list's `from` and `to` compare the **UTC date** of `createdAt` (`createdAt.slice(0, 10)`), inclusive at both ends. — §7 of prompt 08 poses the question and answers it: the server has no notion of the viewer's timezone, and an API that answers "created on 05 Sep" differently depending on where it runs is worse than one that answers in UTC and documents it. The admin UI labels the filter accordingly (prompt 29).
- 2026-09-15 — P08 — Round-robin auto-assignment derives the next sales user from the leads themselves — the most recently created lead that went to an active sales user decides who is next — instead of storing a cursor. — Prompt 08 forbids a new collection for it, and the derived form is also self-correcting: deactivate a user and the rotation simply skips them on the next submission.
- 2026-09-15 — D89/P08 — `POST /admin/leads/:id/claim` resolves to `leads.edit` in the route permission map (so any authenticated role reaches it) and the route itself applies `PERMISSIONS.leads.claim`, which is sales-only: an admin or a manager gets 403 and assigns instead, and a lead that somebody already holds answers 409. — D89 says the route stays open and "the API applies the matrix"; this is what applying it means in practice, and it keeps the 403 message identical to every other role failure.
- 2026-09-15 — P08 — A sales user's `PATCH /admin/leads/:id` may carry only `status`, `priority`, `followUpAt` and `lostReason`; anything else — `assignedTo` above all — is a 403. — D15 gives sales `leads.view`/`leads.edit` scoped to their own pipeline and withholds `leads.assign`; an allow-list is the only form of that rule which cannot be widened by accident when the lead schema grows.
- 2026-09-15 — P08 — `POST /leads` answers with the stored lead minus `ipAddress` and `userAgent`, and a row of `GET /admin/leads` omits `activities`. — `docs/API_CONTRACT.md` types both shapes that way (`Lead`, `LeadList`): the visitor who filled the form in has no business reading the CRM's columns, and a list of 20 leads should not carry 20 timelines the table does not render.
- 2026-09-15 — P08 — `DELETE /admin/leads/:id/notes/:noteId` answers with the lead rather than the `{ data: null, message }` of §5.8, and a sales user may only withdraw their own note. — `src/services/endpoints.js` types the endpoint's response as `Lead` (it is a write against the parent record, not a delete of a resource of its own), and the lead detail re-renders the timeline from the answer without a second request.
- 2026-09-15 — P08 — Phone numbers submitted to `POST /leads` are normalised to `+919876543210` (`+91 98765 43210`, `09876543210` and `9876543210` all land there) before validation. — §7 of prompt 08 requires it; a CRM that stores three spellings of one number cannot tell that they are one person, and normalising **before** validating is what makes the `0`-prefixed form pass the §6.7 rule instead of failing it.
- 2026-09-15 — P08 — `mock-server/routes/properties.js` and `leads.js` own their prefixes: an unknown `/properties/…`, `/admin/properties/…`, `/leads/…` or `/admin/leads/…` path answers 404 from the router instead of falling through to JSON Server. The `PUBLIC_WRITES` exception prompt 07 added to `publicScope.js` is gone with it. — §6 of prompt 08. `POST /leads` now has a real router in front of it, so the generic fallback no longer needs a hole for the lead form, and `PRIVATE_COLLECTIONS` is the flat rule prompt 07 described.
- 2026-09-15 — P08 — The test harness moved to `mock-server/__tests__/helpers.js`, which every suite shares, and it decodes responses with `TextDecoder('utf-8', { ignoreBOM: true })`. — Three suites were about to repeat eighty lines of setup. The decoder matters for one assertion the contract depends on: `Response.text()` strips a leading BOM, so a CSV export tested through `fetch` would have looked BOM-less however correct it was (§5.14 requires the BOM for Excel).
- 2026-09-16 — P09 — Twenty resources share one `makeCrudRouter({ db, model, basePath, schema, … })` factory in `mock-server/lib/crud.js`; each resource declares only what is its own — filters, sorts, `afterRead` embeds and counters, `beforeValidate`, `beforeSave`, `afterSave`, `beforeDelete`, `deleteGuard`, `listShape`, `publicTransform`, `bulkActions`, `routes`. — Writing the §5.8 write semantics, the §5.9 slug rules and the §5.2 envelope twenty times would have meant twenty chances to spell `meta.totalPages` differently. The escape hatch is that a resource with real behaviour (articles, pages, jobs, redirects, media) writes its own router and mounts the factory for the rest, so the factory never has to grow a flag for a one-off.
- 2026-09-16 — P09 — The factory takes a `routes` allow-list (`adminList`, `create`, `get`, `update`, `patch`, `remove`, `bulk`, `checkSlug`, `list`, `bySlug`) and resources whose contract stops short name what they have: newsletter subscribers get `adminList` and `remove`, job applications get `adminList`, `patch` and `remove`. — §5.14 defines those resources without a `POST` or a `PUT`, and inventing the missing endpoints would have put routes on the API that no schema backs (`newsletterSubscriber.create` does not exist) and that the guidelines generator would then have documented as real.
- 2026-09-16 — P09 — A bulk `action` is validated against the **resource's own** action map rather than the shared `BULK_ACTIONS` enum, so `archive` works on articles and `verify` answers 422 there. — §5.8 requires "unsupported action for the resource → 422", which a single site-wide enum cannot express; `ids` and `payload` still come from the shared `bulk` schema, so only the action list is per-resource. A bulk `delete` applies the same usage guard as a single delete and is refused **whole** when any of the selection is in use, so the editor sees one list of what is in the way instead of a partial delete.
- 2026-09-16 — D88/P09 — The delete guard counts only a **named** reference: a `team` block with an empty `memberIds` renders everybody and a `partners` block with no `category` renders every partner, and neither names a record, so neither blocks a delete. A `partners` block that does name a category blocks the partners in it. — A catch-all selection cannot be left dangling by a delete, and treating it as a usage would have made every newly created team member undeletable the moment an About page existed. Banks have no dependants at all and always delete.
- 2026-09-16 — D88/P09 — `ApiError` gained an optional `data` payload and the error handler renders it, so the 409 of a delete-in-use answers `{ message, errors: { id: ['Used by 3 properties'] }, data: { usedBy: [...] } }`. — `docs/API_CONTRACT.md` types that shape and the admin panel lists the usages; without it the 409 could only say that something is in the way, not what.
- 2026-09-16 — P09 — An article is public when `status` is `published` **or** `scheduled` **and** `publishedAt` has arrived; every read settles a due `scheduled` article to `published` and, for a record that has never been through a save, derives `contentText`, `wordCount` and `readingTimeMinutes` from its HTML. — §6.8 makes publication a moment rather than a flag, and the mock has no cron to flip it. Settling on read is what keeps the public list, the admin desk, the sitemap, the RSS feed and the dashboard from disagreeing; deriving the text fields on read is what gives a seeded article a reading time without asking the seed to carry a number it cannot compute.
- 2026-09-16 — P09 — A public article list row is the `ArticleSummary` of §4.4 (id, slug, title, excerpt, featuredImage, category, tags, author, publishedAt, readingTimeMinutes, isFeatured, viewCount); an admin list row is the whole record minus `content` and `contentText`. — The public cards render exactly those twelve fields, while the admin table needs `status`, `updatedAt` and `seo` to sort and filter on them. Both obey the rule the prompt actually states — the body text never travels in a list.
- 2026-09-16 — D28/P09 — Preview tokens live in an in-memory map, 24 hours, one per record (asking twice replaces the previous link). — A preview link is a conversation, not a property of the data, and a restart of the mock ending one is correct. `<Seo>` renders every `?preview=` page `noindex,nofollow` (§9.3), so a leaked link cannot put a draft into an index. A preview read never counts towards `viewCount`: it is the editor checking their own work.
- 2026-09-16 — P09 — Public URLs are built in one place, `PUBLIC_PATHS` in `mock-server/lib/sitemapBuilder.js`: property `/properties/<slug>`, locality `/localities/<slug>`, developer `/builders/<slug>`, article `/insights/articles/<slug>`, category `/insights/articles/category/<slug>`, tag `/insights/articles/tag/<slug>`, author `/insights/authors/<slug>`, page `/<slug>` (`home` → `/`), property type `/buy/<slug>` or `/commercial/<slug>` by segment (D25). — The sitemaps and `GET /admin/seo/overview` both answer "where does this record live", and a second copy of the map is a second chance to disagree with the router.
- 2026-09-16 — D21/P09 — `mock-server/app.js` rewrites a root request for `/sitemap*.xml`, `/robots.txt`, `/rss.xml` or `/llms.txt` onto `/api/...`, so `routes/sitemap.js` is the single implementation behind both spellings. — Nginx proxies the root paths in production and Search Console looks for them there; one implementation cannot drift from itself. All of them answer `text/xml` or `text/plain` with `Cache-Control: public, max-age=3600` and no envelope.
- 2026-09-16 — P09 — `GET /seo/settings` returns the **whole** `seoSettings` singleton. — §5.14 leaves the question open. The public site renders the title templates, the knowledge graph, the verification tags and `customHeadHtml` itself; `robotsTxt` and `llmsTxt` are already public as files at `/robots.txt` and `/llms.txt`; and the `sitemap` branch only describes documents a crawler can fetch. There is nothing in the model to withhold, and a subset would be a rule nobody could justify later. `docs/API_CONTRACT.md` was corrected to match.
- 2026-09-16 — P09 — `PUT /admin/settings` and `PUT /admin/seo/settings` validate as partial, keep only the keys the model declares, descend into objects and **replace** arrays, then stamp `updatedAt`. — §5.14 requires the deep merge. The admin form edits one panel at a time, so a body carrying `{ general: { siteName } }` has to keep the other twenty `general.*` keys and all seven other panels. Arrays replace because `footer.columns` is a list the editor reordered — merging it entry by entry would resurrect a link they deleted. Unknown keys are dropped so a stale field from an old bundle cannot take up residence in the data.
- 2026-09-16 — P09 — The dashboard's `topProperties` ranks **every** listing by `viewCount + enquiryCount × 5`, and `enquiriesThisMonth` counts the leads of the month that name a property. — §6.16 gives neither definition. Ranking all listings means the tile does not change the moment somebody toggles a listing off; counting property-linked leads is what distinguishes an enquiry from an assistance request, which belongs to the funnel but not to a listing. `leadsByStatus` lists all seven statuses including the empty ones, because a funnel chart with a missing rung is a chart that lies.
- 2026-09-16 — P09 — `GET /api/redirects/resolve?path=` is the only place `hits` moves, and `POST /admin/redirects` refuses a duplicate `fromPath`, a rule pointing at itself and a rule pointing at another **active** rule's `fromPath` (422). — D30 has the SPA matching the cached list in the browser, so nothing else ever asks the server about one path; `resolve` exists for the smoke test and for QA. The chain rule is the one a crawler reports as a redirect loop, and refusing it at write time is cheaper than explaining it afterwards. `import` upserts by `fromPath` and counts a row it cannot use as `skipped` rather than failing the whole paste.
- 2026-09-16 — P09 — `POST /jobs/:id/apply` answers `404 { message: 'This opening is closed.' }` for a role that is inactive or past `closesAt`, while `GET /jobs/slug/:slug` still serves an active-but-closed role with `isOpen: false`. — §7 of prompt 09 requires the 404 on apply; keeping the page readable is what lets a link from a job board explain itself instead of looking broken. `coverLetter` is bounded at the 5000 characters `jobApplication.create` declares rather than the 3000 the prompt mentions in passing: the schema is the shared contract and `src/services/schemas` is not this prompt's to edit.
- 2026-09-16 — P09 — `mock-server/lib/articleFilters.js` was added alongside the files the prompt lists, next to `propertyFilters.js` and `leadFilters.js` from prompt 08. — Article visibility is read by five callers (the public list, the admin desk, the counters on categories/tags/authors, the sitemaps and the dashboard), and a route module is the wrong home for something libraries have to require. The naming follows the convention prompt 08 established.
- 2026-09-16 — P09 — `npm run smoke` is **not** part of `npm run check:all`. — It needs a server on the other end (`npm run mock`), and a check suite that fails because nobody started a second process is a check suite people learn to ignore. `check:all` stays the set that runs from a clean checkout; `npm run smoke` is what runs against a deployment, local or not.
- 2026-09-16 — P09 — `scripts/smoke-api.js` invents its fixtures from the schema descriptors, tags every generated name, slug, path and e-mail with a per-run id, creates a throwaway for each `DELETE` and deletes everything it made; a public write that answers 429 is waited out once. — Generating bodies from `src/services/schemas` means the smoke test exercises the real contract rather than a second copy of it that can drift. The run id is what makes two runs against the same database independent; the 429 wait is the API's own §5.11 rule working (one run spends six of the ten submissions a minute), so the script waits rather than weakening it. A base URL with a self-signed certificate is out of scope: `NODE_TLS_REJECT_UNAUTHORIZED` is never set.
- 2026-09-16 — P10 — `db.json` is **generated** by `npm run seed:build` (`scripts/seed/build-seed.js` over `scripts/seed/data/*.js`) rather than hand-written, with a fixed mulberry32 seed (`scripts/seed/lib/rng.js`) and a fixed generation instant (`GENERATED_AT` in `scripts/seed/lib/dates.js`) so two runs produce a byte-identical file. — Forty properties, twelve articles, fifteen pages and forty-five leads have to agree with each other; a cross-reference maintained by hand drifts the first time one side of it is edited. Generating it also makes the diff of a content change readable, which a 1.4 MB hand-edited file never is.
- 2026-09-16 — P10 — A data module holds only what a person has to decide (the project's name, what it is like, what it costs, two or three sentences about it); everything implied is derived in `scripts/seed/lib/property.js` — the title, the carpet area, the rate per square foot, the badges, the FAQs, the timeline, the `seo` object. — A price and a `pricePerSqft` that disagree, or a "Ready to Move" badge on an under-construction project, are bugs the public pages would render faithfully. Derived records cannot have them.
- 2026-09-16 — P10 — The seed holds **40 properties** (ids 1–40), 38 active and 2 drafts, rather than the 36 the acceptance list names or the 44 the §4.2 matrix adds up to. — The matrix and the "34 active" line of the same prompt cannot both hold. 40 satisfies the "36+" floor of §10, the "ids 1–40" range of §4.2 and the "2 drafts" rule together, covers all seventeen property types, all three listing types, all four construction statuses and all twenty localities, and stays inside the 1–60 range `docs/DATA_MODEL.md` reserves. The trim fell on the sale-apartment block (12 rather than 17) because its statuses were already covered several times over.
- 2026-09-16 — D26/P10 — The twelve articles are written for this site in HTML; none of the boilerplate's ten are migrated. — §13 D26. The topics that still apply (RERA, khata, stamp duty, home loans) are rewritten around Karnataka and Bengaluru; the off-scope piece is dropped.
- 2026-09-16 — D84/P10 — Every legal, tax and regulatory figure in the seeded articles and on the awareness page is written as "check the current position with the authority" instead of as a number — the escrow share, the stamp-duty rates, the 80C limit, the title-search period. — A stale percentage about somebody's largest purchase is worse than no percentage. The `facts` block on the awareness page carries the same treatment, and prompt 48 lists each of them for verification.
- 2026-09-16 — D76/P10 — Four article categories (Buying Guides, Market Trends, Legal & RERA, Investment & Finance) and fifteen tags, with every article in exactly one category and two to four tags. — §13 D76; the tags are the cross-cuts a category cannot express (a stamp-duty piece is `legal-rera` by category and `stamp-duty`, `registration` by tag).
- 2026-09-16 — D27/P10 — The five extra block types (`jobs`, `facts`, `expandableCards`, `packages`, `gallery`) are all used by the seed: careers lists roles through `jobs`, the awareness page uses `facts`, `expandableCards`, `quiz` and `checklist`, legal assistance uses `expandableCards`, and interior designing uses `gallery` and `packages`. — D27 added them so the boilerplate's pages survive as data; a block type nothing seeds is a block type nobody will implement correctly.
- 2026-09-16 — P10 — §6.10 leaves the `leadForm` block's `fields[]` shape open, so the seed fixes one: `{ name, label, type (text|email|tel|textarea|select), required, placeholder, options[] {value,label}, fullWidth }`. — Eleven seeded forms need a shape, and the admin form builder of prompt 31 needs to round-trip whatever the seed writes. Documented in `docs/SEED_GUIDE.md`.
- 2026-09-16 — P10 — The four nested CMS pages keep slugs that are URL **paths** (`buyer-assistance/home-loan`, `insights/real-estate-awareness`), and `mock-server/routes/pages.js` matches them with `slug/:slug(*)`. `scripts/validate-seed.js` validates `pages.slug` and `pages.seo.slug` against a path pattern rather than the single-segment one. — §6.10 seeds exactly those slugs and the page has to be served at exactly that URL. Every other collection keeps the single-segment rule.
- 2026-09-16 — D79/P10 — `footer.showGallery` stays `false` with `galleryImageUrls: []`. — D79 keeps the collage as an optional setting; seeding images for a feature that is off by default would only cost media records.
- 2026-09-16 — D80/P10 — The three mock accounts keep their §6.14 credentials and are documented in `docs/SEED_GUIDE.md`. — They are the only credentials in the repository, plaintext because the mock compares them directly, and rotated before go-live.
- 2026-09-16 — P10 — `property.enquiryCount` is **at least** the number of leads naming the property, not equal to it; `scripts/validate-seed.js` enforces `>=`. — Prompt 10 §4.8 leaves the choice open and asks for it to be documented. The counter is the listing's lifetime total while `leads` holds the last ninety days, so a counter that shrank when old leads were archived would be the wrong number.
- 2026-09-16 — P10 — The media library holds one record per distinct image, document **and** video URL the seed mentions anywhere, including inside an article's HTML; `scripts/validate-seed.js` finds them with the same JSON scan `mock-server/lib/usage.js` uses, so "is this media record used?" gets the same answer in the validator and in the admin panel. `virtualTourUrl` is excluded: it is a link to an external experience, not an asset, and it has no `MEDIA_TYPES` value. — §6.12 and prompt 10 §4.7. The build fails if the collection passes the 400 ids `docs/DATA_MODEL.md` reserves.
- 2026-09-16 — P10 — Floor plans are generated for sale listings only, never for rent or lease. — Nobody publishes a floor plan for a flat they are letting; it also keeps the media library inside its reserved range.
- 2026-09-16 — P10 — `nearbyPlaces` name **real** Bengaluru landmarks (Hope Farm Metro Station, Vydehi Institute of Medical Sciences, Manyata Tech Park…) with approximate distances, while every project, developer, bank, partner and person in the seed is fictional. — §10 asks for real landmarks, and it is the right call: a "nearby" section that says "International school, 1.8 km" tells a buyer nothing, and the point of the section is that somebody recognises where the listing sits.
- 2026-09-16 — P10 — §14's display placeholder `+91 98XXX XXXXX` is stored as the structurally valid, obviously synthetic `+919800000001` (WhatsApp `+919800000000`), and optional strings that are unset are `null` rather than `''`. — The model validates `phone` against the Indian mobile pattern and types `social.*` as `url`, so the masked form and the empty string are both rejected. The masked form is what the client's own value replaces.
- 2026-09-16 — P10 — `seoSettings.llmsTxt` is seeded **empty**. — `mock-server/lib/sitemapBuilder.js` generates `llms.txt` from the live data whenever the stored document is blank, so an empty field keeps the file current instead of shipping a stale copy that has to be regenerated by hand.
- 2026-09-16 — P10 — `mock-server/__tests__/` runs against `__tests__/fixtures/starter-db.json`, a frozen copy of the prompt-06 starter seed, instead of the committed `db.json`; one new suite (`seed.test.js`) covers the shipped seed through `LIVE_SEED`. — The suites pin exact ids, totals and facet counts ("`?bedrooms=3` returns listings 1 and 2"), which describe the behaviour of the filters rather than the contents of the dataset; reading the live seed made 31 of them fail the moment the dataset grew, which is the wrong failure. The shipped data is exercised by `seed.test.js` and by `npm run smoke` (269 checks against a running server).
- 2026-09-16 — P10 — `subscriber.one@example.com` is a **reserved** address in `newsletterSubscribers`. — `scripts/smoke-api.js` posts it to `POST /newsletter/subscribe` to exercise the duplicate answer (200 with a `null` payload); without it in the seed the first run creates the record and answers 201.
- 2026-09-16 — P10 — `scripts/validate-seed.js` gained the §10 quality rules (active-listing minimums, rent vs sale pricing, land and commercial configuration, possession and age by construction status, badge consistency, the `enquiryCount` floor, published-article length, unique block ids, filled `seo`, collection counts) and a non-blocking **warning** channel, used today for the scheduled article drifting into the past. — A validator that only checks types passes a dataset nobody can demo. The warning is not an error because the file is still valid; it is the signal to bump `GENERATED_AT` and rebuild.
- 2026-09-16 — D48/P11 — `src/services/http.js` replaces `api.js` as the module that reads `REACT_APP_API_URL` and throws at load when it is missing. `src/setupTests.js` sets a localhost default for Jest, because CRA loads `.env.development` for `start`/`build` but never for `test`, and the suites run against stubs rather than a server. — Keeps §3.5's env-file table unchanged (no new committed `.env.test`) while letting every test import the client.
- 2026-09-16 — D10/P11 — The session lives under exactly three keys — `sna_auth_token`, `sna_auth_user`, `sna_auth_expires_at` — all written through `utils/storage.js`. `setAuthToken()` owns the token in memory _and_ in storage so the interceptor and the context can never disagree, and `clearSession()` removes all three. The caches are `sna_site_settings_cache` and `sna_master_data_cache`, both in `sessionStorage`. — The boilerplate wrote `authToken`/`adminUser`/`tokenExpiry` into both storages and cleared them in three different places (BUG-14).
- 2026-09-16 — P11 — A 401 signs the session out only when the request path starts with the admin or auth prefix, and never for the login call. The two prefixes are derived from the registry (`endpoints.dashboard.get.path`, `endpoints.auth.profile.path`) rather than written as literals, and the login path is compared against `endpoints.auth.login.path`. — `scripts/check-endpoints.js` treats an API path literal in a service file as a finding, and deriving them is also the honest way to say "the admin prefix": if the contract moves, this moves with it.
- 2026-09-16 — P11 — The default 401 handler is latched: several admin calls are normally in flight at once, so one expired token produces several 401s, and without the latch each would call `window.location.assign`. Being already on the login route is also a no-op. — The acceptance criterion is "redirect once, no loop on the login page itself"; measured in a browser, an expired token now produces exactly one full page load.
- 2026-09-16 — P11 — `useApi` keeps the fetcher, the options and the callbacks in a ref and keys its effect on a string built from the caller's dependency list, instead of spreading `deps` into the dependency array. — A spread dependency array cannot satisfy `react-hooks/exhaustive-deps`, which is an **error** in this project (D36); the string key gives the same semantics without an `eslint-disable`, and an inline arrow fetcher no longer restarts the request on every render.
- 2026-09-16 — P11 — With `syncToUrl`, `useApiList` treats the query string as the only source of truth: `params` is derived from `useSearchParams` and `setParams` writes back. Any patch that touches a key other than `page` resets `page` to 1, and a value equal to its default never appears in the URL. — One source of truth is what makes the back button, a reload and a shared link agree; the alternative (local state mirrored into the URL) is the drift the boilerplate's filter panel already had.
- 2026-09-16 — P11 — `MasterDataContext` loads its seven lists with `Promise.allSettled` and `perPage=100`, and a list that fails becomes `[]` rather than taking the others down. `perPage=all` is admin-only (§5.6) and the largest collection is 46 rows. — One unreachable amenity list must not empty the filter panel; the cap is comfortable and honest about the contract's limit.
- 2026-09-16 — P11 — `useSiteSettings()` and `useMasterData()` return a documented fallback outside their provider instead of throwing. — Both are read by UI-kit-level components (`Footer`, `PropertyCard`'s neighbours) that tests and future Storybook stories render on their own; a throw would make every such render need a provider, and the fallback is the brand constants, which is what the screen should show when settings are unreachable anyway.
- 2026-09-16 — P11 — **The adapter decision.** `PropertyDetails`, `PropertyListing`, `PropertyForm` and the admin tables are ~10 000 lines that prompts 18–29 rewrite. Rather than half-rewriting them here, `src/utils/adapters/legacyProperty.js` and `legacyArticle.js` present the contract record under the old field names at the single point where each screen fetches. Each adapter spreads the real record first, so a component that has already been rewritten reads the real fields while its neighbour still reads the old ones. Both files are listed in "Pending rewrites" with their owner prompts. — The alternative was leaving those pages showing empty states until prompt 29, which would have made the intervening prompts impossible to QA.
- 2026-09-16 — P11 — `PropertyListing` keeps both views of each row (`{ record, legacy }`): `PropertyCard` gets the contract record it was rewritten for, and the page's own filter and sort code reads the adapter's shape. — `toLegacyProperty` necessarily overwrites `configuration`, `propertyType` and `location.city` with their old string forms, so a single object cannot serve both; carrying the pair is explicit and deletes cleanly in prompt 26.
- 2026-09-16 — P11 — Four admin saves are disabled behind an info `Alert` naming the prompt that restores them: `PropertyForm` (18–21), `ArticleForm` (33), `AdminSettings` (40) and `AdminSeo` (36–37). Their dead payload builders and validators were deleted rather than left unreachable. — None of the four could have succeeded against this contract (snake_case columns, hardcoded category strings, a flattened settings object, a separate SEO endpoint that no longer exists), so this removes a broken path rather than a working feature; the guardrail's "never a broken button" is met by the disabled state plus the message.
- 2026-09-16 — P11 — `ArticleForm`'s disabled save is the one item beyond the list the prompt enumerated. — Its pickers offer five hardcoded category strings (`buying-guide`, `interior`, …) that do not exist in the seed's four categories, and an author name string where the API wants an `authorId`; saving would have silently dropped the category, the author and every tag.
- 2026-09-16 — P11 — `seoService.llmsPreview()` reads the public `GET /llms.txt` with `responseType: 'text'`, and `redirectService` ships no `import`/`exportUrl`. — The mock also serves `GET /admin/seo/llms-preview`, `POST /admin/redirects/import` and `GET /admin/redirects/export`, but none of the three is in `src/services/endpoints.js`, and adding mock routes was outside this prompt's scope. Prompt 37, which owns the redirects screen, adds them to the registry or builds the CSV in the browser the way the property export does (D44).
- 2026-09-16 — P11 — The `ids` filter the article detail page needs for `relatedArticleIds` already existed: `articles.list` declares `ids: 'csv:int'` and `mock-server/lib/articleFilters.js` returns the ids in the order given. No mock change was needed. — Prompt 09 had already built it; the prompt's instruction to add it was written before that landed.
- 2026-09-16 — P11 — `LegacyHtml` is one shared component under `src/components/common/` rather than a copy inside `ArticleDetail`. — Article bodies, FAQ answers and (later) locality and property descriptions are all CMS HTML; one choke point is what makes prompt 32's swap to `SafeHtml` a single edit instead of a search.
- 2026-09-16 — P11 — `NewsletterSection` moved from `MainLayout` into `Footer`, which renders it only when `settings.newsletter.enabled` and `settings.footer.showNewsletter` allow it. — §6.13 gives the footer the newsletter switch, and the band renders immediately above the footer either way, so the page is unchanged when the setting is on.
- 2026-09-16 — P11 — `package.json` changed in two places the prompt did not anticipate: the ESLint override that exempted `src/services/api.js` from the axios ban lost that (deleted) file, and `transformIgnorePatterns` gained `@iconify/react`. — The first is required by the deletion; the second is the same ESM-in-node_modules problem as D7, and without it the `PropertyCard` test the prompt requires cannot run under CRA's Jest 27.
- 2026-09-16 — D32/P12 — Every role lands on `/admin/dashboard` after signing in, unless `location.state.from` names a route that role may open, in which case the login page sends them back there. — §7 and D32. `canAccessAdminRoute(role, pathname)` in `src/routes/adminRouteConfig.js` answers that question from the same table `RoleRoute` reads, so the redirect can never land a role on a 403.
- 2026-09-16 — P12 — There is no "Remember me" on the login screen, and the checkbox is not replaced by anything. — §5.4 persists every session under `sna_auth_*` for the token's TTL and enforces the expiry client-side, so the control had nothing left to control; the boilerplate's version was a no-op (ADD-19). Recorded because the master context does not mention the control at all.
- 2026-09-16 — P12 — `src/routes/adminRouteConfig.js` declares **every** final admin route from prompt 12 onward; the 26 screens later prompts write render `AdminPlaceholderPage` ("Coming in prompt NN") until their owner prompt lands. — The shell, the RBAC guards, the sidebar and the page titles can then be finished and tested as a whole rather than growing route by route. The component is listed under "Pending rewrites" in `docs/PROJECT_STATE.md` and must not exist after prompt 43.
- 2026-09-16 — P12 — A route entry carries its §7 permission as a `[area, action]` tuple rather than a role list, and `RoleRoute` asks `useAdminAuth().can()`. `allowedRoles` stays supported for the rare screen that really is about the role itself. — §7 forbids hardcoding `role === 'admin'`; a tuple keeps the matrix in `config/rbac.js` as the only place roles are named.
- 2026-09-16 — P12 — Route entries hold no `exact` flag. — React Router 6 matches a route path exactly unless it ends in `*`, so the flag would describe nothing. Prompt 12 §4 asks for it; it is omitted rather than stored and ignored.
- 2026-09-16 — P12 — An expiry, a 401 or a cross-tab sign-out clears the session everywhere, but only toasts and redirects when the current location is an admin screen. — A visitor reading a public page with a stale admin token in storage has no interest in "Your session has expired", and moving them to the login page would be worse; §5.4's toast and redirect belong to the panel.
- 2026-09-16 — P12 — `AdminAuthContext` keeps a teardown latch: from the moment a session ends until the next sign-in, further 401s are ignored. — A single dead token produces a burst of 401s from the calls already in flight, and the expiry path's own `POST /auth/logout` answers 401 too. §7 of the prompt asks for "a single redirect + toast, no double toast"; the latch is what guarantees it.
- 2026-09-16 — P12 — A `GET /auth/profile` that fails with anything other than 401 leaves the restored session in place. — §7: "Mock down while on an admin page → error toast, stays logged in (no logout on network errors)."
- 2026-09-16 — D45/D55/P12 — `LeadNotificationsContext` is mounted inside `AdminLayout`, not in `App.js`. — The poller then exists only while an admin screen is open, so the public site never issues an admin call; `App.js` needed no change.
- 2026-09-16 — P12 — A lead poll that fails is silent: no toast, no error state, the next tick simply tries again. — The screens that need the data (`AdminLeads`, `Dashboard`) surface their own error state; a background poll that toasts every 30 s while the API is down is worse than the outage it reports.
- 2026-09-16 — P12 — The new-lead toast passes a React node as the message — the sentence plus a `Button variant="link" to={PATHS.adminLead(id)}`—instead of adding an `action` prop to `ToastProvider`. — `ToastProvider` already renders `{toast.message}` as a node, so D54's single toast system needed no change to carry the "View" action §4 asks for.
- 2026-09-16 — P12 — `LeadNotificationsContext` also exposes `hasUnseen`, derived from `recentLeads[0].createdAt` and `sna_leads_seen_at`. — §4 defines the rule for the bell's dot but lists only the six other members; the bell needs the answer, and deriving it in the context keeps the comparison in one place.
- 2026-09-16 — P12 — When the sidebar is collapsed to the 72 px rail, clicking a group header expands the rail and opens that group rather than toggling a submenu nobody can read. — A rail that wide cannot show child labels, so the alternative was a control that does nothing.
- 2026-09-16 — P12 — `src/routes/publicRoutes.js` declares the public URLs as a `[path, page]` table mapped into `<Route>`s instead of 22 repeated JSX blocks. — The set of URLs is unchanged, which is what prompt 12 §3 asks for ("unchanged content"); the shape is what makes the file readable next to the admin table.
- 2026-09-16 — P12 — `/admin/settings/users` renders the placeholder owned by prompt 13, not a page wrapping the existing `UserManagement` component. — Prompt 13 §3 lists `src/pages/admin/settings/UsersPage.jsx` as the replacement and `adminRouteConfig.js` as the file it edits to point at it. `UserManagement` stays reachable as a tab of `AdminSettings` in the meantime, so no function is lost.
- 2026-09-16 — P12 — `PATHS.adminLocalities` moves from `/admin/neighborhoods` to `/admin/master-data/localities`, and the existing `AdminNeighborhoods` screen is mounted there. — §7's route map and D13 name the localities screen; the old path had no callers. Prompt 14 replaces the screen behind the same URL.
- 2026-09-16 — P12 — The avatar preview on `/admin/profile` renders only once the URL field holds a complete `http(s)://host.tld/...`. — Previewing on every keystroke fires an image request for every prefix of the URL, and each half-written host fails loudly in the console; the acceptance criteria require admin screens to stay console-clean.
- 2026-09-16 — D97/P13 — `App.js` mounts `createBrowserRouter` + `RouterProvider` instead of `BrowserRouter`, with the route table unchanged (`createRoutesFromElements` reads the same `<Route>` elements `publicRoutes` and `adminRoutes` already declared). — `useBlocker` exists only inside a data router, and it is the only way to stop an in-app navigation away from a dirty form without re-implementing the history stack. Verified on react-router-dom 6.22.3, which emits no future-flag warnings for a data router.
- 2026-09-16 — D97/P13 — Because a data router replaces `BrowserRouter`, the providers that use router hooks moved from `App`'s children into the root route's element (`AppShell` in `src/routes/index.js`): `ToastProvider`, `SiteSettingsProvider`, `MasterDataProvider`, `AdminAuthProvider`, `NavigationGuardProvider` and the `Suspense` fallback. `ErrorBoundary`, `HelmetProvider` and `ThemeProvider` stay in `App`. — `AdminAuthProvider` navigates on a 401; outside the router that throws.
- 2026-09-16 — P13 — The unsaved-changes guard is one `useBlocker` in `NavigationGuardContext`, and `useUnsavedChanges(dirty)` registers a form with it rather than calling `useBlocker` itself. Outside the provider the context is an inert fallback. — One question, asked once, in the design system's own `ConfirmDialog`; and a form rendered by a test or a preview does not need a data router to render. Blocking a **back-button** navigation additionally needs the history entry to belong to the router (`@remix-run/router` needs a `delta`), so a form reached by a full page load blocks link clicks but not the back button; `beforeunload` covers that case.
- 2026-09-16 — D47/D23/P13 — `DataTable` defaults to 20 rows and offers 10/20/50/100; the page size lives in the URL with the rest of the parameters. — §5.6 and D23/D47.
- 2026-09-16 — P13 — `DataTable`'s select-all covers the rows on screen and there is no "select all N results" link. — A bulk action must never reach rows nobody has seen; the prompt's §4.1 says the same.
- 2026-09-16 — P13 — The 400 ms search debounce lives in `FilterBar`, not in `useApiList`'s `debounceMs`. — With `syncToUrl`, `useApiList` writes a history entry for every parameter change, so debouncing only the _request_ would still leave one history entry per keystroke — seven presses of Back to undo typing "manager". `FilterBar` holds the text locally and reports once the typing stops, which gives one entry and one request per pause.
- 2026-09-16 — P13 — `useForm`'s `errors` is a **flat, dotted** map (`errors['location.localityId']`, `errors['images.0.alt']`), and `setServerErrors` normalises a 422 into that one keyspace. — It is the keyspace the API already uses (§5.3), so a server message and a client message land on the same field and `getError(path)` is one lookup at any depth. The prompt's "nested and flat" is satisfied by accepting either shape from the server rather than by keeping two copies of every message.
- 2026-09-16 — P13 — `useForm` gained `normalize`: the values are run through it before validation **and** before `onSubmit`, so the form validates the exact body it will send. `MasterDataPage` passes `config.toPayload`. — Without it a form validates something the API never sees; the concrete failure was an untouched optional `avatarUrl` holding `''`, which is not a valid URL but is also not what gets sent (`null` is).
- 2026-09-16 — P13 — `utils/validation.js` treats `''` as absent on a field that is **not required and is `nullable`**. A plain `string` field keeps `''` as a real value. — A form input has no `null`: an untouched optional URL box holds `''`, and the descriptor is what says whether that means "nothing" or "an empty string". This is the one place the client validator deliberately differs from `mock-server/middleware/validate.js`, which only ever sees the normalised body.
- 2026-09-16 — P13 — `SlugField` sanitises with `toSlugInput` while typing (a single trailing hyphen survives) and tidies with `slugify` on blur; the availability check waits until the value is a slug the API can answer about. — `slugify` on every keystroke eats the separator before the next letter arrives, which makes a multi-word slug impossible to type.
- 2026-09-16 — D88/P13 — `ApiError` now carries `data`, and `http.js` copies `payload.data` onto it. — The 409 of a master-data delete answers `{ message, errors, data: { usedBy } }`; without `data` the panel could say that something is in use but not what, which is the whole point of D88. `firstFieldMessage(error)` was added beside it, because a 422's `message` is Laravel's generic sentence while the useful one ("You cannot deactivate your own account.") is in `errors`.
- 2026-09-16 — P13 — `MasterDataPage`'s delete confirm closes itself on a 4xx refusal and stays open on a network error or a 5xx. — A refusal will not become an acceptance on a second press; a dropped connection might.
- 2026-09-16 — D88/P13 — `config.formFields` may be a function of the record being edited. — The users form needs a required password on create and an optional one on edit, and a disabled role select when you are editing yourself; a static list cannot express that, and per-screen forks of `MasterDataPage` would defeat the point of it.
- 2026-09-16 — P13 — `formMode: 'page'` renders the form in place of the list on the same route rather than navigating to an `add`/`edit` route. — The kit would otherwise need a route table of its own; the screens that want real URLs (properties, articles, developers) have their own routes already.
- 2026-09-16 — P13 — A `DataTable` column may set `mobile: true` / `mobile: false` to force itself into or out of the phone card; `MasterDataPage`'s injected `isActive`/`isFeatured` columns set `mobile: true`. — §6 of the prompt asks for "title/meta/status" on the card, and status is the last column of the table, outside the first three the card shows by default.
- 2026-09-16 — P13 — `ToneSelect` is built on `BADGE_TONES`, whose six values are the tone vocabulary of `ui/tones.js`. The master context's `charcoal` and `muted` are the already-recorded `neutral`. — §6.4 stores tones, not colours, and `enums.js` has spelled it this way since prompt 05.
- 2026-09-16 — P13 — `AMENITY_CATEGORIES` gained an `icon` per entry, so the `IconPicker`'s two enum-driven quick categories ("Amenity categories", "Nearby places") both come from `config/enums.js`. — The prompt asks for them "using the enum icons"; only `NEARBY_CATEGORIES` carried icons. The existing `enums.test.js` already asserts every enum icon matches `^mdi:[a-z0-9-]+$`, so the addition is covered.
- 2026-09-16 — ADD-23/P13 — Of the 17 ids §11 item 23 calls invalid, only **three** are actually missing from the Iconify MDI set (`mdi:apartment`, `mdi:intercom`, `mdi:car-parking` — the last one is a new find) and three more are aliases (`mdi:mountain`, `mdi:restaurant`, `mdi:bricks`). The other eleven render, so they stay. The six are replaced with canonical ids, and all 206 ids in the library were checked against the live collection listing and then in the browser (431 tiles, none blank). — Replacing icons that work would remove working icons; the defect is blank tiles, and there were exactly six ways to get one.
- 2026-09-16 — P13 — `mdi:intercom` and `mdi:car-parking` were also fixed in `src/pages/admin/property-tabs/constants.js`. — Same defect, same two ids, and the prompt's own QA step opens that tab's `IconPicker`. `mdi:stamp` (a public page, owner 31) and `mdi:home-check-outline` (`db.json`, which §12 puts out of reach) are recorded as NEW-28 and NEW-29 instead.
- 2026-09-16 — P13 — MUI controls in the kit pass `slotProps={{ input: … }}` rather than the deprecated `inputProps`, and a `Switch` must re-state `role: 'switch'` when it does. — `Switch` reads only `slotProps.input`, and providing it **replaces** MUI's defaults for that slot, `role` included; a toggle that reports itself as a checkbox is a worse answer than the one MUI ships. They also pass `disableRipple`: the design system's own controls are flat, and the ripple's timer is what made every table test warn about `act()`.
- 2026-09-16 — P13 — `TableSkeleton` was added to `common/SkeletonLoaders.jsx` with `role="status"`. — §8.4 lists it there; it did not exist yet.
- 2026-09-16 — P13 — `AdminSettings` keeps an admin-only "Users" button next to "Save settings", pointing at the new page. — The tab it replaces was where people knew to look; the sidebar entry alone is a quieter answer than the one they had.
- 2026-09-16 — D13/P14 — The boilerplate's `neighborhoods` screen is replaced rather than renamed: `src/pages/admin/AdminNeighborhoods.js` is deleted and `/admin/master-data/localities` renders the new list. Every capability it had survives — the image is an `ImageField` with a preview instead of a bare URL box, the city is a select over `MasterDataContext.cities` instead of a free-text name matched case-insensitively against the city list (a typo silently filed the locality under the first city), the active switch is the kit's optimistic toggle, and a delete refused with 409 opens the guard dialog instead of a toast. — The old screen edited four of a locality's twenty fields; keeping it beside the new one would have meant two ways to write the same record.
- 2026-09-16 — D42/P14 — `src/components/common/MapEmbed.jsx` renders `https://www.google.com/maps?q=<lat>,<lng>&z=<zoom>&output=embed` in a lazy iframe with an accessible `title`, and draws a dashed placeholder when either coordinate is missing, unparseable or out of range. `hasCoordinates(lat, lng)` is exported so a page can decide whether to render the heading above the map at all. — D42: that URL needs no API key, and the keyed Maps JS API (with the draggable pin) belongs to the property form, where a key may exist. `0,0` is what a broken record looks like, not a place we have listings in, so it renders the placeholder rather than a map of the Gulf of Guinea.
- 2026-09-16 — P14 — `MasterDataPage` gained `onCreate` / `onEdit`: when a config supplies them, "Add" and the row's "Edit" navigate instead of opening the form. — Prompt 13 recorded that `formMode: 'page'` renders the form in place "rather than navigating to an add/edit route", because the kit would otherwise need a route table of its own. Prompt 14 needs real URLs (`…/localities/add`, `…/localities/edit/:id`, already in `routes/paths.js` and `adminRouteConfig.js`), and two callbacks give the screen the choice without the kit owning any routes.
- 2026-09-16 — P14 — `orderable` now replaces the table with the reorder list **only while the list is sorted by `order`**, renders each row through an optional `config.renderOrderItem`, and writes `order = (page - 1) * perPage + index`. — Dragging a row of a list sorted by name writes positions nobody can see, and the previous `order: index` renumbered page two from zero, which shuffled it to the top of the collection. The prompt asks for reordering "when sorted by `order`"; sorting by anything else brings the columns back.
- 2026-09-16 — P14 — `MasterDataPage`/`useMasterDataCrud` gained `onMutated`, called after every successful write (save, delete, bulk, reorder, optimistic toggle); the localities and cities screens pass `MasterDataContext.refresh`. — The seven public lists are fetched once per session and cached for ten minutes (D93), and the admin panel is the same SPA, so unfeaturing a locality otherwise left the home strip showing it until the cache expired. The kit hook means prompts 15–17 fix the same class of staleness with one line.
- 2026-09-16 — P14 — `RowActions` accepts `href` and renders it as a link with `target="_blank"` / `rel="noopener noreferrer"`. — "View on site" is a different context, not a navigation away from the record being edited, and a real link is middle-clickable in a way `window.open` is not.
- 2026-09-16 — P14 — `masterDataService` exports `adminCrud(resource)`, which maps a collection onto the `{ list, get, create, update, patch, remove, bulk, checkSlug }` shape the admin kit expects — `list` is the admin list, and `checkSlug` takes `(slug, { excludeId, signal })` rather than a params object. — `MasterDataPage` hands `service.checkSlug` straight to `SlugField`, whose contract is the second shape; without the adapter the availability check would have sent the slug as the whole query object. Prompts 15–17 reuse it.
- 2026-09-16 — P14 — The locality form carries the record's `seo` branch through a save untouched, in a ref, and sends it back with the `PUT`. — §5.8 makes `PUT` a replacement and the mock's own defaults fill anything absent: verified that `PUT /admin/localities/20` without `seo` resets the branch (and `highlights`, `pincodes`, `order`) to their defaults. The SEO panel arrives in prompt 36; until then the form must not be the thing that erases it.
- 2026-09-16 — P14 — The locality form validates against `schemas['locality.create'|'locality.update']` including that carried `seo` branch. — Checked that a seeded locality's own `seo` object passes `utils/validation.js` unchanged, so the form validates exactly the body it sends (the `normalize` rule of prompt 13) without inventing a second, narrower schema.
- 2026-09-16 — P14 — Leaving the form after a save waits for `NavigationGuardContext.isBlocking` to clear and then navigates on the next tick (`setTimeout(…, 0)`). — A saved form is clean, but the provider learns that one render later and `useBlocker` re-registers the question in an effect of its own, which runs after this component's (children before parents). Verified: navigating any sooner met a blocker still holding the previous, dirty answer and asked the editor whether to discard changes that had just been written.
- 2026-09-16 — P14 — `/localities` requests 24 records a page rather than the public default of 12 (D23). — §8.6 caps a page at 24 and the whole Bengaluru set is 20, so the index is one page and one request; `Pagination` still renders if the collection outgrows it.
- 2026-09-16 — P14 — The sort select offers "Recommended" (`order`) beside the prompt's `name` and `propertyCount`. — `order` is the API's own default and the order an editor curates in the admin; leaving it out of the select would have made the default view unreachable once the visitor sorted.
- 2026-09-16 — P14 — `?zone=` is filtered against `LOCALITY_ZONES.values` before it reaches the API, in the fetcher and in the chips' pressed state. — §7 of the prompt: an unknown zone is treated as no filter. `http.buildParams` drops unknown _keys_, not unknown enum values, so `?zone=unknown` would otherwise have reached the mock and matched nothing.
- 2026-09-16 — P14 — The "Updated" column of the localities table is not sortable. — The contract's locality sorts are `order|name|propertyCount` (§6.2 and `mock-server/schemas/models.js`); `sort=updatedAt` falls back to the default sort server-side, so a sortable header would have promised something the API does not do, and the mock is out of this prompt's reach (§12).
- 2026-09-16 — P14 — `LocalityFormPage` imports the admin kit file by file, in the order `MasterDataPage` reaches for the same components, instead of from `components/admin/index.js`. — The barrel is alphabetical, `MasterDataPage`'s imports are not, and two admin chunks holding the same extracted CSS in two orders is a `mini-css-extract-plugin` "Conflicting order" warning, which `build:ci` (CI=true) treats as an error. The barrel's own header now says so.
- 2026-09-16 — P14 — `LeadForm` gained `hiddenFields`, merged into the `POST /leads` body. — The locality CTA has to file `requirement.localityId` without asking for it; prompt 28 unifies every form on the site, and this is the part of that unification the locality page needs now.
- 2026-09-16 — P14 — The locality hero is `min-height: 320px` (220 px below 900 px) rather than a fixed height. — A locality carrying an indicative price, a listing count and two pincodes needs 285 px on a 390 px screen; clipping the figures to hit the number in the prompt would have hidden the data the hero exists to show.
- 2026-09-16 — P14 — `LocalityProperties` renders nothing when the locality has no active listings or when the request fails, rather than an empty row or an error box. — §7 of the prompt asks for the section to be hidden at zero, and the rest of a guide is worth reading when one of its six requests does not answer; the listing search is one click away in the header either way.
- 2026-09-16 — P14 — `src/test-utils.jsx` accepts `initialEntries`, handed to the `MemoryRouter`. — A screen that reads a route parameter (`/localities/:slug`) cannot otherwise be rendered at the URL it belongs to, and the alternative was mocking `useParams` in every such test.
- 2026-09-16 — D88/P15 — Changing a property type's `segment` while listings carry it is allowed by the API and confirmed by the UI: `MasterDataPage` gained `config.confirmSave`, the property-types config asks `GET /admin/property-types/:id?withUsage=true` on a segment change, and a non-empty answer opens the usage dialog with "Used by 12 properties" and the listings themselves, before the write. — §6.1 gives a property its own `segment`, so moving the type does not move the listings; the editor should learn that from the screen rather than from the search results afterwards.
- 2026-09-16 — P15 — `withUsage=true` is a query of the CRUD factory's `GET /admin/<resource>/:id`, not a route of its own: it adds `usedBy` — the same list a 409 carries — and is a no-op on a resource without a delete guard (banks). The registry advertises it on every `adminResource` group except `adminProperties` and `adminUsers`, whose routers are hand-written and have no guard. — One lookup, one shape, asked before a change instead of after it; a second endpoint would have been a second way to spell `usedBy`.
- 2026-09-16 — P15 — `propertyCount` (active listings carrying the record) is computed by `afterRead` for amenities and badges, exactly as it already was for localities, property types and developers, and both collections gained `sort=propertyCount`. It is returned on the public lists too rather than only on `/admin`. — The four master-data screens show it and the delete guard explains itself with it; making it admin-only would have meant two shapes for one field, and the count is not private.
- 2026-09-16 — P15 — `MasterDataContext.refresh(collection)` reloads one of the seven lists and rewrites the `sna_master_data_cache` entry; `refresh()` still reloads all seven, and a name the context does not hold (FAQs, users) is a no-op rather than a silent full reload. `MasterDataPage` passes `config.key` to `onMutated`, so every screen's `onMutated: refresh` became granular without a change at the call site. — Prompt 14 refreshed all seven collections after every toggle of one row; a badge created in the admin has to reach `/properties` in the same session (D93), and six extra requests are not how.
- 2026-09-16 — P15 — `DataTable` gained `groupBy(row) => {key, label}`, which draws a `<th scope="colgroup">` heading above the first row of each run (and an `<h3>` between the phone cards). `MasterDataPage` applies it only while `config.groupSort` is the current sort. — Rows arrive from the API already sorted, so "a new group" is "a different key from the row before"; grouping a list sorted by name would have invented groups that the next page contradicts.
- 2026-09-16 — P15 — Amenities default to `sort=category` (the grouped table) and are reordered by sorting on `Order`; reordering **within** a category is reordering the list filtered to that category, and the drag list says so. — An amenity's `order` is only ever compared inside its category (the public list is grouped and sorted within the group), so dense positions per category are exactly right, and one sortable list is a simpler answer than eight.
- 2026-09-16 — P15 — The legacy `PropertyFilters` property-type select now offers the seventeen types of `usePropertyTypes()` with **ids** as values, and `PropertyListing` resolves `?propertyType=` as an id **or** a slug. `toLegacyProperty` gained `propertyTypeId`. — `propertyTypeId` is the contract's own filter (§5.7) and what prompt 26 will send; accepting the slug as well keeps `/rent/apartments`, the home links and any shared URL working in the meantime.
- 2026-09-16 — P15 — `src/config/adminConstants.js` is deleted. `LEAD_STATUS` gained an `icon` per entry and `FAQ_CATEGORIES` a `tone`, `LEAD_SOURCES.labelOfAny(value)` resolves a legacy or unknown source to a readable label (D20), and the two legacy article screens read their categories from `articleService.categories()` instead of a five-entry constant. — The file was 38 hex literals' worth of duplicated enum; the screens that still read it (prompts 17, 29, 33) needed the same values, and the enum is where §6.17 says they live. `DEFAULT_BANKS`, which named six real banks with invented rates, is gone with it (§14).
- 2026-09-16 — P15 — `AdminArticles`' category filter compares `categorySlug`, not the category's display name. — Its options have always been slugs while the adapter exposes `category` as the name, so the filter matched nothing; changing the source of the options was the moment to make it work.
- 2026-09-16 — P15 — `FinanceGuide` reads `useBanks()` and drops its unused `bankData` prop; with no active bank the "Bank Loan Assistance" tab is not offered at all, and the banner's "rates from", the "Lowest Rate" tile and the "Financing" tile are computed from the lenders on screen. — §6.6 hides the section when no bank is active, and a banner promising 8.35 % over a table that says otherwise is worse than no banner (§14).
- 2026-09-16 — P15 — `useBadgeMap()` keys the map by `String(id)`. — The ids a component holds come from a record, a form value or a URL, and only one of those three is a number.
- 2026-09-16 — P15 — `ICON_ID_PATTERN` (`^mdi:[a-z0-9-]+$`) lives in `src/utils/validation.js` beside `URL_PATTERN`, the four configs validate `icon` against it, and the `icon` control shows `mdi:help-rhombus-outline` in muted grey when the value does not match. — Iconify renders nothing for an id it cannot resolve, which reads as "no icon chosen" rather than "that id is wrong".
- 2026-09-16 — P15 — The badges table carries a "Properties" column the prompt's column list does not name. — The mock computes `propertyCount` for badges from this prompt onwards; a field nothing shows is a field nobody can trust, and it is the number the delete guard will quote.
- 2026-09-16 — P15 — `docs/archive/` is added to `.prettierignore`. — `npm run format` covers `docs/**/*.md` and rewrote the frozen inventory's `snake_case` as `snake*case` (markdown reads the underscores as emphasis); D17 already excludes the archive from the trace scan for the same reason — it is a record of what was, not a document we maintain.
- 2026-09-16 — P15 — The admin amenities table groups its categories in the API's own `sort=category` order (a string sort on the field: Basic, Commercial, Convenience, Eco-friendly, Kids, Lifestyle, Safety, Sports) while `useAmenitiesGrouped()` groups them in `AMENITY_CATEGORIES` order for the public site. — §4.4 of the prompt asks for the enum order in the **hook**; the table is server-paginated, so its order has to be the API's or page two would contradict page one, and inventing a `categoryOrder` field to sort on would have put an artefact of the admin table into every public response.
- 2026-09-16 — P16 — No schema was relaxed for the property form's quick-create (prompt 20): `POST /admin/developers { "name": "Test Builders" }` already answers 201 with the slug generated and `description`, `shortDescription`, the counts, `reraIds` and `highlights` at their defaults. — §6.5 marks only `name` as required and `src/services/schemas/masterData.js` already gives every other field a default; task 5 asked for the relaxation only "if the schema requires more", and it does not. Verified against the running mock before writing the form, and the form's own "Basics" section says so in a sentence.
- 2026-09-16 — P16 — The developer CTA files `pageSlug` as the builder's own slug (`aurelia-estates`) and puts the address in `pageUrl`, rather than the `/builders/<slug>` path the prompt names. — §6.7 types `pageSlug` as a **slug** (`^[a-z0-9-]+$`, ≤ 75 chars), so a path is a 422: verified, `POST /leads` with `pageSlug: "/builders/aurelia-estates"` is refused. The seed's own `developer-page` lead carries the absolute URL in `pageUrl`, and its property-page leads carry the entity's slug in `pageSlug`; this sends both, in the shapes the contract and the seed already use, and `source: developer-page` says which kind of page the slug belongs to.
- 2026-09-16 — P16 — `LeadForm` field descriptors may carry a `defaultValue`, used as the box's starting value. — The builder CTA has to open its message on "Interested in projects by <name>" (prompt task 4) and still let the visitor edit or clear it; `hiddenFields` is merged **after** the typed values, so putting the message there would have overwritten whatever the visitor wrote. Prompt 28 unifies every form on the site; this is one line of that unification, additive for every existing caller.
- 2026-09-16 — NEW-31/P16 — `LeadForm` trims what was typed and leaves the empty boxes out of the `POST /leads` body. — An optional e-mail nobody filled in travelled as `""`, which §6.7 validates as an e-mail and refuses: the builder CTA answered 422 ("The email must be a valid email address") on every lead without one, and so did the locality CTA of prompt 14. An absent key takes the schema's own default (`null`); verified in the browser both ways.
- 2026-09-16 — P16 — A builder's logo box shows the builder's **own initials** ("AE", "TB") when there is no logo or the URL fails, not the Squares N Acres monogram `LazyImage` falls back to. — §7 asks for a monogram placeholder, and §2.2 reserves the site monogram for the favicon, the loader, the default avatar and the map marker. Putting our mark in somebody else's logo box reads as a claim about who built the project.
- 2026-09-16 — P16 — "Established" is printed as it is written (`2004`), without thousands grouping and without counting up from zero; only the three project counts animate. — A year is a label, not a quantity: `formatNumber` would render it "2,004" in the Indian grouping, and counting 0 → 2004 says nothing. The prompt's "`StatCard`s with `useCountUp`" is honoured by the figures that are quantities.
- 2026-09-16 — P16 — `/builders` lists every **active** developer, including those with no active listing, and the card says "0 projects". — §7 of the prompt asks the question and answers it: a builder page is linked from every property that names it, and hiding the company because its inventory is momentarily empty would break those links rather than tidy the index.
- 2026-09-16 — P16 — The `/builders` sort select offers "Recommended" (`order`) beside the prompt's `name` and `propertyCount`, and the search box debounces at 300 ms. — Same reasoning as prompt 14's locality sort: `order` is the API's default and the order an editor curates in the admin, so leaving it out of the select would make the default view unreachable once the visitor sorted. The debounce is §5.6's "the API hears the last keystroke".
- 2026-09-16 — P16 — The admin developers table's "Updated" column is not sortable. — The contract's developer sorts are `order|name|propertyCount` (§6.5 and `mock-server/schemas/models.js`); `sort=updatedAt` falls back to the default server-side, so a sortable header would promise something the API does not do, and the mock is out of this prompt's reach (§12).
- 2026-09-16 — P16 — The builder page's `website` link carries `rel="noopener noreferrer nofollow"`, not the `rel="noopener nofollow"` the prompt names. — `react/jsx-no-target-blank` warns on `target="_blank"` without `noreferrer`, and `npm run lint` runs with `--max-warnings=0`; the triple is what every other outbound link on the site already carries, and it keeps the `nofollow` the prompt asked for.
- 2026-09-16 — P16 — The three project counts are validated as a warning, never a refusal: `ongoing + completed > total` renders an `Alert` under the fields and the form still saves. — §4.2 of the prompt ("client warning, not blocker"); the counts are what the builder publishes about itself and the API stores whatever it is told (§6.5), so the form says what looks wrong rather than deciding it is.
- 2026-09-16 — P16 — The developer form narrows two of the schema's ranges in the browser: `establishedYear` to 1950–current year and `website` to a URL carrying its protocol, each with its own message. — §6.5 allows any year from 1800 and the `url` type refuses a bare domain with "The website must be a valid URL"; §7 of the prompt asks the field to say "include `https://`", and an editor typing a founding year wants the window the form means. The server stays the wider authority.
- 2026-09-16 — P16 — `src/components/sections/developer/DeveloperLogo.jsx` is a sixth component in a folder the prompt lists five for. — The white logo box is used by the card and by the hero, and the alternative was the same ten lines twice; it renders from the folder's shared stylesheet, so it adds no second CSS module to order against the card's (the `mini-css-extract-plugin` conflict of prompt 14).
- 2026-09-16 — P16 — The builder page's description and highlights are laid out by `BuilderDetail` itself rather than by a `DeveloperAbout` section component. — The prompt's component list has no section for them, and the page already owns the same pattern the locality detail page uses for its map block: a page-level block styled from the page's own module.
- 2026-09-16 — NEW-30/P16 — `DeveloperStats` keeps `useCountUp` gated on `useInView`, as `BuilderOverview` already does, and the prerendering problem is recorded for prompt 41 instead of being designed around here. — A browser that never delivers a second animation frame leaves every counted figure at zero, which a prerender would bake into the HTML. The fix belongs where the prerenderer is written and is one line there (emulate `prefers-reduced-motion: reduce`, which makes `useCountUp` jump to the value); changing the hook or dropping the animation would have solved it for this page only, and §3.2 pairs the two hooks on purpose.
- 2026-09-16 — D41/P17 — `TestimonialsSection` filters its own items: inactive records never render, and `isSample: true` records render only while `process.env.NODE_ENV !== 'production'`, under a small "Sample" chip. The section hides itself when nothing is left. — Spec HOME-05 / D41. The gate lives in the component rather than in the pages that mount it (home in 27, About in 30, the CMS block in 30), so no page can forget it and CRA's dead-code elimination is what removes the seeded placeholders from a real build. `isRenderable()` is exported and unit-tested with `NODE_ENV` set both ways.
- 2026-09-16 — D83/P17 — The FAQ page's "Still have questions?" block renders `ContactMethods`, which reads `contactPhone`, `contactEmail` and `whatsappNumber` from `siteSettings.general` through `useSiteSettings()`; a method the client has not provided is not rendered, and with all three empty the component returns `null` rather than an empty grid. — D83 and §14: the boilerplate printed a US number here (ADD-17). The contact page of prompt 30 mounts the same component.
- 2026-09-16 — D98/P17 — A reorder is **one** `PATCH { order }` on the record that moved, carrying the position of the row it landed on (`neighbour.order` moving up, `neighbour.order + 1` moving down); the API then sorts the collection by `order`, breaks ties in favour of the newest `updatedAt` — the record the `PATCH` just touched — and renumbers everything `1..n`. Implemented in `mock-server/lib/crud.js` for every collection with an `order` field, documented in `docs/API_CONTRACT.md` §5.8 and issued by `MasterDataPage`. — The boilerplate's `FaqManager` computed a swap against the **filtered** array and wrote two sequential `PUT`s, so dragging inside a category filter moved the wrong records (NEW-23, ADD-21); prompt 13's kit then wrote `offset + index` for every changed row, which is correct on an unfiltered page and wrong on a filtered one. Saying "put it where that one is" is the only thing a client holding a slice of the collection can say truthfully, and it makes the server the single author of the numbering. The tie-break direction is what places the moved record before its neighbour when it travelled up.
- 2026-09-16 — P17 — `SortableList.onReorder` gained a second argument, `{ from, to, item }`. — D98 needs to know which row travelled and where it landed; the new array alone cannot say, and every existing caller (the two `EntityPicker` lists) ignores it.
- 2026-09-16 — P17 — A `MasterDataPage` column's `render` is called with `(row, { patchField, busy, canEdit })`. — The FAQ table's "Home" switch and the team table's "About page" switch are optimistic `PATCH`es of the same kind the built-in `Active` column makes, and a configuration object has no way to reach the list's state. Additive: the twelve existing columns that render take one argument and keep working.
- 2026-09-16 — P17 — The drag list of an orderable screen now carries a "Back to the table" button (it sorts by the first sortable column that is not `order`). — Four of the eight orderable screens default to `sort=order`, and the drag list replaces the table headers entirely, so the only way back to the toggles, the bulk actions and the row menu was to edit the query string by hand. The button closes a gap prompts 13–16 left as well as this one's.
- 2026-09-16 — P17 — `MasterDataForm` gained a `rating` control: five stars rendered as a radio group with a visible `n/5`. — §4.2 of the prompt asks the testimonial form for a 1–5 `Rating` input. Radios rather than buttons because the control is then one tab stop, moves with the arrow keys and announces "4 stars"; the numeral is there because an icon font that has not loaded must not leave the field looking empty.
- 2026-09-16 — P17 — The FAQ form narrows the stored contract in the browser: the question to 10–200 characters (the schema allows 5–300) and the answer to at least 20 characters of **text** after the tags are stripped; the testimonial form narrows the quote to 20–600 (the schema allows 10–1000). — §4.1 of the prompt states the editorial limits, and every seeded record satisfies them (questions are 30–66 characters, answers 231–368, quotes 174–243), so no existing record becomes unsavable. The server stays the wider authority, as it is for prompt 16's developer form.
- 2026-09-16 — P17 — `stripHtml` is a local helper of `src/pages/admin/content/contentConfigs.js` rather than a new shared util. — It has exactly one caller (the FAQ table's answer preview and the rule that measures it); prompt 32's `SafeHtml` and its sanitiser are where HTML handling becomes shared, and a second copy of the mock server's `lib/html.js` in `src/utils/` would have to be kept in step with it for no reader's benefit. It is exported so a test can reach it.
- 2026-09-16 — P17 — A team member's slug is labelled "Identifier" and its `SlugField` base is `#`. — §6.9 gives `teamMembers` a slug but the site has no per-member page; the prompt asks for the `#` base, and the label says why the box is there — the CMS `team` block and a listing's `agent` refer to a member by id, and the slug is what makes those references legible.
- 2026-09-16 — P17 — The `/insights/faqs` tab row is built from a second, unfiltered `GET /faqs?perPage=100` issued once on mount, not from the filtered list the page renders. — The tabs have to name the categories that hold a question (§4.8), and a filtered answer can only ever name one. The rendered list stays server-filtered (`?category=&q=`), which is the half of BUG-18 that mattered: the page never downloads everything to _display_ it.
- 2026-09-16 — P17 — `PartnersSection` swaps the marquee for a static grid under `prefers-reduced-motion`, rather than only pausing the animation. — A track that is 200% wide and no longer moving hides half the partners off-screen; the CSS `animation: none` stays as the belt-and-braces for a paint that happens before the JS reads the media query.
- 2026-09-16 — P17 — Partner logos are rendered without the boilerplate's `filter: grayscale(100%)`. — §2.2 forbids recolouring a logo with CSS filters, and the rule is about other companies' marks as much as ours; §6 of the prompt asks for it explicitly.
- 2026-09-16 — P18 — The property form is a `useReducer` (`property-form/reducer.js`) rather than the admin kit's `useForm`. — `useForm` is built for a flat record: one `values` object, one schema, `setField` per path. A property is sixteen tabs over nine nested branches and eleven repeating lists, where the common operations are "add a row", "move a row" and "patch the row with this id" — three things `setField` can only express as a whole-array write from inside each tab. The reducer owns those as actions, so a tab dispatches rather than reconstructs, and `PropertyFormContext` hands every tab the same five helpers. `useForm` stays the kit's answer for every other admin form.
- 2026-09-16 — P18 — `area.plotDimensionUnit` defaults to `null`, not `'ft'`. — Prompt 18 §4.1 asks for `'ft'`, but the contract types the field as an `AREA_UNITS` value (§6.1, `src/services/schemas/property.js`), and `AREA_UNITS` holds `sqft, sqm, sqyd, acre, cent, guntha` — no length unit at all. Sending `'ft'` is a 422 on every save, and the seed already stores `null`. `00_MASTER_CONTEXT.md` wins over a prompt when the two disagree.
- 2026-09-16 — D33/P18 — `toPayload` computes `pricing.pricePerSqft` as `round(price / (superBuiltUpArea ?? carpetArea))`, and **only when the field is empty**. — The figure is editable (§6.1 lists it as a stored field, not a computed one), so an editor who types 9 500 because the developer quotes 9 500 must keep it. Deriving it when it is blank means the details page and the SEO `%price%` variable have a number to show without anyone maintaining it twice.
- 2026-09-16 — P18 — "A listing must be priced" is a publication rule, not a save rule: the requirement of PROP-03 (`pricing.price` or `priceOnRequest` or a complete range; `rentPerMonth` for rent/lease) lives in `validateForActivation`, while `validatePricing` keeps the shape checks (non-negative, a range with both ends, min below max, complete `otherCharges` rows). — `pricing.price` is nullable in the contract and the API accepts a listing without one, and §2/§9 of prompt 18 require a property to be creatable from the Basics fields alone — which hold no price until prompt 19. Blocking the save would have made the prompt's own acceptance flow impossible; blocking the publication keeps the promise to visitors.
- 2026-09-16 — P18 — The save split: one primary **Save** (labelled "Create property" on a new listing), with "Save & view on site" and "Save as inactive" behind a menu beside it. — §7 of the prompt offers "Save / Save & continue / Save & view / Save as inactive" and notes the decision to record. "Save & continue" is the same write as "Save" under a longer name — the form never leaves the page — so a second button would be two labels for one action. The two that genuinely differ (publish-then-look; store-without-publishing) stay one click away, and the phone's sticky bar carries "Save as inactive" and "Save" directly, because a menu is the wrong control for a thumb.
- 2026-09-16 — P18 — Completeness weights (total 100): basics 10, description ≥ 300 characters 10, cover + five described images 15, pricing 10, area & configuration 8, ≥ 8 amenities 8, ≥ 3 highlights 4, locality + coordinates 8, a floor plan or a unit configuration 6, a brochure or a document 4, ≥ 3 nearby places 4, ≥ 3 FAQs 5, builder 3, SEO title + description + focus keyword 5. — The meter answers "what does a buyer still not know?", so the photographs and the description outweigh any single input, and a page with no price is not 90 % finished. The rail colours it by band (error < 40, warning < 75, success ≥ 75) and the checklist names every item, so the number is never a verdict without a reason.
- 2026-09-16 — P18 — The rail's completeness meter is a `<div role="progressbar">` styled with the design tokens rather than MUI's `LinearProgress`. — Prompt 18 §7 names the MUI component, but no admin screen written since prompt 13 imports MUI at all: the kit, the tokens and the CSS modules are the house system, and an emotion-styled bar inside a CSS-module rail would need its colours passed as `sx` hex values, which §4.3 forbids. The element carries `aria-valuenow/min/max` and a label, which is what `LinearProgress` renders anyway.
- 2026-09-16 — P18 — `SET_ERRORS` carries the flat, dotted map (`{ 'location.localityId': '…' }`); the per-tab counts are derived by `tabs.js` `groupErrorsByTab`. — Prompt 18 §4.2 names the payload `errorsByTab`, but §4.2 also fixes the state as `errors { [path]: msg }`, and a 422 arrives dotted (§5.3). Keeping one keyspace means a server message and a client message land on the same control, and the badge is a `reduce` over it rather than a second thing to keep in step.
- 2026-09-16 — P18 — The tab strip's columns are `minmax(0, 1fr)` on every grid the form nests (`.layout`, `.stacked`, `.main`, `.panels`, the rail's blocks). — Sixteen tabs with `flex-shrink: 0` give the strip a min-content width of about 1 900 px. An `auto` grid track sizes to that, so the form column grew past the viewport and slid under the 320 px rail; pinning the track lets `AdminTabs`' own `overflow-x: auto` do its job and the strip scroll.
- 2026-09-16 — P18 — The rail sits beside the form from 1200 px (`useBreakpoint().width === 'lg'`) and folds into a "Status & actions" accordion above the tabs below it, with a sticky Save bar at the foot. — §6 of the prompt asks for the two-column layout at ≥ 1200 px and the accordion on mobile, and leaves 900–1199 px unnamed. One switch rather than two: at 1100 px the content column is already under 800 px, where a 320 px rail costs more than it gives, and a third layout would be a third thing to test.
- 2026-09-16 — P18 — The rail is rendered once — the desktop `<aside>` or the phone accordion, chosen in JS — rather than both with CSS hiding one. — The rail's headings carry ids that its sections are labelled by; two copies would duplicate them, which is invalid and makes the labels ambiguous to a screen reader.
- 2026-09-16 — P18 — A possession-date field lives in the Basics placeholder alongside the fields §9 of the prompt lists. — §6.1 makes the date mandatory for a pre-launch or under-construction listing, and the status select that demands it is one of the shell's own fields: without the date beside it, choosing "Under Construction" would leave a listing that cannot be saved until prompt 19 lands.
- 2026-09-16 — P18 — `usePropertyForm` takes the record as a prop; the page owns the fetch. — The four states of §8.2 (loading skeleton, 404 empty state, error with a retry, loaded) belong to the screen, and `useApi` already provides them. The hook re-reads a record only when its `id:updatedAt` stamp changes, so the refetch that follows the redirect to `/edit/<id>` after a create cannot overwrite anything typed in the meantime.
- 2026-09-16 — P18 — `src/utils/format.js` gained `formatTime(value)` (`04:30 pm`, IST). — The rail's autosave indicator reads "Draft saved 10:42" (§6), and §4.3 allows no date formatting outside `format.js`; `formatDateTime` spells the whole date, which does not fit a 320 px rail beside "Last saved 2 minutes ago".
- 2026-09-16 — P19 — The field-visibility rules live in `property-form/fieldRules.js`, take the whole `values` object, and are read by the tabs, by the confirm dialogs and by a unit test that renders nothing. — "Does this listing have bedrooms?" is a function of the segment, "does it have a possession date?" of the construction status, and "does it have a price range?" of the listing type; written inline, each rule would be repeated in the tab that shows the field, in the handler that clears it and in the validator that excuses it, and the three copies would drift. The prompt names eight of them; the file adds `showsBuiltAreas`, `showsFurnishing`, `showsFloors`, `clearedBySegment`, `pricingFieldsClearedBy` and `anyFilled` because the confirm dialogs need to know exactly what they are about to throw away.
- 2026-09-16 — P19 — `showsFloors` and `showsFurnishing` are "not a plot" rather than a list of property-type slugs. — §4.1 of the prompt names "apartments/office" for the floor fields, but a villa has a total floor count, a row house has ownership and a warehouse has both; keying the rules on the seventeen type slugs would hide fields an editor needs and would have to be edited every time master data gains a type. Bare land is the one case where all four genuinely do not apply, and that is what the rule says.
- 2026-09-16 — D33/P19 — The per-sq-ft rate is **shown** derived and **stored** only when somebody types one: an empty `pricing.pricePerSqft` means "follow the price", and `toPayload` fills the same figure in on save. — §3 of the prompt asks for a `pricePerSqftManual` flag in the tab's own state, but tab state dies when the editor moves to another tab and comes back, which would turn an automatic rate into a manual one by accident; and writing the derived figure into the record from an effect makes a freshly loaded listing dirty before anybody has touched it. The record is the flag, so the rate survives a tab switch and a reload still following the price, and clearing the field is how an editor goes back to the computed rate.
- 2026-09-16 — D33/P19 — `toPayload`'s derivation moved into `fieldRules.derivedPricePerSqft`, which converts the area to square feet through `AREA_UNITS.toSqft` and falls back super built-up → carpet → plot. — Prompt 18 divided by the raw number, so a listing measured in sq yd or guntha produced a rate that was wrong by the unit's factor, and the Pricing tab would have had to repeat the arithmetic to display it. One function, two callers, one figure on screen and in the payload.
- 2026-09-16 — D42/P19 — `MapPinPicker` injects the Maps JS API through `src/utils/loadScript.js` only when `settings.integrations.googleMapsApiKey || process.env.REACT_APP_GOOGLE_MAPS_KEY` is set; the script is fetched once per URL, a failure rejects, and the component falls back to the keyless `output=embed` iframe with a warning alert and a toast. — D42 allows the keyed pin where a key exists and no dependency anywhere. The loader is shared rather than inline so a second caller reuses the promise instead of adding a second `<script>` (which is what makes the Maps API complain about being loaded twice), and a failed load is deleted from the cache so a later mount may try again. The URL carries `&v=weekly` and no `callback`: `loading=async` without a callback logs a console warning, and the `load` event is what the component waits for anyway.
- 2026-09-16 — P19 — `area.plotDimensionUnit` offers two options, `sqft` labelled "Feet (ft)" and `sqm` labelled "Metres (m)". — §4.4 of the prompt asks for `ft`/`m`, but the contract types the field as an `AREA_UNITS` value (`src/services/schemas/property.js`), which holds no length unit, so `'ft'` is a 422 on every save (the same clash prompt 18 logged for the field's default). The square of each of those two units is exactly the area unit of the same name, so `plotAreaFrom` multiplies length × width and converts to whatever `area.areaUnit` the listing is stored in through `AREA_UNITS.toSqft` — 30 × 40 ft is 1 200 sq ft, 30 × 40 m is 12 916.68 sq ft.
- 2026-09-16 — P19 — Floors stay on Basics; Area & configuration does not mirror them. — §4.4 of the prompt raises the question and leaves it open. A read-only mirror is a field that looks editable and is not, and a second editable copy is two controls writing one path; "where is this unit in the building?" belongs with furnishing, facing and ownership, which are all on Basics.
- 2026-09-16 — P19 — Commercial washrooms, pantry and server rooms, and a plot's khata and approvals, are entered as `specifications` / `project.approvals` rather than as fields of their own, and the tab says so in an `Alert` naming the tab that holds them. — §4.4 asks for the hint. A commercial unit's fittings vary too much to enumerate, and `specifications[] { group, label, value }` is the contract's answer for exactly that (§6.1); inventing `washrooms` and `pantry` columns would put them outside the model.
- 2026-09-16 — P19 — Badges are picked on Basics, and `tabs.js` moves `badgeIds` from the Amenities tab's field list to the Basics tab's. — §4.1 puts the picker on Basics. The field list is what turns a dotted error key into a red badge on a tab (§5.3), so leaving `badgeIds` on Amenities would send an editor to a tab with no badge control in it.
- 2026-09-16 — P19 — `nearbyPlaces` is stored flat and ordered (§6.1) but shown grouped by `NEARBY_CATEGORIES`, and a reorder inside a group is translated into one `LIST_MOVE` of the flat list. — An editor thinks "which schools are near this?", not "which of these twenty rows are schools", and the public page groups them the same way. The translation keeps the reducer, the payload and the API speaking one shape, and reuses `SortableList`, so dragging, the ↑/↓ buttons, `Alt`+arrows and the `aria-live` announcement all come for free.
- 2026-09-16 — P19 — `ImageGalleryEditor`, `NearbyPlacesRepeater` and `OtherChargesRepeater` take props rather than reaching for `usePropertyFormContext()`. — The tabs read the context and pass what each component needs, which is what lets the gallery — the one with real behaviour to get wrong (cover, alt, reorder) — be tested against a plain `useState` host instead of a mocked form. `coverAfterRemoval` is exported for the same reason: promoting the first remaining image when the cover goes is a rule, and a rule is worth asserting without a render.
- 2026-09-16 — P19 — A new `NumberWithUnit` control (built on the kit's `Field`) carries the `₹` prefix, the unit suffix, right-aligned digits and the formatted figure underneath, rather than a `NumberField` with an adornment prop. — §6 of the prompt asks for the adornments and §8.4 fixes `NumberField` as a plain number input used by twelve other admin screens; a new prop on the shared field would change all of them for the benefit of one form. The readout is the part that matters: money is typed as `15000000` and printed as `₹1.5 Cr`, and nobody can proof-read that gap without seeing both.
- 2026-09-16 — P19 — The possession date is an `<input type="month">` storing `yyyy-mm-01`. — §6.1 types the field as a date and §4.1 of the prompt asks for month granularity; buyers compare listings by quarter, and a developer who promises "June 2027" has not promised the 14th. The first of the month is what the contract stores, so the API and the public page see an ordinary date.
- 2026-09-16 — P19 — A unit configuration priced "on request" with no area is refused ("Add an area or a price"), and the row counts as touched so it cannot be silently dropped. — §7 of the prompt names the case. `toPayload` keeps a row that has a name and drops one that has nothing, and a row that prints neither a size nor a figure is an empty line in the configuration table — the validator says so rather than letting it reach the page.
- 2026-09-16 — P19 — `validateLocation` now asks for a name on **any** nearby row, not only on one that carries a distance. — `toPayload` drops a nameless row (`KEEP_ROW.nearbyPlaces`), so the old rule let an editor add a row, type a distance in the wrong box and lose it on save without being told.
- 2026-09-16 — P19 — `validateMedia` refuses a gallery with images and no cover ("Choose which photograph is the cover"). — `toPayload` already promotes the first image when none is marked (§6.1 asks for exactly one), but silently choosing for the editor is how the wrong photograph ends up on the card; the gallery editor keeps a cover at all times, so the message only ever fires on a record that arrived without one.
- 2026-09-16 — P19 — The carpet ≤ built-up ≤ super built-up rule is an inline `Alert` on the Area tab, not an entry in the error map. — §4.4 of the prompt calls it a warning. The three figures come off a builder's sheet, the sheet is sometimes wrong, and the listing page prints all three: refusing the save over an arithmetic quibble helps nobody, while saying nothing lets it reach the public page.
- 2026-09-16 — P19 — `MultiSelect` drops the `×` from its chips when the field is disabled. — MUI's `getTagProps` hands back its remove handler whether or not the `Autocomplete` is disabled, so a sales user reading a listing found the badge chips were the only live controls on the screen (§7 gives them no write at all). Prompt 19 is the first screen to render the picker read-only, which is why it surfaced here.
- 2026-09-16 — D39/P20 — `constructionSpecs` gets its own `FormSection` on the Highlights & specifications tab, above nothing and below the `specifications` repeater, rather than a tab or a toggle of its own. — D39 renders both blocks inside the public Specifications section under one visibility key; two sections on one admin tab is the closest the form can get to that, and it keeps the two lists visibly different (one carries icons, one does not) without pretending they are one list.
- 2026-09-16 — P20 — `SpecificationsRepeater` is one component used twice (`withIcon` on / off) rather than two files. — `specifications` and `constructionSpecs` differ by exactly one field in §6.1. The grouping, the "Add row to <group>" buttons, the empty-group collapse and the single-`moveItem` drag are the behaviour worth getting right once; a second copy would drift the moment one of them gained a fix.
- 2026-09-16 — P20 — A drag inside a `SPEC_GROUPS` group is translated into one `moveItem` of the flat list, the way prompt 19 did it for `nearbyPlaces`. — The legacy `DetailsTab` rewrote every row of the list on every drag (`updateListItem` per row), which is what made it crawl on a long specification sheet; the reducer already has a single `LIST_MOVE`.
- 2026-09-16 — P20 — A specification row with a **label and no value** is not an error, and is **not sent to the API**: `validateHighlights` only refuses a value with no label, and `toPayload` keeps a row only when it has both halves. The repeater says so under the list ("3 rows have no value yet and are not saved"). — "Add standard rows" exists to lay out twenty-one labels with empty values (§4.2), and the shared schema (`src/services/schemas/property.js`) makes `value` required, so the API answers a blank one with a 422. Refusing the save would make the preset unusable; sending it is impossible; dropping it silently would be a trap. Dropping it with the count on screen is the only option left, and re-pressing the button re-lays the scaffolding.
- 2026-09-16 — P20 — The construction-specification presets (21 rows): Structure — RCC framed structure; Flooring — Living / Dining, Bedrooms, Kitchen, Balconies; Kitchen — Counter, Sink; Doors & Windows — Main door, Internal doors, Windows; Bathroom — Sanitary ware, CP fittings; Electrical — Wiring, Switches, Power backup; Walls & Painting — Internal, External; Security — CCTV, Intercom; Lift & Common Areas — Lifts, Lobby. The button inserts only the rows the listing does not already hold (same group, same label, ignoring case) and says how many that is. — §4.2 of the prompt fixes the list. The labels are the questions a buyer asks; the answers are the developer's, so every preset value is empty.
- 2026-09-16 — P20 — The five standard milestones (Foundation, Structure, Masonry, Finishing, Handover) are inserted the same way — only the ones missing, matched case-insensitively — and the button disappears once the timeline holds them all. — §4.5. A preset that silently duplicates what is already there is worse than no preset.
- 2026-09-16 — D40/P20 — `highlights` is a `string[]` in the contract, so the sortable list keys its rows from a parallel array of generated keys held in a ref, moved and spliced with the rows. — §4.3 of the master context forbids an array index as the key of a reorderable list, and the contract gives a highlight no id of its own. The ref is reconciled by length, so a record load or a draft restore re-keys the rows rather than reusing stale keys.
- 2026-09-16 — P20 — "From short description" splits on a sentence terminator **followed by whitespace or the end of the text** (`/[.!?]+\s+|[.!?]+$|\n+/`), drops a sentence longer than 140 characters rather than truncating it, and the button carries the count of what it would add (disabled at zero). — A lookbehind would have been shorter but is not safe on every browser in the support range; requiring the whitespace keeps "1.2 km from the metro" one highlight. Half a sentence on a card is worse than no card, and a button that reads "From short description" and then does nothing — because the summary is one long sentence — is worse than one that says "(0)".
- 2026-09-16 — P20 — "Generate from unit configurations" skips a unit whose **title or drawing** the floor-plan list already holds. — §4.3 names the title. Matching the image as well is what makes "generating twice does not duplicate" (§7) true after an editor has renamed a generated plan — which is exactly what the seed does ("2 BHK" → "2 BHK — 1,180 sq ft").
- 2026-09-16 — P20 — The amenity groups render as `FormSection`s with the select-all checkbox in the section's `action` slot, and the **search bar** is what sticks to the top of the tab rather than the group headings. — §6 asks for sticky group headers; a `<legend>` cannot be sticky in any browser, and §4.1 asks for `FormSection` per group. The search is the control an editor reaches for while scrolling forty-odd chips, so it is the one that follows them down. The two grouped repeaters (`SpecificationsRepeater`) use their own headings and do stick.
- 2026-09-16 — P20 — `amenityIds` is written back sorted by group order and then by the order inside the group, and an id master data no longer holds keeps its place at the end. — The public page prints the groups in `AMENITY_CATEGORIES` order, so storing them in that order makes the record and the page agree without a sort at render time. Dropping an unknown id would quietly un-claim an amenity that was switched off in master data after this listing promised it.
- 2026-09-16 — D62/P20 — The quick-create developer dialog refuses a name master data already holds, and refreshes the developer list when it opens so that check is made against the current one. A 409 from the API is handled with the same message. — §6.5 makes only the **slug** unique, and the mock answers a duplicate name with a second record and a suffixed slug (verified); without the client-side refusal the edge case of §7 could not happen at all and an editor would create "Aurelia Estates" twice by accident. The refresh is one request per dialog open, against a ten-minute cache (D93).
- 2026-09-16 — P20 — The property form's tab context gained `goToTab(key)`, wired to the shell's own tab change (so leaving a tab still validates it). — Documents summarises the brochure that lives on Media and Project & builder points at the RERA switch on Basics; a link is the honest way to reach a field that lives elsewhere, and a second control writing the same path is not.
- 2026-09-16 — P20 — The construction timeline is **highlighted, not hidden**, for a ready-to-move or resale listing: the section keeps a dashed border and an `Alert` reading "Optional for completed properties". — §4.5 asks for exactly this. A finished project with a handover date still has a story worth telling, and hiding the fields would lose what a record already holds.
- 2026-09-16 — P20 — `suggestFaqs(values, context)` is a pure function at the property-form root, beside `fieldRules.js` and `completeness.js`, and returns `[{question, answer}]` with the answer as `<p>` HTML. Six candidates are built (price, possession, RERA, amenities, location, configurations), the ones already asked are dropped by a normalised comparison of the question, and the first five of what is left are offered. — §4.6. Keeping it pure is what lets the dialog preview exactly what would be added and lets a test assert the wording of every branch.
- 2026-09-16 — P20 — The suggestion phrasing policy: **an answer never claims more than the record holds.** No price becomes "Price is available on request"; no possession date becomes "A possession date has not been announced yet"; no RERA number becomes "Registration details will be shared on request"; no amenities, no locality and no configurations mean the question is not suggested at all. Every clause maps onto a field, the editor's own text is HTML-escaped, and nothing is added until somebody has read it in the preview dialog. — §14 of the master context forbids inventing facts, and a generated FAQ is published prose. The preview with checkboxes (§6) is the second half of the same promise.
- 2026-09-16 — D64/P20 — A document row's `leadGated` defaults to **on**; the brochure keeps its own field and its own gate on Media, and the Documents tab shows what that field holds with a link to it rather than a second copy of the control. — §6.1 keeps `brochureUrl`/`brochureLeadGated` separate from `documents[]`; two ways to attach the brochure would mean two brochures.
- 2026-09-16 — D66/P20 — A FAQ answer containing `<script>`, `<iframe>`, an `on…=` handler or a `javascript:` URL is refused by `validateFaqs` with "An answer may hold formatting, not code". — §7 asks for the regex block here and the real sanitiser in prompt 32; the answer reaches the public page through `LegacyHtml` (`dangerouslySetInnerHTML`) until then, so the one thing that turns an answer into code is worth refusing now. A question is also held to 10–200 characters (§4.6).
- 2026-09-16 — P20 — `src/utils/format.js` gained `formatMonthYear(value)` (`March 2027`, IST). — A suggested possession answer quotes a month, not a day (§4.1 of prompt 19 already stores `yyyy-mm-01`), and §4.3 allows no date formatting outside `format.js`.
- 2026-09-16 — P20 — `useMasterData.js` gained `useDeveloperSearch()`, which returns the developers plus a `search` function shaped like a service `list` for `EntityPicker`. — The picker is written against the API (§5.6), but developers are one of the seven lists `MasterDataContext` already holds (D93): searching in the browser answers instantly, costs no request per keystroke, and reads the same array a `refresh('developers')` rewrites, so a developer created from inside the form is findable the moment it exists.
- 2026-09-16 — P21 — **Properties have no preview token.** An unpublished listing is previewed at `/properties/<slug>?preview=admin`, and the page serves it only while `useAdminAuth().isAuthenticated`, by reading a new `GET /admin/properties/slug/:slug` behind the ordinary Bearer token. — D28 gives articles and pages a signed 24-hour token because a draft article is shown to somebody who is **not** an editor (a client, a reviewer). Nobody asks to be shown an unfinished listing: the only reader is the person who is editing it, and they already hold a token. A signed token per property would be a second secret to mint, store, expire and leak for a case that does not exist, and the query string on its own grants nothing — dropping the session answers 404, which is what §5.10 promises. The admin endpoint is by **slug** because a public URL carries no id.
- 2026-09-16 — P21 — `src/utils/propertySections.js` is created now, by the admin's Section-visibility tab, rather than with the public page in prompt 23. — Eighteen switches are useless without saying which of them would show anything, and "would this section show?" is exactly the question the page asks. Writing the rule twice is how the toggle and the page came to disagree in the boilerplate (BUG-06); `getVisibleSections(property, context)` is exported for the page and `getSectionHints(values, context)` renders the chips, both over one `SECTION_DEFINITIONS`, and a unit test holds its eighteen keys to `SECTION_VISIBILITY_KEYS`.
- 2026-09-16 — P21 — `SECTION_DEFINITIONS` takes a `context` of `{ banksAvailable, similarAvailable }` rather than reading the master data itself. — Two of the eighteen rules are not facts about the property: the finance section needs an active lender (§6.6) and the similar section is topped up by the API (§5.14). A pure function that answers the same way for the same arguments can be tested without a provider, and the two callers — a React tab and the page — each already hold the answer (`useBanks()`, the `/similar` response).
- 2026-09-16 — P21 — The finance hint reads "Hidden automatically — no active banks" / "— sale listings only" instead of "No data yet". — §7 of the prompt names the first case. A hint that tells an editor to add something they cannot add from this form is worse than no hint; both sentences name the reason and neither asks them to act.
- 2026-09-16 — P21 — The Agent tab keeps a **copy** of the team member's name, number, WhatsApp, e-mail and photo beside `teamMemberId`, and changing the member asks before overwriting fields that differ. — §6.1 stores both, and the copy is what makes an old listing keep the name it was published with after somebody leaves. The confirm dialog is §7's edge case; it fires on a genuine difference only, so re-picking the member a listing already names is silent.
- 2026-09-16 — P21 — `agent.showOnListing` over an empty agent is a rail **warning**, not a refusal, and is not raised at all when the listing names a team member. — Every field of `agent` is optional in §6.1 and the API backfills the display fields from `teamMembers` on read, so half the seed would have been unsavable. Nothing here blocks a draft (PROP-03).
- 2026-09-16 — P21 — `EntityPicker` gained `renderSelected`, `action` and an `aria-label` on each ordered row's Remove button. — A similar property is chosen by looking at a thumbnail, a locality and a price, and has to be recognisable again in the chosen list; six buttons all called "Remove" are six identical announcements to a screen reader. All three are additive: a picker that passes none of them renders exactly as before.
- 2026-09-16 — P21 — The Similar tab's card lookup (`GET /admin/properties?ids=…`) forgets its request key in the effect's **cleanup**, not in the request's `catch`. — `StrictMode` mounts every effect twice in development: the cleanup aborts the first request and the second run starts before the rejection is delivered, so a key cleared in the `catch` is cleared too late and the cards read "#2" for the rest of the visit. `remember()` also returns the array it was given when an answer adds nothing, because `known` is a dependency of that effect and a new identity per answer would ask for the same ids for ever.
- 2026-09-16 — P21 — Ctrl/Cmd+S is registered on `window` by `usePropertyForm`, reads `save` through a ref, and is skipped entirely for a read-only (sales) form. — The shortcut has to work from inside any of the sixteen tabs, which rules out a handler on the form element; the ref is what keeps the listener registered once instead of on every render. `preventDefault()` stops the browser offering to save the HTML of the page.
- 2026-09-16 — P21 — "Save & view on site" becomes "Save & preview" and the rail's "View on site" becomes "Preview" while a listing is unpublished, both pointing at `?preview=admin`. — The old rail hid the link altogether until the listing was live, which is precisely when an editor wants to look at the page. The note under it says who can see it.
- 2026-09-16 — NEW-31/P21 — `SlugField` treats a `value` it did not write itself as an edit: it unlocks instead of overwriting. — The property form's tabs mount one render before the reducer's `LOAD` effect runs, so the field initialised itself as "following the title" over an empty slug and then rewrote a published listing's URL from its title. Every write the field makes goes through one `write()` that records it, so only a value arriving from outside — a record, a restored draft — unlocks it.
- 2026-09-16 — D40/P21 — `toLegacyProperty` maps `highlights` (strings) onto the `specialities` objects the public "Property Highlights" section reads, splitting "name — description" on the dash. — The adapter had been handing that section an empty array since prompt 11, so the section had silently stopped rendering; D40 says the highlights are what feed it. The rest of the adapter shed the four mappings only the deleted `PropertyForm.jsx` used (`publishStatus`, `propertyTypeName`, `statusLabel`, `category`).
- 2026-09-16 — D87/P21 — The SEO tab hosts `SeoPlaceholderTab`: `seo.title`, `seo.description`, `seo.focusKeyword` with the §9.1 length guides, and a read-only mirror of the slug. — D87 puts the full panel here and prompt 36 writes it; a tab that says "later" for fifteen prompts would mean no listing could carry a title of its own in the meantime. The fields write through the reducer into the same `seo` branch the panel will open, and the alert names what is still to come so nothing looks lost.
- 2026-09-16 — D44/P22 — The admin property export is built in the browser from `GET /admin/properties?perPage=all&<the current filters>`, through `src/utils/csv.js`. — Eleven filters and six sorts already live in the URL; an export endpoint would have to reimplement every one of them and then be kept in step with them. The one thing the client cannot fake is the data, and `perPage=all` hands it over.
- 2026-09-16 — P22 — `utils/csv.js` prefixes a value that begins with `=`, `+`, `@`, a tab or a carriage return with a `'`, and leaves a leading `-` alone unless what follows is not a number. — A CSV is opened in Excel, which evaluates those four. The exception is what keeps `-500` a number: the later exports (leads, prompt 29) carry both figures and text typed by strangers, and mangling every negative to guard against a rare formula would be the worse trade.
- 2026-09-16 — P22 — Sales may export the property list (they hold `properties.view` and nothing else). — The file holds exactly the rows already on their screen, built from the answer the API already gave them; refusing it would only push them to copy the table by hand. D15's export rule for leads (scoped, server-side) is the same reasoning.
- 2026-09-16 — D47/P22 — The list keeps `DataTable`'s 20 rows and its 10/20/50/100 choice, with `perPage` in the URL. — D23/D47; the page size is part of the view a shared link reproduces.
- 2026-09-16 — P22 — `useApiList` is given `paramKeys` as a `{ key: type }` map rather than the array of §4.1, so `constructionStatus` is read back from the URL as a list. — It is the one multi-value filter of the screen; as an array of keys the hook would infer `string` from an absent default and hand `MultiSelect` a comma-separated string.
- 2026-09-16 — P22 — The table carries a narrow "Priority" column (`priorityOrder`, hidden below 1200 px). — §4.2 of the prompt asks for `priorityOrder` among the sortable fields, and `DataTable` sorts from a column header: a sort with no column is a sort nobody can reach.
- 2026-09-16 — P22 — `PREVIEW_QUERY`, `publicUrlOf()`, `viewPathOf()` and `viewUrlOf()` moved from `property-form/usePropertyForm.js` to `pages/admin/properties/publicUrl.js`, which the form re-exports. — The list needs the same four addresses; importing them from the form would have pulled its reducer, validators and payload builder into the list's chunk for four one-line functions (§8.6).
- 2026-09-16 — P22 — `DataTable` gained `rowActionsMenu` and `rowActionsLabel`; the property row uses both, with short item labels inside the menu and the listing's title on the button that opens it. — Six actions inline is six 44 px icon buttons per row, and six menu items each repeating a 55-character Bengaluru listing title is a menu wider than a phone. The row is named once, where the menu opens.
- 2026-09-16 — P22 — A flag chip's visible label flips (Active / Inactive) and its `aria-pressed` follows the record, rather than a constant label with a state. — The chip is read at a glance far more often than it is pressed, and a column of chips all reading "Active" with only a colour between them is the thing a status column exists to avoid; the accessible name matches the visible one (WCAG 2.5.3).
- 2026-09-16 — P22 — `DataTable`'s footer reads "Page 5 of 2 — no rows on this page" when the address is past the end of the list, instead of "Showing 81–40 of 40". — `?page=5` is a real address, kept by a shared link after the rows behind it moved; §7 of the prompt asks that the reader be told where they are, and the empty state offers the way back.
- 2026-09-16 — D7/P23 — `PropertyLightbox.jsx` is a module of its own that imports `yet-another-react-lightbox`, its `captions` and `counter` plugins **and their three stylesheets**, and `PropertyGallery` reaches it through `React.lazy` behind a `Suspense` spinner. — D7 requires the lazy load because the library is ESM-only; putting the CSS imports inside the lazy module as well is what keeps the 8 kB of lightbox styles out of the first paint too. The build confirms the split: `yarl__` appears in one 36 kB JS chunk and one 8 kB CSS chunk and nowhere in `main`.
- 2026-09-16 — P23 — A `videoUrl` is offered as a tab only when it parses as a YouTube page, a Vimeo page or a file a browser can play (`.mp4`, `.webm`, `.ogv`); anything else is no tab at all. YouTube is embedded through `youtube-nocookie.com`. — §7 of the prompt asks for the hidden tab. A tab that opens on a blank iframe is worse than no tab, and the nocookie host is the one that does not set a tracking cookie before the visitor has pressed play.
- 2026-09-16 — D29/P23 — `viewTracker` keeps the counted ids in `sessionStorage` and `recordView(id)` returns whether the caller should send `POST /properties/:id/view`, rather than sending it itself. — D29 says once per property per session; a function that both remembers and posts cannot be tested without a network stub, and the page is the only thing that knows whether it is an admin preview (which counts as neither a view nor a recently-viewed entry).
- 2026-09-16 — P23 — EMI assumptions: `startingEmi(price, banks)` quotes the **lowest `interestRateMin` of the active lenders**, at **80 % loan to value** over **20 years**, and the card prints the three of them in the footnote. With no active lender, or a listing priced on request, it returns `null` and the line is absent. — §4.6 fixes the three figures. Returning `null` rather than `0` is what makes "no lender" and "no price" the same thing on screen — a missing line — instead of "₹0/month" (§6.6, BUG-05). When the card shows a **range**, the EMI is computed from the range's lower end, because "EMI from" naming the instalment on the upper end would be a figure nobody could get.
- 2026-09-16 — D57/P23 — `utils/finance.js` is written now with `estimateEmi`, `emiBreakdown`, `eligibleLoanAmount`, `foirScore` and `startingEmi`; prompt 25 deletes `FinanceGuide`'s copies of the arithmetic rather than moving it. — §3 of the prompt creates the file with `foirScore` in it. The FOIR reading follows D57 exactly — 60 % of the household income (the co-applicant's added in) less the existing obligations — and the four bands keep the boilerplate's thresholds, so prompt 25 is a deletion and not a re-derivation.
- 2026-09-16 — P23 — Share: the operating system's sheet where `navigator.share` exists, and otherwise a `menu`/`menuitem` popup offering Copy link · WhatsApp · X · Facebook · LinkedIn · E-mail, with arrow-key movement, `Escape` and focus returned to the button. A dismissed native sheet is silent; a failed clipboard write says to copy from the address bar. — The boilerplate branched on **viewport width**, so a desktop Safari user (which does have `navigator.share`) got a clipboard write and a phone without the API got nothing. Branching on the capability is the only test that answers the actual question, and the six targets are what a buyer forwards a listing with in India.
- 2026-09-16 — P23 — `ShortlistContext` stores ids in `localStorage` (`sna_shortlist`), not in the session, holds **ids only**, and listens for the `storage` event. — A shortlist is compared over days, so the session is the wrong lifetime; storing a copy of the record would mean a saved listing showing a price that has since changed, so the `/shortlist` page (26) reads the live records by id. The `storage` listener is what stops two open tabs overwriting each other's hearts.
- 2026-09-16 — P23 — `ShortlistProvider` is mounted in `routes/index.js`'s `AppShell`, not in `App.js` as §3 of the prompt says. — D97 moved every provider that uses a router hook inside the data router, and `ShortlistButton`'s toast links to `/shortlist`. The prompt predates that decision; the master context's "this file wins" rule applies.
- 2026-09-16 — P23 — The bottom navigation's fifth item changes from "Insights" to "Saved", with the shortlist count as a badge, instead of becoming a sixth item. — Five is the pattern's limit on both mobile platforms, and a sixth item overflows a 360 px viewport (the narrowest test width), which §8.1 forbids outright. Articles stay one tap away in the mobile header's drawer ("Real Estate Insights → Articles"); the shortlist had no other entry point on a phone at all.
- 2026-09-16 — D86/P23 — `BottomNav` returns `null` on `/properties/:slug` (matched with `matchPath`, not a string prefix), and `MobileCtaBar` takes its place. — The property page's bar is about _this_ listing — Call · WhatsApp · Enquire, the advisor's number when the editor published one — and two stacked fixed bars would take a third of a phone screen. This bar is also what makes D86 true: switching `sectionVisibility.enquiry` off hides the in-page block and leaves the listing perfectly contactable.
- 2026-09-16 — P23 — The page renders a `<section id="section-<key>">` wrapper for every visible section and puts `SectionPlaceholder` inside it, which renders **only outside production**. — Prompt 24's own context requires the wrappers (the section navigation scrolls to them and its `IntersectionObserver` observes them) and prompts 24/25 fill them. A dashed "arrives in prompt 24/25" card is useful to a developer and unacceptable to a visitor, so `process.env.NODE_ENV === 'production'` returns `null` and the wrapper stays empty until its content lands.
- 2026-09-16 — P23 — The scroll-spy's observation band starts **32 px below** where a clicked section lands (`SPY_GAP = SCROLL_GAP + 20`), and the strip itself sticks to `var(--header-height)` rather than to a pixel value measured in JS. — With the band starting at the landing position, the section _above_ was still clipping it by a pixel and won the "topmost visible" rule, so the chip lit up one section behind the click (reproduced, then fixed). Sticking to the token means a resize across the 900 px switch cannot leave the strip in the wrong place, which a number read once at mount would.
- 2026-09-16 — P23 — `NotFound` gained optional `title`, `subtitle` and `description` props and now sets `noindex, follow`. — §4.1 of the prompt asks the details page to render `NotFound` with the title "Property not found". Two `Helmet`s in one tree resolve by mount order, which is a fragile thing to depend on; a prop says what is meant. A 404 that search engines index is a defect in its own right.
- 2026-09-16 — P23 — `ToastProvider`'s live region is portalled to `document.body`. — It is `position: fixed` with `--z-toast`, and a fixed element inside a transformed ancestor is positioned against that ancestor rather than the viewport: `MainLayout` animates every page with framer-motion, and the property page now has a sticky column, so the region was one refactor away from landing in the wrong place. The portal also keeps it out of a test's render container, which is what `renderWith` needed before it could mount the provider for every component test.
- 2026-09-16 — P23 — `utils/format.js` gained `formatWhatsappNumber` / `whatsappLink`, built on `formatPhoneForTel`, and `SiteSettingsContext` uses them instead of its own stripper. — The seed stores a team member's WhatsApp as ten digits, and `wa.me/9876543210` is a dead link: stripping the punctuation is not enough, the country code has to be added the way a `tel:` target already does it. One helper means the property page, the agent card, the lead dialog and the settings context cannot disagree about what a number is.
- 2026-09-16 — ADD-09/P23 — `LeadForm` gained an additive `onSuccess(values)` callback. — `LeadModalTemp` has to remember the visitor's details in `sna_lead` and swap the form for its own success panel (with WhatsApp and Call), and prompt 25's scope names the same prop ("add `meta`/`hiddenFields`/`onSuccess` if 23 did not"). It changes nothing for the six existing call sites.
- 2026-09-16 — P23 — The "Verified" mark is printed once: the `isVerified` flag is skipped when the record's embedded badges already carry a `verified` badge. — The seed carries both, so the title block read "Verified Verified". The record's own flag stays the fallback for a listing whose editor has not attached the master-data badge.
- 2026-09-16 — P24 — Gated content unlocks **per kind** (`floorPlans`, `documents`) under `sna_lead.unlocks[propertyId]`, with one exception: a `property-enquiry` opens every kind on that listing. — Asking for a floor plan is not asking for the title deed, and a site that treats the two as one gate either over-collects (a second form for something already earned) or under-collects (the legal papers handed over for a drawing). The exception is not a convenience: somebody who has enquired about the listing has already told us who they are, and asking again before each file would be theatre. The session is the right lifetime — a new visit is a new conversation, and the lead, not the unlock, is the thing of value.
- 2026-09-16 — P24 — The Gallery section prints **every** photograph, cover included, although the gallery at the top of the page already shows the cover. — The section is the contact sheet ("here is everything we have") and its subtitle counts the photographs; a grid that silently skips the first one makes a visitor count tiles to work out why the numbers disagree, and the lightbox index would no longer match the counter the hero gallery uses. Duplication is the cheaper cost.
- 2026-09-16 — D39/P24 — `constructionSpecs` render inside the Specifications section as a second grouped block titled "Construction specifications", under the one `specifications` visibility key, and the block title appears only when both blocks exist. — D39 puts them in one section; printing "Project specifications" above a block that is the only block on the page is a heading that distinguishes nothing, and it would push the group headings from H3 to H4 for no reason, which §8.3's heading discipline forbids. `SpecBlock` therefore picks its group heading level from whether it has a title of its own.
- 2026-09-16 — P24 — `LocationSection.jsx` exports two components: the default `LocationSection` (map, address, locality guide) and a named `NearbySection` (the nearby places). — §6.1 keeps `nearby` and `location` as two `sectionVisibility` keys and the sub-navigation offers one chip per visible key, so one component covering both would either render twice or leave a chip scrolling to nothing. They share a file and a stylesheet because they are one idea — where this is — split by a toggle.
- 2026-09-16 — P24 — `useGatedContent` lives in `src/hooks/`, not in `src/components/sections/property/` where the prompt's file list implies. — §4.1 puts every hook in `src/hooks/` and §4.2 spells the convention out (`useThing.js`); the master context wins where a prompt and it disagree. Nothing else about the hook changes.
- 2026-09-16 — P24 — `propertySections.js`'s `overview.hasData` now reads "a description **or** a project-snapshot fact" instead of "a description **or** highlights". — Highlights have a section and a navigation chip of their own; a listing with highlights and nothing else was promised an Overview chip that scrolled to a section with nothing to print, which is the very disagreement between the rule and the page that BUG-06 was about. The snapshot facts are listed twice — once in the rule, once in `OverviewSection.snapshot()` — and both carry a comment saying so.
- 2026-09-16 — P24 — The `video` and `virtualTour` section keys keep the development-only `SectionPlaceholder` rather than gaining components of their own. — Their content is already on the page: `PropertyGallery` shows the walkthrough and the 360° tour as tabs beside the photographs, which is where a visitor looks for them. What is unresolved is the navigation — two chips that scroll to an empty wrapper — and that belongs with prompt 25, which deletes the placeholder and must decide whether the chips point at the gallery or the sections move down the page.
- 2026-09-16 — P24 — `NotFound` is imported at the head of `PropertyDetails`' component imports, out of the file's alphabetical order. — `mini-css-extract-plugin` refuses to emit a stylesheet whose modules two chunks disagree about the order of, and `BuilderDetail` imports `NotFound` before the developer sections that `BuilderSection` now pulls into the property page's chunk. The CI build failed on it ("Conflicting order"); a one-line comment at the import says why it may not be re-sorted.
- 2026-09-16 — P24 — The description's twelve-line clamp is a measured `max-height`, not `-webkit-line-clamp`, and "Read more" appears only when the text actually overflows. — A CMS description is several paragraphs, and line clamping across block children is not something every engine agrees about; `max-height` keeps the promise everywhere. Measuring `scrollHeight - clientHeight` on mount and on resize is what stops a two-sentence listing being given a button that does nothing, at the one width where it would matter.
- 2026-09-17 — D56/P25 — The whole eligibility questionnaire travels in `leads.meta` as a flat object (`score`, `band`, the eight answers, `propertyPrice` and, on a per-bank check, `bank`), and `message` carries one readable summary line. — `FinanceGuide` flattened every answer into a pipe-separated sentence in `message` and sent a second, undeclared `assessmentData` key that the API dropped: nothing the CRM could read back, filter on or count. §6.7's `meta` is the field for exactly this, and one line in `message` is what the leads table shows in its own column.
- 2026-09-17 — D57/P25 — `utils/finance.js` keeps the FOIR reading as the score and nothing else, and puts the four answers FOIR cannot use — credit score, years in the job, the down payment, the co-applicant — to work in `recommendations()` and in the eligibility breakdown instead. — The boilerplate collected six fields it then ignored, which is both a worse answer and a promise the form does not keep. Inventing weights for them would be inventing an underwriting model, which §14 forbids; naming what each one actually changes is honest and useful.
- 2026-09-17 — D57/P25 — A co-applicant's income is added to the household income only when `hasCoApplicant === 'yes'`; a bracket left behind after switching the toggle back to "No", and a "Yes" with no bracket chosen, both add nothing. — The record must not contradict itself: `meta.coApplicantIncome` is blanked on the same rule, so a lead never carries an income the visitor said not to count.
- 2026-09-17 — P25 — Completing either eligibility check (`financial-assessment`, `bank-eligibility`) unlocks every gated kind on the listing, as a `property-enquiry` already did. — The questionnaire asks for a name, a phone number, an income and a credit band; asking the same visitor for a name and a phone number again before the brochure would be theatre. `leadStorage.UNLOCKS_EVERYTHING` is the one list that decides this.
- 2026-09-17 — P25 — `leadStorage` announces every write on a `sna:lead-change` window event and `useGatedContent` listens for it. — `sessionStorage` fires no event in the tab that wrote it, so a section that read the gate when it mounted would still show a lock after an enquiry elsewhere on the same page had opened it. The event is the smallest thing that makes "one lead opens the whole listing" true on screen as well as in storage.
- 2026-09-17 — P25 — A gated file is opened with `window.open(url, '_blank', 'noopener,noreferrer')` from inside the lead form's own success handler, and when the call returns `null` a toast offers the same address as a link. The success panel offers an "Open <file>" button either way. — Opening inside the handler keeps the browser's user-gesture chain, so the tab is normally allowed through; a blocker that swallows it must not cost the visitor the download they have just paid for with their phone number. Under the dialog the toast is inert, so the panel's button is the path that always works.
- 2026-09-17 — P25 — The similar row is fetched by `PropertyDetails` and handed to `SimilarSection` as a prop, rather than fetched inside the section. — `getVisibleSections` decides the sub-navigation as well as the page, and the navigation may only offer "Similar properties" once the endpoint has answered with something. A section that fetched its own row could only report emptiness after the item had already appeared.
- 2026-09-17 — P25 — `propertySections.js`'s `similar.hasData` prefers `context.similarAvailable` and falls back to `similarPropertyIds` only when the caller leaves it out. — The page knows what the endpoint answered; the admin's visibility tab has made no such request and the editor's picks are the right signal there. Reading the picks on the page would promise a row when every pick had since been unpublished (BUG-07).
- 2026-09-17 — P25 — The `video` and `virtualTour` navigation items scroll to a 1 px anchor at the top of the gallery instead of to sections of their own. — Their content is the gallery's tabs (prompt 24 left this as the last open half of the two keys). An empty band with a navigation item pointing at it is the defect `getVisibleSections` exists to prevent, and giving `PropertyGallery` a controlled tab would rewrite a prompt-23 component for a scroll target.
- 2026-09-17 — P25 — A document whose `url` equals `brochureUrl` is dropped from the document rows. — §6.1 keeps `brochureUrl` beside `documents[]` and the seed fills both with the same file; an editor who attaches the brochure in both places means one download, not two rows offering it.
- 2026-09-17 — P25 — The finance sub-components share one `finance.module.css` rather than a stylesheet each, which is why that file is longer than 300 lines while every component in the folder is not. — The prompt's own file list names a single module, and the form, the per-bank dialog and the calculator share the chip rows, the sliders, the fieldsets and the result cards; a stylesheet each would duplicate all of it.
- 2026-09-17 — P25 — `assessmentLead.js` holds `emptyAnswers`, `validateAnswers`, `summaryLine` and the `POST /leads` body; `AssessmentForm.jsx` holds only the screen. — The prompt caps each finance file at 300 lines and the form is the largest of them; the split also lets the lead contract of §6.7 be tested without rendering anything.
- 2026-09-17 — P25 — "Recently viewed" is a section of the property page with no `sectionVisibility` key and no navigation item. — It belongs to the visitor's browser rather than to the listing, so there is nothing for an editor to switch and nothing the sub-navigation could honestly promise before the strip has read `sna_recent_properties`.
- 2026-09-17 — D94/P26 — One `ListingEngine` serves every listing URL, the locality tabs and the builder profile; a route contributes nothing but its fixed filters and its words (`components/listing/listingRoutes.js`). — The boilerplate had five hand-written category pages with five copies of the same filter, sort and pagination code, and each copy drifted; the engine is written once and the route table is data.
- 2026-09-17 — D94/P26 — Filtering, sorting, paging and counting are all `GET /properties`; the only thing computed in the browser is how the facets are drawn. — BUG-19: the old page asked for `perPage=100` and then narrowed, sorted and paginated the answer, so page 9 of a search was a page the visitor's own machine had invented.
- 2026-09-17 — P26 — The URL parameters are the contract's own names (`localityId`, `minPrice`, `constructionStatus`, …), with csv arrays, and a value equal to its default never appears. — A shared listing link is then a request the API could answer as it stands, and `?sort=relevance&page=1` never accumulates in an address.
- 2026-09-17 — P26 — The desktop rail applies live, 300 ms after the last click, and keeps a Reset but has no Apply; the mobile sheet keeps both Apply ("Show N results") and Reset. — On desktop the results _are_ the feedback, so an Apply button is a second click for nothing; behind a sheet the results are invisible, so the sheet has to say what the draft would return and be dismissed deliberately (ADD-11: the boilerplate's panel differed between the two for no reason).
- 2026-09-17 — P26 — The rail commits its draft only when the visitor touched it (a `touched` ref), never when the params changed from outside. — Removing a chip changes the params while the rail still holds the old draft; without the flag the next commit would put the filter straight back.
- 2026-09-17 — D25/P26 — `/buy/:slug`, `/rent/:slug` and `/commercial/:slug` resolve the four construction-status slugs first, then master data's property-type slugs, then 404; while master data is still loading the page waits rather than flashing a 404. — Keeps `/buy/ready-to-move` a status page and `/rent/apartments` a type page without either shadowing the other, and a valid URL never renders as a missing one.
- 2026-09-17 — P26 — An embedded engine (a locality's Buy/Rent tabs, a builder's projects) keeps its filters in memory and publishes only its page number, as `?p=`. — The page it sits inside owns `?page=` and the rest of the query string; a second engine writing into the same address would fight it, but the second page of a builder's portfolio still has to be a shareable link.
- 2026-09-17 — P26 — A route's or an embed's filters are **fixed**: merged into every request, absent from the URL, rendered as a locked chip, and their whole group is hidden in the rail. — `/rent` is rent and `/localities/whitefield` is Whitefield; a control that cannot change anything is worse than no control.
- 2026-09-17 — D90/P26 — The budget group switches to `PRICE_BUCKETS_RENT` whenever the effective listing type is `rent` or `lease`, and changing the listing type clears `minPrice`/`maxPrice`. — The two bucket sets are two orders of magnitude apart, so carrying "₹1 Cr – ₹1.5 Cr" into a rent search would return nothing at all.
- 2026-09-17 — P26 — A facet count is shown when `meta.facets` names the option and nothing is shown when it does not; a zero is never printed. — The facets are computed after the other filters, so an option they omit may well come back when another filter is removed — printing "0" would claim it is dead.
- 2026-09-17 — P26 — The rail keeps a **Builder** group (`developerId`, single choice) although the prompt's group list omits it. — §12 of the prompt forbids losing any legacy filter, and the boilerplate's panel filtered by developer.
- 2026-09-17 — P26 — `listingSeo.js` is a pure function returning `{ title, description, h1, canonicalPath, canonicalUrl, noindex, intro, pagination }`; the page renders it through a temporary `<Helmet>`. — Prompt 38 hands the same object to `<Seo type="listing">`, so the §9.4 rules are written and unit-tested once.
- 2026-09-17 — P26 — The `<h1>` is rebuilt from the effective filters rather than taken from the route, and a route contributes a `noun` (and sometimes a `verb`) rather than a finished sentence. — Narrowing a search renames the page — "3 BHK Villas for sale in Whitefield, Bengaluru" — and a status page still reads "Ready-to-move homes in Bengaluru" rather than "…for sale".
- 2026-09-17 — P26 — The listing meta description is generated from the result set ("Browse 24 apartments for sale in Whitefield, Bengaluru…") rather than from a template variable. — §9.5 defines a title template for listings and no description template; the generated sentence is specific, and `seoSettings.defaults.metaDescription` stays the fallback for pages that have no subject.
- 2026-09-17 — P26 — `%count%` takes its noun down to the singular ("1 Listing"), and a count that has not arrived removes the whole "– %count% Listings" phrase. — The template is client-editable and written in the plural; neither "1 Listings" nor a dangling "– Listings" should ever reach a browser tab.
- 2026-09-17 — P26 — `GlobalSearch` is one component for the hero, the header dialog and the listing's results header, with `sna_recent_searches` (max 5) shown while the box is empty. — The hero used to own three hundred lines of type-ahead nobody else could use, so the header had no search at all and `NotFound` sent `?search=`, a parameter the listing has never read (BUG-10).
- 2026-09-17 — P26 — A property-type suggestion goes to `/rent/<slug>` while the visitor is under `/rent` and to `/buy/<slug>` otherwise. — Somebody searching "villas" from a rental page means rentals; the old dropdown linked `?propertyTypeSlug=`, which no endpoint and no page reads.
- 2026-09-17 — P26 — `/shortlist` reads `?ids=` when it is present and the visitor's own `sna_shortlist` otherwise, and offers "Save all" on a shared list. — A shortlist has to be shareable to somebody who has never been to the site, and the receiving browser must be able to keep it without retyping anything.
- 2026-09-17 — P26 — The shortlist page fetches the live records by id rather than rendering what the browser stored. — Ids are all that is stored, so a price cannot go stale and a listing that has been taken down simply stops appearing; the page says how many saved listings are no longer listed.
- 2026-09-17 — P26 — `ShortlistContext` reads the saved ids through a ref that `write()` updates synchronously. — Two hearts pressed in the same tick both read the render's copy of the list under React 18 batching, and the second write silently dropped the first.
- 2026-09-17 — P26 — The locality's embedded listing is headed "Listings in Whitefield" rather than "Properties in Whitefield". — The page's own `<h1>` already says "Properties in Whitefield", and a heading that repeats the one above it reads as a rendering fault.
- 2026-09-17 — D92/P26 — `QuickActions` is six tiles linking to `/buy`, `/rent`, `/lease`, `/commercial`, `/plots` and `/buy/ready-to-move`. — The three `?type=` tiles pointed at a parameter the listing never read, and `type=lease` in particular returned an empty search (BUG-10).
- 2026-09-17 — P26 — The bottom navigation's Search goes to `/properties?filters=open`, and the engine consumes the parameter on arrival (`replace: true`). — On a phone "Search" means "narrow this down", and the parameter must not travel on in a shared link.
- 2026-09-17 — D68/P26 — The bedroom chips send `BEDROOM_OPTIONS` values unchanged (`5` is the "5+ BHK" chip) and the engine does no bedroom logic of its own. — The ≥ 5 rule and the match against a project's active `unitConfigurations` belong to the API; the chip highlights from the same value the facet counts, so what the chip says and what the list returns cannot disagree.
- 2026-09-17 — P27 — `GET /pages?showInHeader=&showInFooter=` is a new public endpoint returning `{slug,title,headerMenu,footerColumn,order}` for published pages only, unpaginated unless asked otherwise. — The header and the footer are built from data, and an editor who adds a service page decides where it appears by ticking a box; the existing `GET /pages/slug/:slug` answers one page with all of its blocks, which is the wrong request to make four times for a menu. A menu arrives whole, so the default is every matching row rather than the first twelve.
- 2026-09-17 — P27 — `useNavPages()` caches the two navigation lists in module memory for the page load and shares one request between `Header`, `MobileDrawer`, `Footer` and the bottom bar's drawer. — Four components on every public page need the same answer; the alternative was prop-drilling through `MainLayout` or four requests per navigation (the same reasoning as D93 for settings and master data).
- 2026-09-17 — D81/P27 — "Why choose us" and "How it works" are `HomeFeatures` and `HomeSteps`, rendering the `features` and `steps` blocks of the `home` CMS page, and they render **nothing** when the page is unpublished or the block is missing. — There is no hardcoded fallback copy by design: a fallback would mean the site quietly ignores what an editor did, and the whole point of D81 is that the words are theirs (BUG-11).
- 2026-09-17 — P27 — The home page's sections hide themselves rather than showing empty states: a row below three results, a tile whose count failed, a section whose collection is empty. — On a landing page an empty band reads as a fault; a visitor cannot miss a section that was never announced.
- 2026-09-17 — P27 — `useCategoryCounts` asks `GET /properties?perPage=1` per filter set and caches `meta.total` in memory for five minutes, sharing in-flight promises between the tiles and the property-type grid. — There is no count endpoint and there needs to be none: `meta.total` already answers the question for exactly those filters, at the cost of one row of JSON. A failed count renders as no count at all — never as "0", which would claim a category is dead when the request simply did not arrive.
- 2026-09-17 — D90/P27 — The hero's budget select follows the tab, and switching tabs always clears the chosen budget. — The sale and rent scales are two orders of magnitude apart; carrying "₹1 Cr – ₹1.5 Cr" into a rent search would return nothing at all. The same rule applies to the requirement modal's budget, whose options follow its listing-type select and whose value is dropped when the new options do not offer it.
- 2026-09-17 — P27 — A locality chosen from the hero's type-ahead becomes `localityId`; free text becomes `q`. `GlobalSearch` gained an `onSelect` hook that a host may consume (returning `true`) instead of navigating, and an `inline` variant that renders no `<form>` of its own. — A form inside a form is invalid markup, and "Whitefield" as a filter returns the locality's listings while "Whitefield" as a search term returns whatever mentions the word.
- 2026-09-17 — P27 — The hero search sends the `search` analytics event and navigates; it never writes a lead. The `hero-search` value of `LEAD_SOURCES` stays unused. — Searching is not an enquiry, and a lead nobody chose to send is a lead nobody will answer.
- 2026-09-17 — P27 — The Buy menu's "By budget" column folds the nine `PRICE_BUCKETS_SALE` buckets into five **contiguous** bands at four of the enum's own boundaries (`BUDGET_BOUNDARIES`). — Nine entries is too many for a menu and any five of them would leave gaps a visitor falls into ("₹60 L? not on this menu"); folding rather than listing also means a boundary the enum stops offering merges two bands instead of opening a hole.
- 2026-09-17 — P27 — Between 900 px and 1199 px the header keeps its first five menus and folds the rest into a trailing "More" panel (`collapseMenus`), each folded menu becoming a column. — Ten labels plus the mark and the buttons need about 1200 px; below that they do not shrink, they overlap. Nothing is lost and the bar stays one row tall, so `--header-height` remains the layout's single offset.
- 2026-09-17 — P27 — The header's call and WhatsApp buttons are icons with a `title` and an `aria-label` at every desktop width; only the CTA keeps its words. — The same room problem: their labels cost about 90 px that the menus need, and a telephone icon linking to `tel:` is not ambiguous.
- 2026-09-17 — D52/P27 — The home page's `<main>` drops the layout's top padding and the hero reserves `--header-height` itself, so the transparent bar sits over the photograph rather than over blank background. — A header that is transparent over nothing is just an invisible header.
- 2026-09-17 — D52/P27 — Over the hero the bar carries a top-down black gradient and its labels are white with a text shadow; the wordmark sits in a white plate. — §7 asks for readability over dark _and_ light hero images, and D3/§2.2 forbid placing the logo straight on a dark surface.
- 2026-09-17 — P27 — The footer draws the editor's `settings.footer.columns` first and tops the row up with the generated ones (Buy by type, Popular localities, Insights, Company) to a limit of five (`FOOTER_COLUMN_LIMIT`). — §6 asks for four or five columns; with the seed's two configured columns all four generated ones would make seven. An editor who has already filled the footer is never overruled, and a site whose settings carry no columns still has a usable one.
- 2026-09-17 — P27 — The footer's "Privacy · Terms · Disclaimer" line is built from the published pages that actually carry those slugs. — A legal page nobody has published yet is not linked at all, rather than being a dead link in every footer of the site. (`PATHS.terms` still spells `/terms-and-conditions` while the seeded page is `terms-of-use`; the footer follows the page, and prompt 30's CMS route is what will resolve all three.)
- 2026-09-17 — P27 — `PATHS.page(slug)` encodes each segment separately, so a nested page slug (`buyer-assistance/home-loan`) keeps its slashes. — It encoded the whole slug, which turned every nested CMS page into `%2F` and would have made the Buyer Assistance menu unreachable.
- 2026-09-17 — D82/P27 — The "Post Requirement" CTA opens `LeadModalTemp` with `requirement`, which adds the six selects of D82; `LeadForm` gained `group` (nest this answer under `requirement`), `toBody` (one budget select fills `budgetMin` and `budgetMax`) and function `options` (choices that depend on the other answers). — The requirement is a nested object in §6.7 and an unknown top-level field is dropped silently by the API, so the shape had to be built before prompt 28 replaces the dialog with `LeadCaptureModal`.
- 2026-09-17 — P27 — Each host of the requirement modal (`Header`, `MobileDrawer`, `BottomNav`) mounts its own instance rather than sharing one through a context. — Only one can be open at a time (the header and the drawer are mutually exclusive by width) and `LeadCaptureContext` is prompt 28's to write; a context added here would be rewritten there.
- 2026-09-17 — P27 — `ui/Drawer` gained `padded={false}`, which hands the body to the caller edge to edge and makes it a flex column. — The navigation drawer's own rows carry the padding and its footer block sticks to the bottom; the alternative was the layout drawer not using the kit's drawer at all.
- 2026-09-17 — P27 — "Trending topics" is replaced by "Latest insights" (`GET /articles?perPage=3&sort=newest`). — Ranking the home strip by view count showed the same three pieces for months; the newest three are what a returning visitor has not read.
- 2026-09-17 — P27 — The CTA band links to `/sell-let` rather than opening a lead modal. — Telling us about a property takes more than a name and a phone number, and that page's `leadForm` block already has the fields.
- 2026-09-17 — D43/P28 — The throttle is one submit per ten seconds **per form instance**, started when the POST begins and shown as a disabled button ("Please wait 7s") plus an `aria-live` countdown under it. — D43 asks for one submit per 10 s; tying it to the instance rather than to the source keeps an honest visitor who enquires about two different listings from being told to wait, while a double-click, a retry after a server error and a reopened success panel all hit it. The mock's own per-IP limit (10/min, §5.11) is the second line, and its 429 is surfaced as "Too many requests, please wait a minute" in a toast and under the button.
- 2026-09-17 — D82/P28 — The six requirement selects of D82 live in `RequirementFields`, are declared once in `utils/leadSources.js` with `group: 'requirement'`, and are rendered inside a `<fieldset>` with the legend "What are you looking for?". — A screen reader announces the legend before each select, so "Budget" is heard as part of a requirement rather than as a stray box under a phone number; the `group` marker is what nests every answer under `lead.requirement` (§6.7) instead of having it dropped as an unknown top-level key.
- 2026-09-17 — P28 — **A repeat gated action in the same session files no second lead.** `LeadCaptureModal` skips the form — and posts nothing — when `leadStorage` holds a phone number **and** either a capture for that listing or an open gate of the kind being asked for; it delivers the file and says "We have your details — opening <file>". A form filled in about a _different_ listing never skips. — The lead is the thing of value and the first capture per listing already carries it; a second, third and fourth identical record for the same visitor reading four papers of one project is noise the sales desk has to de-duplicate, and asking again for details the page already has is theatre. The record of what was downloaded lives in the analytics events (`brochure_download`, `document_download`), not in a duplicate lead.
- 2026-09-17 — P28 — **An identified visitor is one `sna_lead` holds a name and a phone number for, and who is not signed in to the admin panel.** `WhatsAppButton` and `CallButton` file a `whatsapp-click` / `call-click` lead only for such a visitor; everyone else's click is a `dataLayer` event and nothing more. The post is fire-and-forget — started in the click handler, never awaited, its rejection swallowed. — `POST /leads` requires a name and a number, so an anonymous click has no lead to file; a staff member browsing the public site is not a prospect and must not appear in their own pipeline (`sna_auth_token` is the test); and nothing may delay or fail the `tel:`/`wa.me` link the visitor actually clicked.
- 2026-09-17 — D56/P28 — A field whose `name` is not one of the four the API stores at the top level (`name`, `phone`, `email`, `message`) and which does not declare `group: 'requirement'` is filed under `lead.meta`. — The mock's `sanitize()` drops unknown keys, so the boilerplate's `monthlyIncome`, `workspaceType`, `serviceType` and `askingPrice` were being posted and silently discarded; `meta` is exactly the free-form, source-specific payload D56 reserves for them, and prompt 29's lead detail renders it as a key/value card.
- 2026-09-17 — P28 — `utils/leadSources.js` `ENTRY_POINTS` is the only place in `src/` that writes a lead source. A component names an **entry point** (`openLeadModal({ entry: 'site-visit-request' })`, `leadFormProps('home-loan')`) and the table answers with the canonical value, the heading and the boxes. — BUG-09 existed because every page chose its own spelling; one table makes `home_loan` unreachable rather than merely discouraged, and `leadSources.test.js` asserts that every entry's source is a `LEAD_SOURCES` value and that every canonical value but the CRM's `other` has an entry.
- 2026-09-17 — P28 — The campaign is captured once per visit into `sna_utm` (sessionStorage) by `LeadCaptureProvider` on mount, and the **first** landing wins. — A visitor lands on `/whitefield?utm_campaign=…`, reads three pages and fills a form in on the fourth, by which time the parameters are long gone; a later page that happens to carry its own must not overwrite the campaign the visit actually started with.
- 2026-09-17 — P28 — `LeadCaptureContext` also carries `setPageContext({ propertyId, propertyTitle })`, which the property page sets on mount and clears on unmount. — The floating WhatsApp button lives in `MainLayout` and cannot know which listing is below it; §6 asks its message to name the property, and the alternative — a second floating button mounted by the details page — would leave two circles on screen during the route transition.
- 2026-09-17 — P28 — The gated entry points (`brochure-download`, `floor-plan-request`, `document-request`) ask for a name, a number and an e-mail address only; what was requested travels as a `hiddenFields.message`. — A download gate competes with the file itself, so every box costs conversions; the context still reaches the sales desk, and a hidden field cannot overwrite something the visitor typed because there is no box to type in.
- 2026-09-17 — P28 — After a gated submit the "Open <file>" button in the success panel is a real `<a href>` rather than a second `window.open`. — The dialog has already tried the tab from inside the click; a blocker that swallowed that one would swallow another, and a link is the one thing no blocker intercepts (BUG-08).
- 2026-09-17 — P28 — The newsletter is the one entry point that is not a lead: `NewsletterForm` posts to `POST /newsletter/subscribe` with the same honeypot and throttle every lead form carries, and reads the API's "Already subscribed" back as the success "You're already subscribed". — §6.14 stores subscribers in their own collection, and a duplicate address is a success from the visitor's point of view, not a failure (BUG-15).
- 2026-09-17 — D91/P28 — The Careers "Apply" buttons open the shared dialog with the `careers` entry and the role in `meta.position`; the boilerplate's hand-rolled modal and its dead file input are gone. — D91 keeps a careers lead while job applications stay separate records; the résumé upload and the `jobApplications` write belong to prompt 31, and a file input with no `name` and no `onChange` (ADD-18) promised something the page never delivered.
- 2026-09-17 — P28 — `validators.js` now accepts Indian mobile numbers only (ten digits starting 6–9, `+91` and punctuation tolerated) and normalises every number to `+91XXXXXXXXXX` before it is posted. — The boilerplate accepted any 10–15 digit string, which the mock's `phone` type then rejected with a 422 the visitor could not act on; one stored shape is also what lets the CRM see that `+91 98765 43210` and `09876543210` are one person.
- 2026-09-17 — D46/P29 — The lead CSV is `GET /admin/leads/export` fetched with `downloadAuthenticated()` and saved from a blob, not built in the browser. — A bearer-protected file cannot be reached by an `<a href>`, and the server already writes the BOM, the property title and the quoted newlines the hand-rolled export got wrong (NEW-22). The request repeats the filters and the sort that are on screen and carries no `perPage`: the endpoint writes every matching row by definition.
- 2026-09-17 — D55/P29 — `LeadNotificationsContext` gained `refreshKey`, a counter bumped only when a poll's answer differs from the last one (total, newest id, newest `updatedAt`); `LeadsListPage` refetches on it rather than on `lastUpdatedAt`. — `lastUpdatedAt` changes every thirty seconds whether or not anything happened, so a table keyed on it would reload eight times an hour for nothing; keyed on the signature, a quiet desk costs one request per poll and a new lead still reloads the list without touching the scroll position.
- 2026-09-17 — D56/P29 — `LeadMetaCard` renders `lead.meta` generically: `score` gets a band chip, `bankName` a line of its own, and every other key becomes a humanised label (`monthlyIncome` → "Monthly income") with nested objects and arrays flattened to one readable line. — `meta` is free-form by definition, so a card that knew the financial assessment's keys would render the next calculator's payload as nothing at all.
- 2026-09-17 — D89/P29 — A sales user claims; an admin or a manager assigns. The list shows "Claim" in the Assigned cell of an unassigned row, the detail shows it under "Assigned to", and neither role sees the other's control. — The two are different permissions in §7, and a 409 ("This lead is already assigned") is the answer when two people reach for the same lead at once.
- 2026-09-17 — P29 — **The duplicate window is 30 days, measured between the two leads rather than from today.** `isPossibleDuplicate` is computed on every admin read from a phone-keyed index over the whole collection, including leads outside the caller's scope. — Measuring from "now" would make a pair's answer change silently as the calendar moved on; computing it over all leads is what lets a sales user see that the caller they are about to ring has already spoken to a colleague they cannot see (D15 governs the records, not the boolean). A number the Indian mobile rule does not recognise — a landline, a number from abroad — is compared on its digits rather than dropped.
- 2026-09-17 — P29 — **Moving a lead backwards through the pipeline asks for confirmation; moving it forwards does not.** Reopening a lost lead counts as backwards. — Forward is what the screen is for and must cost one press; a mis-click that demotes a lead from Negotiation to Contacted rewrites the history of a deal, and the confirmation is one press against that. "Lost" is not a rung at all: it leaves the funnel and asks for a reason of at least three characters first.
- 2026-09-17 — P29 — Reading the staff directory (`GET /admin/users`, list and detail) is `users.list`, held by admin **and** manager; every write on `/admin/users` and the `/admin/settings/users` screen stay `users.view`/`'*'` — admin only. — §7 gives `leads.assign` to managers, and naming a colleague means reading the list of them; the alternative was a manager who may assign but cannot see anybody to assign to. The mock enforces it through a `READ_ACTIONS` entry in `lib/routePermissions.js` and the registry through `readAuth: 'manager'`.
- 2026-09-17 — P29 — `FilterBar` gained a `custom` field type whose control the caller renders (`render({ values, onChange })`), used for the lead list's property filter. — A catalogue of listings is searched, not scrolled, so the filter is an `EntityPicker`; as a field rather than a `children` slot it sits in the row with the other filters on a laptop and moves into the "More filters" popover with them below 900 px.
- 2026-09-17 — P29 — The follow-up date and time are read and written in **IST** (`Asia/Kolkata`, a fixed +05:30), not in the browser's timezone. — D22 prints every absolute date in IST, and a laptop set to UTC would otherwise show "16 Sep, 10:30 am" above two inputs reading 09/16 and 05:00.
- 2026-09-17 — P29 — The four dashboard charts are `React.lazy` chunks around `recharts@3.10.1`; each reads its colours from the design tokens through `useCssVar` and publishes its series as a visually hidden table beside the SVG. — `recharts` is the heaviest thing the admin bundle would carry and it is needed on one screen (it lands in a 396 KB chunk of its own, and the main bundle contains none of it); a chart is a picture of a table, and the table is what a screen reader, a page search and anyone with images off actually gets.
- 2026-09-17 — P29 — The only comparison on the dashboard is `leadsThisMonth` against `leadsLastMonth`, and it is omitted entirely when last month was zero. — BUG-11's "+12 %" badges were string literals; a percentage off a base of zero is the same decoration by another route.
- 2026-09-17 — D11/P30 — The CMS catch-all route is `/:slug/*`, not `*`, and it is registered after every static route. — React Router ranks by specificity, not declaration order: a bare `*` would tie with the 404 route `routes/index.js` registers after it, while `/:slug/*` outranks that and is outranked by every static path, which is exactly the precedence D11 asks for.
- 2026-09-17 — D11/P30 — Twelve first segments are reserved and can never be a page slug: `properties, buy, rent, lease, commercial, plots, localities, builders, insights, careers, shortlist, admin`. `CmsPage` refuses one that reaches it through the catch-all without calling the API, and `PageFormPage` refuses one on save ("Reserved path"). The list lives in `src/routes/paths.js` because both files import it and `publicRoutes.js` lazily imports `CmsPage` — a constant with no imports of its own is the one place that cannot become a cycle. — A page under a reserved prefix would be shadowed by the static route that owns it: it would exist, and never be reachable.
- 2026-09-17 — D11/P30 — A page that already lives under a reserved prefix keeps its slug through an edit; only a **change** to a reserved path is refused. — `insights/real-estate-awareness` is a seeded page (§6.10) whose route is spelled out, so it is not shadowed; refusing to save it would make the record uneditable.
- 2026-09-17 — P30 — **A CMS page's slug is a URL path.** `src/services/schemas/page.js` types `slug` and its mirrored `seo.slug` as `string` with the pattern `^[a-z0-9]+(?:-[a-z0-9]+)*(?:/[a-z0-9]+(?:-[a-z0-9]+)*)*$` (≤ 120 chars) rather than as `slug`, whose `[a-z0-9-]+` refuses the separator. `mock-server/lib/slug.js` gained `slugifyPath()` and `makeCrudRouter` a `pathSlug` option, which the pages router sets. — §6.10 seeds `buyer-assistance/home-loan` and `scripts/validate-seed.js` has allowed it since prompt 10, but nothing had ever written a page **through the API**: a `PUT` of the seeded record answered 422, and `slugify` would have turned the separator into a hyphen. Prompt 30 is the first to save a page, so it is where the contract had to catch up with the seed.
- 2026-09-17 — P30 — `scripts/smoke-api.js` samples a patterned `string` descriptor as a slug rather than as a sentence. — "Smoke slug 3" does not match a path-slug pattern; the only two patterned strings in the contract are a page's slug and a redirect's `fromPath`, and a slug satisfies both.
- 2026-09-17 — P30 — `GET /faqs`, `/testimonials` and `/team` accept `ids` (already generic in the mock's `applyFilters`, now declared in the registry and in `docs/API_CONTRACT.md`). — The `faq`, `testimonials` and `team` blocks store the ids an editor picked and want them back **in that order**, which is what §5.7's `ids` filter answers; fetching everything and filtering in the browser would be BUG-19 by another route.
- 2026-09-17 — D27/P30 — `blockSchemas.js` holds the **editing** half of a block type (a sentence, a picker group, its fields) while `config/enums.js` keeps the vocabulary (value, label, icon, `defaultData`), because the mock validates against the enum. `BlockForm` renders any block from that schema alone. — Twenty-five block types cost one switch statement rather than twenty-five form components, and adding one is two entries rather than a new file.
- 2026-09-17 — D27/P30 — A block type the running build does not know is **kept**, not dropped: the renderer skips it (with a development-only `console.warn`) and the editor shows it as "Unsupported block" with a delete button and no other control. — The data is on the server; a browser that does not understand a block is not a reason to silently delete it on the next save.
- 2026-09-17 — P30 — Block backgrounds alternate white/grey, computed **after** the empty blocks have been dropped; `hero` and `cta` are full-bleed bands of their own and do not take a turn. A block component may declare a static `isEmpty(data)` so the renderer can ask before mounting it. — §6.10 renders a `stats` block only when it has figures and §14 seeds every one of them empty, so "empty" has to cost neither a gap nor the next block's band.
- 2026-09-17 — D81/P30 — The home page's "Why choose us" and "How it works" are `components/cms/blocks/FeaturesBlock` and `StepsBlock` — the very components every CMS page uses for those two block types. `HomeFeatures.jsx` and `HomeSteps.jsx` are deleted. — One markup for one block type, wherever it is rendered; the home page and `/about` cannot drift apart.
- 2026-09-17 — D28/P30 — "Save & preview" saves first, then asks for a token and opens `/<slug>?preview=<token>` in a new tab. The banner is shown to anyone holding a token; the "Back to the form" link only to a signed-in session. — A preview of unsaved work is a lie about what the page says, and the token is the permission — a link forwarded to a client should open the page, not a link into the admin panel.
- 2026-09-17 — D83/P30 — The `contactInfo` block renders nothing it was not given: the phone, e-mail, WhatsApp, address, working hours and social links all come from `siteSettings`, each part is omitted when empty, and the whole band stands down when all of them are. — ADD-17's five `#` social links and hardcoded Brigade Road address are what a block with its own copy looks like.
- 2026-09-17 — P30 — Blocks an editor adds carry a `tmp-…` id in the editor and are given the next free integer in the page's own array when the payload is built. — §5.5 has the server assign ids, and it does renumber `order`, but `blocks[].id` is a required integer: a `tmp-…` string would fail validation in the browser before the request was ever made.
- 2026-09-17 — P30 — `SlugField` gained a `path` mode (each segment slugified, `/` kept) and stands its availability line down while the field carries an error. — A slug the API would accept can still be one this form refuses, and "This URL is available" above "Reserved path" reads as a contradiction rather than as two facts.
- 2026-09-17 — P30 — `.prose` lives in `src/assets/styles/prose.css`, imported once by `PageRenderer`, and is the typography of every piece of CMS-authored markup (rich text, custom HTML, an expandable card's detail). — `global.css` owns the tokens (a guardrail of this prompt); prose is a component's stylesheet that happens to have no class names to hook onto.
- 2026-09-17 — D12/P31 — `utils/cloudinary.js` reads the cloud name and the unsigned preset from `siteSettings.integrations` first and from `REACT_APP_CLOUDINARY_*` second, and `isCloudinaryConfigured()` answers `false` unless **both** halves are present. — The environment is the developer's default and the settings are the client's answer, so the client's answer wins without a rebuild; and a cloud name without a preset cannot upload, so half a configuration has to render the URL box rather than an upload zone that fails on use.
- 2026-09-17 — D12/P31 — The upload is `XMLHttpRequest`, not `fetch`, and takes an `AbortSignal`. — `xhr.upload` reports how far a send has got and `fetch` does not; a résumé on a phone connection needs a progress bar rather than a spinner that might mean anything, and the signal is the Cancel button beside it.
- 2026-09-17 — P31 — `cloudinaryUrl()` returns a URL that **already carries a transformation** unchanged rather than adding a second segment in front of it. A segment counts as a transformation when every comma-separated part starts with one of Cloudinary's own parameter prefixes, which is what tells `w_640,c_fill` from a public id like `my_folder`. — The brand favicons of §2.3 are deliberate crops; a chained `f_auto,q_auto,w_640/` in front of `w_16,h_16,c_pad` would resize to 640 and then pad to 16, which is not what either transformation was asked for.
- 2026-09-17 — D91/P31 — A job application fires its own analytics event (`job_application`), never `lead_submit`. — An application is its own record and not a lead (D91); counting it as a lead would inflate the pipeline in GA4 against a `careers` source that no lead was written under.
- 2026-09-17 — P31 — **There is no bulk action on job applications.** Every write on `/admin/jobs/applications` is per row. — §5.14 gives applications a list, a `PATCH` and a `DELETE` and nothing else; a client-side loop pretending to be a bulk action fails halfway through and leaves the desk guessing which half, which is the mistake prompt 22 took out of the property list.
- 2026-09-17 — P31 — **An application is never de-duplicated.** The same address applying twice for the same role within a minute stores two records. — §6.11 has no uniqueness rule and neither does the API; a second application is usually a corrected one, and the hiring desk can see both and decide, which a silent refusal would not allow.
- 2026-09-17 — P31 — A closed opening stays readable at its own URL: `GET /jobs/slug/:slug` answers an inactive-by-date role with `isOpen: false` and the page draws a notice, `noindex, follow` and a link back to `/careers`, while `POST /jobs/:id/apply` refuses it with a 404. — Somebody following a three-week-old link from a job board should see what the job was and that it has gone, not a dead end; and the page must not keep taking applications the API will refuse.
- 2026-09-17 — P31 — `ResumeUpload` offers the URL box again after a failed upload ("Send a link instead"), beside "Try again". — The alternative is telling somebody whose file will not go through to come back later, and a Drive link is a résumé the desk can open just as well (D12).
- 2026-09-17 — P31 — `MasterDataForm` gained a `list` field type: a reorderable list of short strings, drawn with `SortableList`. — A job's `responsibilities` and `requirements` are `string[]` in §6.11, and the alternative was a third hand-written repeater after the locality's highlights and the developer's; a field type is what the rest of the kit already is.
- 2026-09-17 — P31 — `DataTable` gained `onRowClick`, which opens a row in place when the record has no page of its own. — An application is read in a drawer rather than at a route, and the alternative was a table where only the kebab opened the row; `rowLink` still wins when a row does have a URL.
- 2026-09-17 — P31 — `/careers` and `/insights/real-estate-awareness` are spelled-out CMS routes rather than catch-all ones, because `careers` and `insights` are reserved first segments (D11). The `slug` prop is what tells `CmsPage` the route meant it. — A page under a reserved prefix is refused by the catch-all by design; naming the route is how a seeded page under one stays reachable.
- 2026-09-17 — P31 — The phone placeholder in `utils/leadSources.js` and in the block editor's starter fields is `98XXX XXXXX` rather than a number-shaped string. — §14 spells placeholders that way for exactly this reason: nothing user-visible should read as somebody's real number, and the hint is just as clear.
- 2026-09-17 — D8/P32 — `Placeholder` and `CharacterCount` are imported as named exports from `@tiptap/extensions` (`import { CharacterCount, Placeholder } from '@tiptap/extensions'`), confirmed against the installed package's type definitions. — D8 chose the package; this records the exact import the v3 build actually offers, which is what the prompt asked to be pinned down.
- 2026-09-17 — P32 — **Code blocks are off** (`StarterKit.configure({ codeBlock: false })`); inline `<code>` stays. `Link` and `Underline` are configured from their own packages with `link: false, underline: false` on the kit rather than through the kit's copies. — Real-estate content quotes a clause number or a RERA id, never a program, and `<pre>` is a layout an editor cannot lay out; the second half puts the link's `openOnClick`, `autolink` and `rel` where somebody looking for them will find them.
- 2026-09-17 — P32 — `Link` does **not** declare `protocols`. — `http`, `https`, `mailto` and `tel` are schemes linkify already knows; registering them as custom ones only prints "already initialized" four times per editor. What a link may point at is decided by the sanitiser, which is also the rule that covers pasted and legacy markup.
- 2026-09-17 — P32 — A figure's alignment and width are `data-align` / `data-width` attributes, not `class="align-x width-y"`. — Both are on the sanitiser's attribute allow-list as written in ART-04, while `class` is narrowed to `sna-*` / `prose-*`; layout classes would have needed a second exception for no gain. The figure still carries `class="sna-figure"`.
- 2026-09-17 — P32 — The iframe allow-list is four hosts over `https` only: `www.youtube.com`, `www.youtube-nocookie.com`, `player.vimeo.com` and `www.google.com` restricted to paths under `/maps`. `data-youtube-video` is on the attribute allow-list so the 16/9 wrapper the extension writes survives. — A host alone is not enough for Google, which serves the map embed and everything else from the same name; and an embed stripped of its wrapper is an iframe with no aspect ratio.
- 2026-09-17 — P32 — A `data:` URL is dropped from `src` and `href`, including the `data:image/` DOMPurify allows on an `<img>` by default. — The contract embeds pictures by URL (§6.1, prompt 39's media library); a base64 image in a description is a record nobody can audit and a payload the API has no size rule for.
- 2026-09-17 — P32 — `SafeHtml` takes an `onFaqItems(items)` callback rather than publishing the questions through a context. — Prompt 38 needs the `faq` blocks of a body merged into the page's `FAQPage` schema, and the page already owns its `<Seo>`; a context would make every page that renders prose a provider for the benefit of the two that care.
- 2026-09-17 — P32 — The three rendered blocks are `React.lazy` inside `SafeHtml`, and `FaqAccordion` is reached only through that boundary. — Almost all CMS HTML is plain prose and every public page renders some, so the listing cards and the accordion should not be in each page's own bundle; the dynamic import is also what breaks the cycle `FaqAccordion` closes by rendering its answers through `SafeHtml`.
- 2026-09-17 — P32 — `prose.css` is loaded once in `src/App.js` beside `global.css` rather than by each component that renders prose. — It is a global vocabulary, not a component's stylesheet, and a file every lazily loaded route imported for itself left `mini-css-extract-plugin` unable to order the extracted CSS, which fails `build:ci` where `CI=true` treats a warning as an error.
- 2026-09-17 — P32 — Forms import `src/components/editor/RichTextField.jsx`, a `React.lazy` wrapper, never `RichTextEditor` directly; its loading placeholder is drawn with the design system's `Skeleton` and has no stylesheet of its own. — Tiptap and ProseMirror are ~150 KB gzipped on one kind of screen, and a stylesheet shared between the panel's bundle and the lazy one is the same CSS-ordering ambiguity by another route. The wrapper is also what keeps the FAQ block's node view — which edits its answers with a compact editor — out of `extensions.js`'s own dependency cycle.
- 2026-09-17 — P32 — `package.json` → `jest.transformIgnorePatterns` gained `@tiptap`. — Jest 27 does not read an `exports` map, so `@tiptap/pm/state` resolves to the TypeScript source beside it and `@tiptap/core/jsx-runtime` to an ESM re-export; transpiling the scope is one entry, where mapping each subpath to its `.cjs` build would be a list to maintain per release.
- 2026-09-17 — P32 — The block schema's `html` field type is renamed `richtext`, and the `<script` refusal in `validateBlockData` and in the property form's validators stays. — The editor sanitises what it emits and `SafeHtml` sanitises again on the way to the page, so nothing typed can reach either check; they remain as the cheap guard against a value that went through neither — a legacy record, an import, a payload assembled by hand.
- 2026-09-17 — P32 — The bubble and "+" menus are written here rather than taken from `@tiptap/react/menus`. — They have to be real `role="toolbar"` strips a keyboard can reach, and their position is measured against the editor's own box so a full-screen editor and one inside a scrolling form agree; both also need the `editor.isInitialized` guard, without which React's strict mode tears the ProseMirror view down under them and the page throws.
- 2026-09-17 — P32 — "Is anything selected" is answered by the text between the selection's two ends, never by `selection.empty`. — An editor that has not been clicked into yet reports a selection covering the document, and a link dialog that believed it hid the box the new link's words go in.
- 2026-09-17 — P32 — `normalizeHtml` also trims the empty paragraphs a document **opens** with, not only the trailing ones the prompt names. — A leading blank line is the same artefact at the other end, and a single deliberate blank line between two paragraphs still survives: only runs of two or more are collapsed.
- 2026-09-17 — P32 — `sanitizeHtml` answers `''` where no DOM is available (`DOMPurify.isSupported` false). — Nothing in this app runs without one — Jest is jsdom, the prerender of prompt 41 is a real browser — and the alternative for a hypothetical caller is returning markup nobody sanitised.
- 2026-09-17 — D26/P33 — The twelve seeded SNA articles are what `/admin/articles` administers; nothing from the boilerplate's ten was migrated and no screen reads its field names any more. `src/utils/adapters/legacyArticle.js` is deleted with the two files that needed it, and `src/utils/adapters/` with it. — D26 decided the content; this is where the code stopped pretending the old shape still existed.
- 2026-09-17 — D28/P33 — Both article previews — the list's row action and the form's button — ask `GET /admin/articles/:id/preview-token` and then open `<PATHS.article(slug)>?preview=<token>` **on this origin**, not the absolute `url` the response also carries. The form saves first when it is dirty or unsaved, because a preview of unsaved work is a lie about what the page says. — The token is the permission; the absolute URL is built from `seoSettings.siteUrl`, which in development is the placeholder production domain, so following it would send an editor to a site that does not exist yet.
- 2026-09-17 — D76/P33 — The four categories are ordinary records on `/admin/articles/categories`, ordered by drag when the table is sorted by `Order`, and the article form can create a fifth through a two-field dialog. — D76 fixed the starting four; nothing about them is hardcoded, and a category is a public URL with a landing page, which is why it gets a dialog that says so rather than a creatable select.
- 2026-09-17 — P33 — **Duplicating an article is a client-side `POST`, and it leaves the slug keys out of the body rather than sending them empty.** §5.14 gives properties a `duplicate` endpoint and articles none, so `articleService.duplicate()` reads the record, drops the ten fields the API owns (`contentText`, `wordCount`, `readingTimeMinutes`, `viewCount`, the three embeds, the ids and the timestamps) and posts the rest as a draft with `publishedAt: null`, `isFeatured: false` and a title shortened so `" (Copy)"` still fits the 100-character field. `slug` and `seo.slug` are **deleted from the body**: §5.9 has the API derive and de-duplicate a slug when the client does not choose one, but an empty _string_ is not "no slug" to the validator — it is a slug that fails the slug pattern, and the request is refused with a 422 on a field nobody typed into (verified against the mock; the copy lands on `<slug>-copy`). The same mistake is still live in `PagesListPage` and is booked as NEW-36.
- 2026-09-17 — P33 — **There is no default author.** `authorId` is a required select over the real `/admin/authors` records, and a new article opens with it empty. — The boilerplate defaulted it to the old brand name, so every article was signed by a byline nobody chose (ADD-20); an empty required field asks the question once, and the rail's checks card says it is unanswered.
- 2026-09-17 — P33 — The publish rules (an excerpt, a featured image, ≥ 300 words) are enforced for **both** states that put an article in front of a visitor, `published` and `scheduled`. A scheduled article is a published one with a date on it. — The alternative would let a thin article through on Tuesday because it was booked on Monday.
- 2026-09-17 — P33 — Alt text is required as soon as the featured image has a URL, whether or not the article is being published, and it is the one publish rule that also applies to a draft. — Alt text is what a reader who cannot see the picture is given (§8.3); making it a publication formality invites filling it in at the last moment, which is when it gets filled in with the headline.
- 2026-09-17 — P33 — "Publish now", "Schedule" and "Save as draft" set the status and save on the render that commits it (a `pending` mode plus an effect keyed on `values.status`), rather than validating the values on screen and posting a different status. — The publish rules have to be read against the state being asked for; and a refusal then leaves the radio where the editor can see what they were about to do.
- 2026-09-17 — P33 — `publishedAt` is decided by one rule: a scheduled article gets the moment its field names; anything else keeps the stored moment **only when it has already arrived**, and otherwise sends `null` for the API to stamp. — §6.8 keeps the first publication date through an unpublish and a republish, but `isLive` in `mock-server/lib/articleFilters.js` reads the date and not the word, so a scheduled article demoted to a draft and then published would have been "published" with a future date and invisible to everybody.
- 2026-09-17 — P33 — The scheduling control is a native `datetime-local` and the conversion is a fixed **+05:30**, not the browser's own offset. `utils/articleUtils` exposes `toDateTimeLocal` / `dateTimeLocalToIso` / `isFutureDateTime`. — India has never observed daylight saving, so IST needs no timezone database; and D22 makes every absolute date the site shows a Bengaluru one, which an editor scheduling "Tuesday 06:30" from anywhere means too.
- 2026-09-17 — P33 — The word count the publish rule is decided on is computed from the stored HTML (`articleUtils.wordCount`), not read from the editor. `DOMParser` walks the document and inserts a space wherever a block element or a `<br>` ends, because `textContent` concatenates `<p>one</p><p>two</p>` into one word and would undercount every article. — It is the number the API will derive from the same string, so the rule is a function of what gets saved; the editor's own counter agrees with it (Tiptap's `CharacterCount` reads the document's text with block boundaries as spaces) and is asked only for the character figure, which nothing else knows.
- 2026-09-17 — P33 — `generateExcerpt(html)` takes the **first paragraph**, not the first 300 characters of the document, and cuts it at a word boundary with one character of the budget reserved for the ellipsis. — A body that opens with a heading or a pull-quote would otherwise produce an excerpt that reads as a fragment, and a hard cut mid-word reads as a bug.
- 2026-09-17 — P33 — The tag box lower-cases and trims what is typed before creating anything, checks the loaded list first, and on a 409 looks the tag up by slug and selects it instead of reporting the refusal. — `Khata `, `khata` and `KHATA` are one tag; two of them would split the archive at `/insights/articles/tag/khata` in two. A duplicate is the tag the editor asked for, not an error.
- 2026-09-17 — P33 — The article form carries the record's **whole** `seo` branch through every save and edits three of its fields (`title`, `description`, `focusKeyword`) in a "Search result" section of the main column; the categories and authors forms carry it untouched behind an `Alert`. The `seo.slug` is kept equal to the entity slug (D34). — A `PUT` replaces the record (§5.8), so a partial `seo` object would reset the keywords, the canonical, the social cards and the stored analysis that prompt 36's panel owns. The counters are duplicated from `SeoPlaceholderTab` rather than imported, so the articles chunk does not pull in the property form's.
- 2026-09-17 — P33 — The FAQ repeater and the "Search result" fields live in the **main column**, not in the rail the prompt lists them under. — §6 of the same prompt asks for compact rail cards; a rich-text repeater and three length counters in a 340 px column are neither compact nor usable. Everything else the prompt puts in the rail is in the rail.
- 2026-09-17 — P33 — A FAQ row carries a client-only `_key` for its React key and its repeater callbacks, stripped from the payload. — §6.8 gives a FAQ `{question, answer}` and no id, and §4.3 forbids keying an editable list on its index: removing the first question would otherwise move every editor below it.
- 2026-09-17 — P33 — Article categories, tags and authors are read and written through `services/masterDataService` (three new `collection()` entries) rather than through `articleService`. — They behave exactly like the rest of the master data — a slugged name with a count of what points at it and a delete guard — so they get the factory the rest of it uses and `adminCrud()` hands them to `MasterDataPage` unchanged. `articleService` keeps the articles themselves.
- 2026-09-17 — P33 — `useArticleTaxonomy` loads the three lists once per screen from the **admin** endpoints with `perPage: 'all'` and merges records created on the screen over the answer. — Both the list's filters and the form's pickers need the same three lists; the admin half is what shows a category that has since been retired but that an article still points at, and merging locally is what stops a tag somebody just created from appearing as a bare id while the lists reload.
- 2026-09-17 — P33 — The list screen's header offers only "New article": the three taxonomy screens are already in the sidebar's own Articles group (`config/rbac.js`). — Found in the browser walk, where both copies of the same three links were on screen at once.
- 2026-09-17 — P33 — Neither the list nor the form offers a `seoScoreBand` filter or an SEO score the editor can set, although the API answers both. — The score is computed and stored by prompt 36's panel; a filter over a column that is `null` on every record today would be a control that does nothing.
- 2026-09-17 — P33 — A duplicated article drops `seo.canonicalUrl` and `seo.redirect` as well as the score and the analysis, and keeps the keywords, the title, the description, the social cards, the schema and the sitemap settings. — A canonical or a redirect names one page and the copy is a different page: inherited, they would quietly canonicalise the copy to the original or redirect it away, and no screen in the panel shows either field until prompt 36's SEO panel. Clearing the score matches what §5.14 already has the property `duplicate` endpoint do.
- 2026-09-17 — P33 — Ctrl/Cmd+S refuses `event.repeat` and anything while a save is open. — Every button that saves is `disabled` while one is in flight; the keyboard is not, and a held Ctrl+S on the add route was several `POST`s and several articles.
- 2026-09-17 — P33 — `tagIds`, `relatedArticleIds` and `relatedPropertyIds` are de-duplicated both at the control and in the payload. — `MultiSelect` appends whatever `onCreate` hands back, and `createTag` answers an existing tag by slug: typing `stamp-duty` over an already-selected "Stamp Duty" is the same tag under a different label, and two identical chips (and two identical cards on the public page) is a worse answer than one.
- 2026-09-17 — P33 — **A duplicated CMS page chooses its own slug; only an article's is left to the API.** `pageService.duplicate()` asks for `<slug>-copy` (the suffix on the last segment of a path, trimmed to keep it inside the 120-character budget) and takes `check-slug`'s free variant when that name is taken, setting `seo.slug` to the same string (D34). — §5.9 has the API derive a slug when the client does not choose one, but §6.10 makes a page's slug `required` **with no default**: measured against the mock, `slug: ''` answers `422 {slug, seo.slug}` and an omitted `slug` answers `422 {slug}`, so the fix that worked for articles does not transfer. Choosing `<slug>-copy` is also what §5.14 has the property `duplicate` endpoint do server-side.
- 2026-09-17 — P33 — The two rules a copy of any record follows — `copyTitle(title, { maxLength })` and `copySeo(seo, { slug })` — live in `src/utils/duplicateRecord.js`, and both `articleService.duplicate` and `pageService.duplicate` read them. — Articles and pages are the two resources §5.14 gives no `duplicate` endpoint, so both assemble the copy in the browser; two copies of "what may a copy inherit" is how they drift, and the `seo.canonicalUrl` / `seo.redirect` reasoning is the part that is easy to forget twice.
- 2026-09-17 — P33 — A duplicated page is a draft in **neither** menu **and** carries neither menu (`headerMenu` and `footerColumn` null beside `showInHeader`/`showInFooter` false). — `PageFormPage` nulls the placement whose switch is off on every save, so a copy that kept a stale `headerMenu` would be data the next save silently corrected; and a copy appearing in the site's header the moment it exists is the thing the two switches were turned off to prevent.
- 2026-09-17 — P33 — `pageService.duplicate` appends `-copy` to `record.slug` **as it stands**, and takes `PATH_SLUG_MAX_LENGTH` from `src/services/schemas/page.js` rather than from `src/utils/slug.js`. — Two reasons. The slug came from the API, which is what canonicalises one (§5.9), so `slugifyPath` over it is work with no effect; and `pageService` is imported by the public `CmsPage` route as well as the two admin screens, so an extra module in its graph moves what the chunks contain — the first version of the fix imported `utils/slug` and `build:ci` stopped compiling with a `mini-css-extract-plugin` "Conflicting order" between `FaqAccordion.module.css` and `finance.module.css`. A service two layers share is not the place to reach for an admin util.
- 2026-09-17 — P34 — **`GET /articles/:id/adjacent?categoryId=` is added to the contract**, beyond the §5.14 catalogue: `{ data: { prev, next } }`, two `ArticleSummary` rows, `prev` the piece published before this one and `next` the one after, ordered by `publishedAt` within the category (ties broken by id). It is in the mock, the registry, `endpoints.test.js`'s catalogue list, `npm run smoke` (`articles.adjacent-pair`), `npm run test:mock` and `docs/API_CONTRACT.md`. — The article page's previous/next pair is two rows, and `GET /articles` has no "published before this" filter: the alternative the prompt offers, two list calls, means reading a whole category into the browser to find the two neighbours of one record, which costs more the better the blog does. A Laravel implementation is two `LIMIT 1` queries.
- 2026-09-17 — P34 — **A contents list is drawn only from three headings up**, and only when the record's `tableOfContents` is true; `h2` and `h3` are collected, nothing below. — Two headings is a list as long as what it indexes, which is furniture rather than navigation; the editor's switch stays the override in both directions.
- 2026-09-17 — P34 — The heading-id rule lives in `src/utils/toc.js` (`assignHeadingIds`, `buildToc`) and `components/editor/SafeHtml` calls it while rendering. The article page builds its list from the **sanitised** body — the very string `SafeHtml` writes the ids onto — rather than from `article.content`. — One implementation, so an anchor in the list and an id in the body cannot drift; sanitising first is what makes them the same document, down to a heading the allow-list would have dropped. Duplicate titles get `-2`, `-3`, and a heading with no sluggable text gets `section`.
- 2026-09-17 — P34 — The share bar is four plain links (WhatsApp, X, LinkedIn, Facebook) plus a copy button, and it sends `share_click` with `{ method, context: 'article' }` — the registered event name of `utils/analytics`, not the `share` the prompt names. — `navigator.share` exists on phones and on Safari and nowhere else, so a bar that depended on it would be a dead row on most desktops; and §4.6 of the master context makes an event name that is not in `EVENTS` a typo rather than an event. `ShareButton` already sends `share_click`, so one GA4 report covers the whole site.
- 2026-09-17 — P34 — An article has **one** list of questions: `article.faqs` and the questions of every FAQ block in the body, merged into the accordion under the article, with `SafeHtml` told not to print the blocks a second time on the way past (`faqBlocks={false}`, the FAQ twin of `propertyCards={false}`). The blocks are still reported through `onFaqItems`, which is what prompt 38's `FAQPage` graph is built from. — The prompt asks for both the merge and live FAQ blocks in the body; doing both literally shows a reader the same question twice on one page. CTA and listings blocks still render exactly where the editor put them.
- 2026-09-17 — P34 — An author's outbound links carry `rel="noopener noreferrer nofollow me"`, not the `rel="noopener nofollow me"` the prompt names. — `react/jsx-no-target-blank` refuses `target="_blank"` without `noreferrer` and `npm run lint` runs at `--max-warnings=0`; the extra token is also what every other external link on the site carries. `me` and `nofollow` are unaffected.
- 2026-09-17 — P34 — **`components/sections/article/index.js` is an ordered barrel and every blog page imports through it.** `ArticleCta` (`LeadForm.module.css`), `ArticleFaqs` (`FaqAccordion.module.css`) and `RelatedProperties` (`PropertyCard.module.css`) are exported in that order, which is the order the property page loads the same three stylesheets in. `components/cms/blocks/index.js` was reordered to match (`LeadFormBlock`, `FaqBlock`, `BanksBlock` ahead of the alphabet; `PropertiesBlock` left where it was, because hoisting it put `PropertyCard.module.css` in front of the home page's partners band). `ArticleFaqs` exists as a component at all so that the accordion's stylesheet has one fixed position in that order. — A route's stylesheets are extracted in the order its modules are pulled in, and `mini-css-extract-plugin` fails `build:ci` when two routes disagree. The CMS barrel's alphabetical order was the property page's backwards and survived only while no third route pulled all three together; the blog is that route. Measured against `npm run build:ci`, not reasoned about.
- 2026-09-17 — P34 — **`prose.css`'s paragraph reset is wrapped in `:where()`.** `.prose p { margin: 0 }` is one class and one element, which outranks the `.prose > * + *` flow rule whatever the source order, so every paragraph in every CMS body on the site — an article, a listing description, a locality guide, a page's rich text — had a zero top margin and ran into the one above it. `:where(.prose) p`, `:where(.prose) figure` and `:where(.prose .sna-figure)` keep the reset and hand `margin-top` back to the flow rule that was written for it. — Found in the browser on the article page while building it, measured with `getComputedStyle` (`margin-top: 0px` before, `16px` after); the rhythm the file already intended now applies.
- 2026-09-17 — P34 — One engine, `ArticleIndex` in `pages/public/Articles.jsx`, draws `/insights/articles` and the three archives; the category, tag and author pages pass the filter their route fixes (`fixedParams`) and their own copy. The hero is fetched and drawn on page one of the unfiltered index only, and the grid leaves it out. — Four copies of a search box, a sort, a grid and a rail is how three of them end up with different ones. The hero excludes itself from the grid because a lead item that is also the first card makes an index look like it has one article fewer than it has.
- 2026-09-17 — P34 — The featured hero is one column at every width, with a 21/9 cover; the cards stay 16/9. — The hero sits in the listing's main column, which is about 860 px beside the rail however wide the window is: split in two that leaves the headline twenty characters, and at 16/9 across the full width the cover is most of a laptop screen before the headline is reached.
- 2026-09-17 — P34 — The contents list is **one element**, placed by the stylesheet: a sticky rail from 1200 px where it is always open, and a collapsible card above the article below that, where the heading is the control. The body is first in the markup and the list is moved with `order`. — Two copies mean two `IntersectionObserver`s watching the same headings, and a JavaScript breakpoint that disagrees with the CSS one; content-first markup is also what a screen reader and the source should agree about.
- 2026-09-17 — P35 — **The weight tables are apportioned from points, not written down.** Each test carries an importance of 1–5 (`POINTS`), `APPLICABILITY[entityType]` says which tests a type is asked, and `WEIGHTS[entityType]` is those points apportioned to exactly 100 by the largest-remainder method with ties broken by test id. — Eight hand-written tables of up to 41 rows each have to sum to 100 eight times, and the first test anybody adds breaks one of them silently. Points also say the same thing across types ("a title length is worth five times a power word"), which a percentage cannot: the same test is worth 4 on an article, which is asked 41 questions, and 9 on an author, which is asked 17. The cost is that two tests with the same points can differ by one inside a column; `score.test.js` asserts the sum is 100 and that no weight is 0, and `docs/SEO_ENGINE.md` prints every column.
- 2026-09-17 — P35 — **A record nobody has written yet fails every applicable test, and scores 0.** `analyze()` recognises a record with no title, no slug, no description, no summary, no body and no focus keyword and reads every non-skipped result as a failure, keeping its message. — §7 of the prompt requires the empty record to score 0, and half the enhancements of SEO-07 (`rera-present`, `faqs-min-3`, `floor-plan-or-units`, `amenities-min-8`) warn rather than fail by design, which is half a weight each. `indexable` is worse: robots default to `index: true` and most types default to active, so an empty form was passing a test about a page that does not exist. Doing it once in `analyze()` rather than threading an `isEmpty` flag through fifty rules keeps every analyser a function of what it measures.
- 2026-09-17 — P35 — **A test whose subject is missing fails rather than passing vacuously.** "Is the title in capitals?" is not a pass when there is no title; `keyword-density` over an empty body is not a pass; a body with no headings fails `heading-hierarchy`. — A vacuous pass is a score for work not done, and it is exactly the shape of the boilerplate's old scorer, which gave 40/100 to an empty record.
- 2026-09-17 — P35 — **The description tests measure `entity.seo.description` alone**, never the `seoSettings.defaults.metaDescription` a visitor would actually see when it is empty (§9.3). — The site default is the same sentence on every page, which is the thing `description-unique` exists to prevent; scoring it as this record's description would pass every record on the site for a field nobody filled in.
- 2026-09-17 — P35 — **The effective title is `seo.title`, or the type's resolved template only when the record has a title of its own.** — Resolved against an empty form, `%bhk% %propertytype% %listingtype% in %locality%, %city% – %price% %sep% %sitename%` cleans down to the site's own name and whatever punctuation survived. Measuring that would have the five title tests scoring the brand instead of the record.
- 2026-09-17 — P35 — **`indexable` fails, rather than warns, for a record that is not published.** — It is the truth: a draft is not indexable. A warning would earn half a weight for a page search engines cannot see at all, and the hint says what to do about it ("publish the record when it is ready").
- 2026-09-17 — P35 — **`og-image-set` passes on an explicit `seo.og.imageUrl`, warns when only the cover image is there, and fails when there is neither.** — §9.3's fallback chain means the cover image is used and the share works, so failing it would be wrong; but a 1200×630 image built for sharing survives the crop and a portrait cover photograph does not, which is worth a warning.
- 2026-09-17 — P35 — **The whole content-readability group is skipped for a property.** — SEO-09 names articles, pages and localities. A listing description is a specification, and scoring it for transition words and the passive voice would be scoring it for the wrong thing; §7's `heading-hierarchy` test travels with the group for the same reason.
- 2026-09-17 — P35 — **`heading-hierarchy` (§7) is a content-readability test that warns, never fails, for a structural problem**: an `<h1>` inside a body (the page already has one, §9.7), an outline that jumps a level, or a body that opens below `<h2>`. It fails only when there are no headings at all. — An H1 in the body is an editor's mistake, not a broken page; `keyword-in-subheading` ignores H1s for the same reason, so the two tests never contradict each other about the same heading.
- 2026-09-17 — P35 — **Stemming is three plural rules and a short irregular list, and hyphens equal spaces.** `flats`→`flat`, `houses`/`boxes`→`hous`/`box`, `cities`→`city`, plus `bhks`→`bhk`, `properties`→`property`; `ready-to-move` matches `ready to move` both ways because `keywords.normalize` replaces hyphens with spaces **after** the markup is stripped. Word order and intervening words are not forgiven. — A Porter stemmer would match "apartment" to "apart" and tell an editor their keyword is present when it is not; matching across word order would tell them "whitefield flats" ranks for "flats in whitefield", which is two different search results. Replacing the hyphens before stripping the tags would turn `<!-- a comment -->` into visible text.
- 2026-09-17 — P35 — **Keyword density counts occurrences, not the words they are made of.** One use of a four-word phrase counts once. — The question `keyword-density` asks is how often the phrase is used; counting its words would put a four-word keyword over 2.5 % after six uses in a 600-word article, which is normal writing.
- 2026-09-17 — P35 — **The passive-voice heuristic is an auxiliary followed by a past participle, with about forty named exceptions.** `is|are|was|were|be|been|being|am|get` (optionally through an adverb) plus a word of four letters or more ending `-ed`/`-en`, or one of ~50 irregulars; `red`, `open`, `kitchen`, `between`, `speed` and their kind are excluded by name. It warns, never fails. — There is no parser here and there will not be one; the shape catches "the plan was approved" and "the account is maintained", and the exception list stops "the door is red" and "the kitchen is open" from being reported as grammar. A heuristic that can be wrong is allowed to warn and not to fail.
- 2026-09-17 — P35 — **Pixel widths use a canvas in the browser and a per-character table in Node and Jest (D85), and the question is never asked in jsdom.** jsdom has a `document` and answers `getContext('2d')` with `null` while logging an implementation error, so `snippet.js` checks the user agent for `jsdom` before it asks. The table is the Helvetica metric set (what Arial was drawn to match) in 1/1000 em units; an unknown character costs the width of a digit, which at the 14 px description size is the ~8 px per character the fallback promises. — Determinism is the point: a test that asserts 489 px has to assert it on every machine. A consequence worth knowing: a 160-character description — the top of the §9.1 character guide — is about 1030 px of real prose and therefore **warns** on the pixel limit. Both limits are in the spec and both are enforced; the message prints the two numbers so an editor can see which one bit.
- 2026-09-17 — P35 — **`text.js` parses HTML into one event stream with two backends, and `text.test.js` asserts they agree.** `DOMParser` where there is one, a tag scanner where there is not; every rule is written against the stream. — §7 requires identical results in both environments, and the only way to be sure of that is to compare them on real fixtures rather than to reason about them. The scanner is not a sanitiser and never renders anything: `components/editor/SafeHtml` remains the only thing that puts markup on a page.
- 2026-09-17 — P35 — **The JSON-LD validator treats `{ "@id": … }` and `{ "@type": "X", "@id": … }` as references and does not ask them for required properties.** — A graph references its own nodes: `mainEntityOfPage: { "@type": "WebPage", "@id": <canonical> }` is a pointer at the page node, and requiring a `name` on it reported a correct graph as broken.
- 2026-09-17 — P35 — **A listing node carries two `@type`s** — `['RealEstateListing', 'Apartment'|'House'|'Place'|'Residence']` — rather than a second node for the dwelling. §9.3 asks for both; one node with an array keeps one `@id` and one set of properties, where two nodes would need a relationship between them that schema.org does not really define for a listing and its subject.
- 2026-09-17 — P35 — **Test fixtures are `.json`, not `.js`.** CRA's `testMatch` is `src/**/__tests__/**/*.{js,jsx,ts,tsx}`, so a data module inside `__tests__/fixtures/` is collected as a suite and fails with "your test suite must contain at least one test", and `testPathIgnorePatterns` is not one of the Jest keys CRA lets a project override. The five seed records, the SEO settings, the master data and the overview rows are data, so JSON costs nothing; helpers live in the test file that needs them.
- 2026-09-17 — P35 — **`src/seo/urls.js` imports `routes/paths.js`** rather than writing the public routes out again, and `analyzers/additional.js` imports `SLUG_MAX_LENGTH`/`SLUG_PATTERN` from `utils/slug.js`. — Both files are pure and framework-free, so the engine stays pure; a second copy of "where does a locality live" is how a renamed route half-updates.
- 2026-09-17 — P35 — **`components/listing/listingSeo.js` keeps its own `cleanTitle` and canonical builder until prompt 38.** `src/seo/variables.js` and `src/seo/urls.js` now hold the general versions. — Prompt 35's guardrails forbid touching UI components, and prompt 38 is the one that rewires the listing head through `<Seo type="listing">`; two copies for three prompts is better than an unrequested edit to a screen this prompt does not test. Recorded here so 38 deletes the copy rather than the original.
- 2026-09-17 — P35 — **`data/powerWords.js` lists a hyphenated phrase once** (`ready-to-move`, `rera-approved`) and the matcher treats hyphens and spaces as interchangeable; the current and next calendar years are computed rather than listed. — A list with "2026" written into it is a list that goes stale in December, and two spellings of one phrase is two rows to keep in step.
- 2026-09-17 — P35 — **`generateDefaults` never replaces a value an editor wrote unless `overwrite: true` is passed**, and whitespace counts as empty. — ADD-27's "Auto-Generate" overwrote everything with the boilerplate's own titles and canonicals, which is why that screen ended up read-only. The panel's per-field buttons fill what is blank; only "Regenerate everything" passes `overwrite`.
- 2026-09-17 — D87/P36 — **One panel, two variants, eight mounts.** `SeoPanel variant="full"` is the property form's tab 16, the article form's second tab and the page form's "Search engines" section; `variant="compact"` — General inline, Social, Advanced and Schema as disclosures — is the locality and developer forms and the three `MasterDataPage` screens (article categories, authors, property types) through a `seoPanel: 'compact'` line in their configuration. — The spec asks for both shapes and nothing else differs between them: compact folds the tabs rather than dropping fields, so a locality still has a canonical, a redirect and a custom schema, and "do not reduce existing functionality" holds on every screen.
- 2026-09-17 — D34/P36 — **The panel's permalink field writes the entity's slug, not a copy of it.** `onSlugChange` sets the record's `slug` and the panel sets `seo.slug` in the same handler, and every host's payload builds `seo.slug` from the entity slug it is about to send (`toSeoPayload(values.seo, slug)`). — One URL. The alternative — a read-only mirror, which is what the placeholder tab had — meant an editor who wanted to change the address had to leave the SEO tab to do it, and left two fields that could disagree if anything ever wrote only one.
- 2026-09-17 — P36 — **Two things in `seo` block a save, and only two** (`components/seo/seoSideEffects.js` → `validateSeoBranch`, called by all five host validators): custom JSON-LD that does not parse or does not pass the structural validator, and a redirect switched on with an empty or non-path target. — Invalid JSON-LD does not fail quietly: it invalidates the whole `<script type="application/ld+json">`, so the generated nodes go with it, which is worse than publishing none. A redirect with nowhere to go cannot be written as a rule and would leave an editor believing the page had moved. Everything else the panel measures is advice — a sixty-eight-character title is cut in a result, not refused — so the meters colour and the save goes through.
- 2026-09-17 — P36 — **The redirect is written after the entity save, by the host, through one helper.** `applySeoSideEffects(entityType, savedRecord)` takes the record **as the API answered it**, because `fromPath` is the record's own public path and a new record has no slug until then. It upserts through `redirectService.upsertByFromPath` (a `q` search on `/admin/redirects`, then `PATCH` or `POST` — the filter already existed, so no mock change was needed), deactivates rather than deletes when the switch goes off, and returns instead of throwing when the write fails. — The panel cannot do it: it does not know when its host saved. A failed side effect must not be reported to an editor as a failed save, and a rule an editor turned off is a change of mind about the redirect, not a request to erase its history — the redirects screen (37) is where a rule is deleted.
- 2026-09-17 — P36 — **The analysis is debounced 400 ms and guarded twice against looping.** Its input is the record with the six computed fields of §9.6 stripped out (`withoutComputed`), so storing a result never schedules the next analysis; and the result is only written when the score, the band, the counts or any row's status or message actually changed (`signatureOf`). — The panel writes its answer back onto the record it analysed, which is a loop unless both ends are closed. Measured: a property with twenty images, forty sections of body and eight FAQs analyses in well under the 100 ms budget (`SeoPanel.test.jsx`).
- 2026-09-17 — P36 — **`src/seo/resolve.js` is the single resolver for what a record publishes**, and the Resolved values box renders exactly what it returns. Prompt 38's `<Seo>` calls the same function. — A box whose job is to answer "so what does the page send?" is worse than useless if it is a second implementation that can drift from the head it describes.
- 2026-09-17 — P36 — **`schema.buildGraph` is the record's own share of the `@graph`** — the node its type leads with, its questions, its video and its trail — and not the page's. `Organization`, `WebSite` and the lists of whatever else a page shows belong to the page and arrive in 38. `schema.type` retypes only the leading node, so the questions stay a `FAQPage` whatever the listing calls itself. — The Schema tab previews one record; a preview that included the site's knowledge graph would tell an editor their locality publishes an `Organization`.
- 2026-09-17 — P36 — **The panel is a `React.lazy` chunk (`SeoPanel/index.jsx`) and the §9.6 value helpers live outside it (`components/seo/seoValues.js`).** — Measured, not reasoned about: with the panel imported statically by six forms plus `MasterDataForm`, `build:ci` failed with eleven `mini-css-extract-plugin` "Conflicting order" errors between `SeoPanel.module.css` and seven admin-kit stylesheets, and no import ordering resolved them. The lazy boundary removes the constraint entirely, the way `RichTextField` already does for the editor; `SeoSummaryCard` — which renders eagerly in a form's rail — got its own small stylesheet for the same reason. The helpers are outside because a form's `toPayload` needs `toSeoPayload` without needing the panel.
- 2026-09-17 — P36 — **A `seo` patch reaches a form as dotted paths, one key at a time** (`toSeoPaths({ title: 'x' })` → `{ 'seo.title': 'x' }`, applied through `setField`/`setFields`). — Both writers compose on the current values inside the reducer. Merging `{ ...values.seo, ...patch }` instead reads `values.seo` at render time, so an editor's keystroke and the analysis landing behind it in the same tick would each overwrite the other.
- 2026-09-17 — P36 — **An empty control means "not set", and `toSeoPayload` spells that `null` before the branch is sent.** The contract types `canonicalUrl`, the two image URLs, the four social strings, the breadcrumb title and the change frequency as nullable, and the API refuses `''` for a URL or an enum. — Found against the running mock, not in a review: every form that sent `values.seo` verbatim would have been refused the first time an editor cleared a field. One translation in one place, so no host has to remember which of the fifty fields are which.
- 2026-09-17 — P36 — **The site-wide SEO index is a module-level cache refreshed by an event, not a context.** `useSiteSeoIndex` loads `GET /admin/seo/overview?perPage=all` once per admin session and re-loads on `emit('seo:changed')` from `src/utils/events.js`, which every save publishes. — §13.1 settles that the uniqueness tests read this list; it is a cache rather than state, it outlives the tree that asked for it, and nothing should re-render because it arrived. A desk that cannot read it at all gets a panel whose three uniqueness tests `skip`, which is what they already do without it.
- 2026-09-17 — P36 — **A fix hint focuses by DOM id rather than by ref.** The panel's own controls carry `fieldId('seo.title')`; the property form's tabs carry `propertyFieldId('description')` and the article form maps the analysers' field names onto the ids its cards already had. — The design system's inputs are plain function components, so a `ref` on one is a React warning; and an id on the **block** (the rich-text editor, a repeater) lets the same helper focus the first control inside it.
- 2026-09-17 — D30/P37 — **The SEO desk reads the overview twice, on purpose, and the two reads are different questions.** `useSeoOverview()` is the table: server-paged, server-filtered, URL-synced. `useSeoOverviewAll()` is the cards, the duplicates and the issues: `perPage=all`, cached for 60 s in the module, thrown away on `emit('seo:changed')`. — An average over twenty rows is not the site's average, and paging a duplicates list would split the pairs it exists to show; equally, a desk of four thousand records must not fetch four thousand rows to draw one page of a table. The cache is separate from the SEO panel's (`useSiteSeoIndex`, session-long) because this one is the screen an editor is watching and has to be re-read after a bulk run; the cost is one extra request per admin session.
- 2026-09-17 — P37 — **The site-wide reader is exported as `useSeoOverviewAll`, not hung off the table hook as `useSeoOverview.all`.** — `react-hooks/rules-of-hooks` recognises a hook by the name at the **call site**, and `useSeoOverview.all()` reads as an ordinary method call, which would switch the checks off silently. The prompt's spelling is the only thing lost.
- 2026-09-17 — P37 — **Duplicate detection is computed by the API and each overview row carries `duplicateOf: { title, description, focusKeyword }`.** Values compare trimmed, lower-cased and whitespace-collapsed; an empty value never collides; the comparison is always over the whole site, never the filtered page. Entries are row `key`s (`"<type>:<id>"`), and every row now carries that `key`. — Grouping eight collections by three values is a pairwise comparison the browser would repeat on every render of the dashboard; the server groups once, in one pass. A bare id would have been ambiguous across eight collections, so the row grew a key of its own rather than the flags growing a type beside every id.
- 2026-09-17 — P37 — **`customHeadHtml` and `customBodyEndHtml` are administrator-only, enforced in the UI and on the API.** The SEO settings screen does not offer the tab to a manager (§7 of prompt 37: "Custom HTML tab hidden for manager"), and `PUT /admin/seo/settings` answers 403 to a manager whose body **changes** either field — a body echoing the stored value unchanged, which is what saving another tab does, is allowed through. — §7 gives the whole SEO area to managers, and these two fields are different in kind: what they hold runs in every visitor's browser with the site's own origin. Prompt 37 §4.6 also describes a read-only notice for a manager; the §7 edge case is the more specific requirement, so the tab is hidden and the notice would have had nowhere to appear.
- 2026-09-17 — P37 — **Eight tests are "critical" on the Issues tab** (`SeoIssuesTab.CRITICAL_TESTS`): `focus-keyword-set`, `keyword-in-title`, `title-length`, `description-length`, `indexable`, `og-image-set`, `canonical-set` and `image-count`. Each carries the dotted path the dialog opens on, and the stored result's own `field` wins when it has one. — `src/seo` runs about fifty tests and most are advice; these eight are the ones that cost traffic outright, and a list of two hundred advisory warnings is a list nobody works through. A record nobody has analysed contributes nothing to the tab: "no answer" is not "failed".
- 2026-09-17 — P37 — **`redirectService.resolve(path)` asks the API; the pure matcher is `matchRedirect(pathname, rules)`.** — They answer different questions. `matchRedirect` reads the list the caller already loaded, which is what makes client-side redirects free on every navigation (D30, prompt 38's `RedirectHandler`); `resolve` is the redirects screen's "Check a URL", and a tester that repeats the screen's own reading of the data can only ever agree with it. `resolve` is also the one endpoint that counts a hit, so it is never on the navigation path. A 404 from it resolves to `null` — "no redirect" is an answer, not a failure.
- 2026-09-17 — P37 — **The bulk runs are sequential, write only what changed, and keep everything written before a stop.** Each record is read in full, its patch worked out (`planReanalysis` / `planGeneration`), and `PATCH { seo }` sent only when the answer differs from what is stored. "Stop" breaks the loop at the record it is on; the dialog cannot be dismissed while a run is going, so its close control only appears once the run is over. — Forty parallel writes to a JSON file is a race, and a progress bar that moves is worth more than a run that finishes two seconds sooner. Re-writing forty records to store the numbers they already hold is forty requests nobody asked for — measured on the seed: a second "Re-analyse all" over 119 records wrote 47 and skipped 72. The dismissal rule is not theoretical: the first browser pass closed the dialog mid-run with the header ×, the loop's next progress update reopened it, and the run had silently been cancelled.
- 2026-09-17 — P37 — **`templateKeyFor` selects the `home`, `listing` and `search` templates too** (`src/seo/variables.js`, prompt 35's engine). — Found while building the settings screen's head preview: §6.14 stores eleven `titleTemplates` keys and the function could select eight of them, so the home, listing and search templates were fields an editor could write and nothing could ever read. `resolveSeoOutput('home', …)` now resolves the home template, which is what §9.3 says it does and what prompt 38's `<Seo type="home">` needs.
- 2026-09-17 — P37 — **The redirects table is exported as Nginx rules as well as CSV** (`nginxSnippet.js`, one `location = <path> { return 30x <target>; }` per rule). Inactive rules are written as comments rather than dropped, notes become single-line comments, and anything outside `[A-Za-z0-9-._~/%]` is quoted. — D30 is honest about its limit: on an SPA the redirect happens after the app has loaded, so a crawler sees a 200 and a JavaScript navigation. The snippet is the same table for the server that fronts the build, where a 301 is a real 301. `location =` is an exact match because a redirect table holds exact paths: `/old-properties` must not also catch `/old-properties-in-whitefield`.
- 2026-09-17 — P37 — **The settings screen holds the whole singleton in one `useForm` and sends the whole singleton.** The `PUT` deep-merges on the API (§5.14), so the merge is what makes a partial save safe rather than what the screen relies on; a 422 opens the tab that owns the first failed field. — Ten panels over one record: a screen that sent only the open tab would have to know which keys that tab owns, which is the mistake `AdminSettings` made with `footerLinks` (NEW-11).
- 2026-09-17 — P37 — **The SEO playbook reads the engine rather than repeating it.** `seoGuideContent.js` carries 25 topics of prose; the weight table on the "What each test is worth" topic is rendered from `WEIGHTS`. — The boilerplate's sixteen accordions stated scoring rules that did not match the engine the admin ran (ADD-20). A table printed from the engine cannot drift, and a test added in `src/seo` appears in the playbook on the next build.
- 2026-09-17 — D30/P38 — **`RedirectHandler` is mounted in `routes/index.js`'s `AppShell`, not in `App.js`.** — Prompt 38 §3 says "in `App.js`", which is where `SiteSettingsProvider` lived when the prompt was written; D97 moved every router-aware provider into the data router's root route, and the handler needs both a router hook and the settings. `AnalyticsScripts` is mounted beside it for the same two reasons, and the inner `ErrorBoundary` — the one that can render `<Seo type="error">` — is there too, because the outer boundary in `App.js` has to survive a failure of the router itself and therefore cannot use a router hook.
- 2026-09-17 — D31/P38 — **The canonical's site URL is `seoSettings.siteUrl` → `settings.general.siteUrl` → `SITE.url`.** — D31 makes the runtime setting authoritative; the build-time environment is what answers before `GET /seo/settings` has, and `SITE.url` is what answers when neither is reachable. A page with no canonical at all would be worse than one pointing at the placeholder domain the client has not replaced yet (§2.7).
- 2026-09-17 — D36b/P38 — **The SEO engine's pure half is CommonJS: `src/seo/{text,urls,variables,entityAdapters,resolve,breadcrumbs,pageTypes,pageGraph}.js` and all of `src/seo/schema/`, plus the three leaves they reach (`src/config/site.js`, `src/routes/paths.js`, `src/utils/format.js`).** React and Jest keep importing them unchanged; `module.exports` is the namespace object the default import already was. — `scripts/validate-jsonld.js` has to build the _same_ graph the browser builds, and a validator that re-implemented the builder would be checking its own opinion. Node cannot load ESM `src/` files without a bundler, so the chain from `pageGraph` down to `formatPrice` is CommonJS. The set is exactly the transitive closure: nothing was converted that the scripts do not reach.
- 2026-09-17 — P38 — **`buildPageGraph` lives in `src/seo/pageGraph.js`, not inside `useSeoResolved`.** — It is the difference between what a record says about itself (`schema/index.js`) and what a page says: the publisher, the site node, the list the page shows, the questions it answers, the reviews on it. Both `<Seo>` and the Node validator call it, which is the only way the two can agree about a page they never see together.
- 2026-09-17 — P38 — **The page-type tables are `src/seo/pageTypes.js` (CommonJS, no imports); `components/seo/seoDefaults.js` re-exports them and adds `THEME_COLOR`.** — `theme-color` is a hex literal and §2.4/D17 allow one in `theme.js` and `global.css` only, so it is read from `theme.palette.primary.main`; a MUI theme is also the one thing a Node script cannot load, which is why the tables are a file below it.
- 2026-09-17 — P38 — **`<Seo>` has two title channels.** `title` is the page's own name and goes **through** the type's §9.5 template; `overrides.title` is the finished `<title>` and is used verbatim (still variable-resolved, as §9.3 requires). — Almost every page wants the first: "Localities in Bengaluru" should become "Localities in Bengaluru | Squares N Acres" and should follow the separator an editor changes in Admin → SEO. Only `ListingEngine` wants the second, because `listingSeo.js` has already applied the `listing` template to build its count-aware title.
- 2026-09-17 — P38 — **The index pages that are routes rather than records (`localities`, `builders`, `blog`, `faqs`, `jobs`, `shortlist`, `search`, `notFound`, `error`) get their fixed words from `pageTypes.INDEX_PAGES` and are marked `status: 'published'` on the synthetic record the engine resolves.** — Prompt 38 §4.4 suggests `copy.js`; those strings are head copy rather than page copy, and the page keeps its own visible `<h1>`. The publication flag is not cosmetic: without it `toSeoInput('page', …)` reads a record with no `status` as a draft, and `/localities` shipped `noindex` — found in the browser and now covered by a test.
- 2026-09-17 — P38 — **`seo/breadcrumbs.js` accepts and produces both `{ name, path }` and `{ label, to }`.** `<Breadcrumbs>` was written against the second and the route tables of prompts 26 and 31 still speak it; schema.org's `ListItem` speaks the first. — One trail per page, drawn and published from the same array (§9.3); translating at the two edges is cheaper than migrating every route table, and the last crumb carries no path either way because it is the page you are on.
- 2026-09-17 — P38 — **A `JobPosting` generator (`seo/schema/jobPosting.js`) and six new `<Seo type>`s — `localities`, `builders`, `blog`, `faqs`, `jobs`, `job`.** `publicPathFor` gained the `job` case. — §9.2 lists `jobs`/`job` and the four index pages are real routes with real titles; giving them a type is what lets the test table assert every page the site answers rather than the eight that happen to be records.
- 2026-09-17 — P38 — **`customHeadHtml` is parsed with `DOMParser` and handed to Helmet as elements; `customBodyEndHtml` is appended to `document.body` with real elements built by hand.** Both render scripts verbatim; RBAC (§7, prompt 37) restricts the two fields to `admin`. — Helmet takes children, not a string, and `dangerouslySetInnerHTML` never runs a `<script>` it writes, so a tag manager pasted into the body box would have been silently inert. The control on these fields is who may write them, not what they may say: a sanitised verification script is a script that does not verify.
- 2026-09-17 — P38 — **The JSON-LD graph does not go through Helmet; `components/seo/JsonLd.jsx` owns one `<script data-sna-jsonld>` in the head and rewrites its text in place.** — `react-helmet-async` treats `<script>` as a cumulative tag de-duplicated by its _content_, and registers each instance during `render()` rather than in an effect, so a render React starts and discards — which `React.lazy` + `<Suspense>` do on every route here — leaves an instance behind holding the props it was discarded with. The two together are visible: a listing page renders `<Seo>` once with no results and again with them, and the head carries **both** graphs. Measured against the production build on five routes, not only the dev server. Every other tag survives this because its de-duplication key is its name — a second `<meta name="description">` collapses onto the first — and only the graph is keyed by what it says. Covered by "keeps one script when two pages are mounted at once" in `Seo.test.jsx`.
- 2026-09-17 — P38 — **The analytics tags are not injected in the admin panel.** — `robots.txt` disallows `/admin` and no public page links to it (D24); an editor spending a day in the CMS would otherwise arrive in the client's analytics as their most engaged visitor.
- 2026-09-17 — P38 — **`check:links` and `check:jsonld` have two modes and say which one they ran.** With `CHROME_PATH` they open every sitemap URL in Chrome and check the rendered page (one `<h1>`, `<img alt>`, every internal link); without it they resolve the records behind those URLs through the shared CommonJS engine. The Node mode skips the routes that have no record — a listing index's head is assembled from filters and a count that only exist on a rendered page — and prints how many it skipped. — "We could not check" is a poor answer for a check meant to gate a release; judging a page from data it does not render from is a worse one.
- 2026-09-17 — P38 — **Both browser checks wait for a canonical **and** a JSON-LD script before reading the head, not for `networkidle0`.** — A detail page fetches its record after the bundle has settled and renders a skeleton in the meantime; read at `networkidle0` plus a frame, `/properties/:slug` reported a head with a title and nothing else. The canonical and the graph are what every page emits and no skeleton does.
- 2026-09-17 — P38 — **`public/index.html`'s title, description, icons, `theme-color` and `application-name` carry `data-rh="true"`.** — react-helmet-async replaces every tag carrying that attribute rather than adding a second copy beside it, so the static head — which is what the first paint, a crawler that runs no JavaScript and prompt 41's prerender see — is taken over cleanly instead of being duplicated.
- 2026-09-17 — P38 — **`/careers` stays `<Seo type="page">`; the `jobs` index type is defined but unused.** — Prompt 38 §4.4 asks for `type="jobs"`, and prompt 31 had already made `/careers` a CMS record: as a record it keeps its own `seo` branch, its draft state and its custom schema, all of which the index type would throw away — and the title and the trail come out identical either way. The type exists in `pageTypes.js` and is exercised by `Seo.test.jsx` so that a careers index that is _not_ a CMS page has one waiting.
- 2026-09-17 — P38 — **A listing is `"@type": ["RealEstateListing", "Apartment"]` rather than a `RealEstateListing` with an `about`.** — §9.3 describes it that way ("`RealEstateListing` (+ `Residence`/`Apartment`/`House`/`Place` per property type…)"), the master context wins over a prompt (§0), and it is what prompt 35's generator and its tests already did. Dual typing is also what Google's own real-estate examples use.
- 2026-09-17 — P38 — **The public pages that want a whole taxonomy ask for `perPage: 100`, not `perPage: 'all'`.** `TagCloud`, `ArticleIndex`, `ArticleTag` and `ArticleCategory` each declare a local `TAXONOMY_PER_PAGE = 100`. — §5.6 reserves `perPage=all` for admin routes, and `queryTranslate` honours that: a public route asking for it silently falls back to the default page size. The archive has 15 tags, so the tag rail, and the lookup that decides whether `/insights/articles/tag/:slug` is a 404, saw the first 12 — `stamp-duty`, `whitefield` and `sarjapur-road` were in the sitemap, linked from three article bodies, and rendered "Tag not found". 100 is the public cap and the figure `MasterDataContext` already uses for the same reason.
- 2026-09-17 — P38 — **`check:links` is not taught to forgive a link into unpublished content.** The two remaining failures (NEW-41) are left failing. — The alternative was a rule that says "a 404 whose slug has a record is only a warning", and the public API cannot tell a draft from a typo: §5.4 has it answer 404 for both, deliberately. Classifying them would have meant giving a crawler's link checker an admin token to ask a question a crawler cannot ask — which would make the script agree with the site's private state rather than with what a visitor gets. A published page linking to a 404 is a real defect whoever caused it; the fix belongs to the seed, and `db.json` is off-limits here (§12).
- 2026-09-18 — D12/P39 — **`cloudinaryUrl(url, { merge: true })` chains the new transformation _behind_ the one the URL already carries; without `merge` such a URL is still returned untouched.** `buildSrcSet`, `blurThumb` and `LazyImage` all merge. — Prompt 31 refused to touch a URL that already had a transformation, because inserting one _in front_ would decide the size before the crop narrowed it. Chaining after it is the other way round and is correct: the brand square (`iconSquareUrl`, a `c_crop`) becomes `…/upload/c_crop,…/f_auto,q_auto,w_320/…` — one `/upload/`, two components, the crop first. Every existing caller keeps prompt 31's behaviour byte for byte, which is why `merge` is opt-in rather than the default.
- 2026-09-18 — §8.6/P39 — **`buildSrcSet` writes `dpr_auto` as §8.6 and prompt 39 §4.1 both specify, and the `w` descriptors are what actually decide.** — `dpr_auto` only takes effect when the browser sends a `DPR` client hint, which needs a `Client-Hints` opt-in this site does not have; it is therefore inert today and harmless. Writing it is what the spec asks for and what a later prompt would have to add anyway if the hint is ever enabled.
- 2026-09-18 — §2.2/P39 — **`LazyImage` applies the box's ratio as a `c_fill` crop only when `fit="cover"`.** A `fit="contain"` box — a logo, a floor plan, the drawer preview — gets width-only variants with no `h_` and no `c_fill`. — Prompt 39 §2 says "`c_fill` with the box ratio when `ratio` is set", and `ratio` has a default, so the literal reading would crop _every_ image including the wordmark, which §2.2 forbids outright, and would cut the edges off a floor plan. "Fitted inside a box" and "cropped to fill a box" are different requests and only the second one has a crop in it.
- 2026-09-18 — P39 — **The media library's folders are a plain string, and `sna/<folder>` is what Cloudinary is told.** `cloudinaryFolder('properties')` → `sna/properties`; the record stores `properties`. — The prefix is one deployment's housekeeping and the record is the contract (§6.12): filtering, the datalist and the picker all read a name an editor typed, and a client who later moves the prefix does not have to rewrite 400 rows.
- 2026-09-18 — P39 — **Upload limits are images (jpg, jpeg, png, webp, avif, gif) ≤ 10 MB, videos (mp4, webm) ≤ 100 MB, documents (pdf, doc, docx) ≤ 10 MB, two files at a time, one automatic retry, then a Retry button.** — They are enforced in the browser because that is the only place they _can_ be: the binary never touches our API (D12), so an oversized file would otherwise be Cloudinary's problem to refuse several megabytes later. Two concurrent uploads keep a connection busy while leaving the progress bars readable, and a failure never holds up the files behind it.
- 2026-09-18 — P39 — **`DELETE /admin/media/:id` answers 409 with `data.usedIn` when a listing, article, page, locality, developer, team member, partner or the settings still shows the file; `?force=true` removes the record anyway.** The Cloudinary asset is never deleted either way, and the drawer says so. — The usage search is a `JSON.stringify().includes(url)` (`lib/usage.js`): good enough to warn, not good enough to overrule an editor who has read the list. Refusing outright would strand a wrong match for ever; deleting silently would strand eight listings pointing at a picture nobody can find again.
- 2026-09-18 — P39 — **`useMediaField` lives in `ImageField`, so wiring one component wired every image and file slot in the admin.** The picker is `React.lazy`-loaded from inside it, and a caller may still pass its own `onOpenMedia`/`onUpload` to override. — Fourteen call sites each remembering to open a dialog is fourteen chances to forget one; the hook returns `onUpload: undefined` when Cloudinary is not configured, which is exactly what `ImageField`'s prompt-13 contract already used to hide the button. The same reasoning put `onRequestImage` on `RichTextField` rather than on its nine hosts.
- 2026-09-18 — P39 — **A file slot that is not an image is an `ImageField` with `accept="document"` and `preview={false}`** (the brochure, a floor plan's PDF, every documents row). — The control is identical — a URL box, a Clear, and the same two ways in — and a second component would have been the first one with the preview deleted. What changes is what the picker offers, which is what `accept` is for.
- 2026-09-18 — P39 — **A picked file hands a form five fields (`toSelection`: `id`, `url`, `alt`, `title`, `width`, `height`), never the whole record.** A multiple selection arrives in the order the tiles were pressed. — A property image row, a page block and the editor's figure all want the same small thing; handing over `usedIn`, `tags` and `folder` would invite a form to store them. The gallery appends what it is given minus anything already there, by URL.
- 2026-09-18 — P39 — **Nothing in `src/` may `require()` an npm package from a CommonJS module; `import` it, or use the platform.** — Create React App's catch-all asset rule excludes `.js`, `.mjs`, `.jsx`, `.ts`, `.tsx`, `.html` and `.json` and nothing else, so a `require` that resolves to a `.cjs` entry is emitted as a **file** and the call returns its URL as a string. No build error, no runtime error — just an undefined import. `require('date-fns')` in `src/utils/format.js` (CommonJS since D36b, so `scripts/validate-jsonld.js` can read the chain) is what took the whole admin panel down with `formatDistanceToNowStrict is not a function`. `formatRelative` is now built on `Intl.RelativeTimeFormat('en-IN')`; the `date-fns` pin stays in `package.json` per §3.3, but a prompt that reaches for it from a CommonJS file in `src/` has to check what webpack resolved first.
- 2026-09-18 — P39 — **State that mirrors a prop is adjusted during render, not in an effect**, in `MediaEditDrawer`, `MediaPickerDialog` and `MediaUrlForm` (whose detected type is derived outright). — MUI mounts a drawer's or a dialog's contents when it opens, so the one frame an effect-based fill leaves empty is the frame the editor actually sees. React documents the render-phase adjustment for exactly this; it also makes the behaviour testable, which the effect version was not.
- 2026-09-18 — P39 — **`priority` on `LazyImage`/`Picture` marks a page's LCP image** — the property gallery's first slide, an article's featured image and its hero, the home hero, a locality hero, a builder hero — and means `loading="eager"` plus `fetchpriority="high"`. Everything else stays lazy. — One image per page: `fetchpriority="high"` on six images says nothing at all. It replaced the ad-hoc `loading="eager"` + `fetchPriority` pairs those six places each spelled out.
- 2026-09-18 — P39 — **The home hero is a `<picture>` (`Picture`), not a CSS background chosen by `useBreakpoint`.** — `hero.mobileImageUrl` beside `hero.backgroundImageUrl` (§6.13) is art direction, and a media query resolves it before the first paint while a JS breakpoint resolves it after — on the page's LCP image. A background image also cannot carry `srcset`, `fetchpriority` or an `alt`.
- 2026-09-18 — P40 — **The settings form validates against `schemas['settings.update']` itself, with four branches overridden** (`settingsSchema.js` builds the client descriptor from the contract rather than restating it): `general.establishedYear` gains 1900–current-year, `hero.stats`/`hero.badges` a maximum of four, `footer.columns` four with eight links each, `footer.galleryImageUrls` six, and the seven integration ids their formats. — A second copy of a 60-field descriptor drifts from the contract within two prompts; an override list is one screenful and cannot describe a field the contract does not have.
- 2026-09-18 — P40 — **The client is stricter than the API, never looser.** The integration-id patterns (`G-XXXXXXXXXX`, `GTM-XXXXXXX`, an 8–20-digit pixel id, `AIza…`, a Cloudinary cloud name and preset, a `6L…` reCAPTCHA site key) live only in `src/pages/admin/settings/settingsSchema.js`; the mock stores plain strings. — A mistyped measurement id does not fail, it silently collects nothing, so the browser is where it has to be caught; and because the rules only narrow what the API accepts, a body the form approves is always one the API takes (no mock change, §5).
- 2026-09-18 — P40 — **`normalizeSettings` sends the record the contract describes**: it drops every key the model does not declare, trims each string, and turns an empty optional box into `null`. — The `PUT` deep-merges known keys only, so a stale key would be dropped by the API anyway; doing it before the request keeps `''` out of fields typed `nullable` and makes “empty” mean one thing on both sides.
- 2026-09-18 — P40 — **The three phone boxes normalise to `+91 98765 43210` when the editor leaves them** (`formatIndianPhone`), and a number that is not an Indian mobile is left exactly as typed. — The footer, the contact page and the agent card print this string as it is stored, while `tel:` and `wa.me` targets are derived from it by `utils/format.js`; rewriting a number that is wrong would hide the error instead of reporting it.
- 2026-09-18 — P40 — **The tab a failed save opens is computed before the submit**, from `settingsErrors(form.values)`, plus the first field of a 422 recorded by the form's own `onSubmit` wrapper. — `form.errors` inside the click handler is a render behind the validation the submit runs, so reading it after the `await` opens whichever tab was wrong last time, or none at all.
- 2026-09-18 — D93/P40 — **A save calls `SiteSettingsContext.updateLocal(record)` and then `refresh()`**, and `updateLocal` strips the `leads` branch on the way in. — The context holds the public subset (§5.10) and its session cache is what the next public page paints from; the local write puts the new brand name in the header immediately and the refresh confirms it against the server's own copy. `refresh()` now answers with the new settings so a caller can act on them.
- 2026-09-18 — P40 — **`EVENTS.settingsChanged` (`settings:changed`) is emitted after a successful save**, carrying the record. — The screen has already refreshed the context; the event is for the caches that are not components — the Cloudinary configuration a media screen read, a preview built from the brand assets — the way `seo:changed` serves the SEO index.
- 2026-09-18 — D79/P40 — **The footer collage is drawn only from three pictures up**, enforced in `Footer.jsx`; the settings screen warns below three rather than refusing the save. — Two pictures in a six-cell strip is not a collage, and a half-filled gallery is a state an editor passes through on the way to filling it.
- 2026-09-18 — P40 — **The firm's own RERA and GST numbers are rendered in the footer's legal block.** — `general.reraNumber`/`gstNumber` (§6.13) had no reader on the public site: the property pages print a _listing's_ registration, not the firm's. A RERA registration is required to be displayed, and the settings field now says where it appears because it appears there.
- 2026-09-18 — P40 — **Clearing every hero search tab is legitimate and means “offer all five”** — `HeroSearch.resolveTabs` has always read an empty list that way — so the tab list has no “at least one” rule, and the panel says so.
- 2026-09-18 — P40 — **A drag inside a footer column's links is stopped from reaching the column list** (`onDragStart`/`onDrop` are not allowed to bubble out of the nested `SortableList`). — Both lists are draggable; without it one gesture moves a link _and_ the column that holds it.
