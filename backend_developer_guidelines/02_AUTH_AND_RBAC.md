# 2. Authentication and roles

> Generated by `npm run generate:backend-guidelines` — do not edit.
> The matrices come from `src/config/rbac.js` and `src/services/endpoints.js`;
> the prose from `docs/backend-notes/02_auth.md`.
> generatedFrom: dfe0f41

Three roles, one token type, and a matrix that is enforced three times: in the
admin panel's routing, in its navigation, and — the only one that counts — in
the API.

---

## Token flow

Authentication is **Laravel Sanctum personal access tokens** — not the SPA cookie
mode. The site and the API are separate origins, the admin panel is a static
bundle, and a bearer token in a header needs neither a CSRF dance nor a shared
cookie domain.

```
POST /api/auth/login  { email, password }
        │
        ├─ user not found, inactive, or password mismatch
        │     └─→ 422  { message, errors: { email: ['These credentials do not match our records.'] } }
        │
        └─ match
              ├─ $user->tokens()->where('name', 'admin-panel')->delete()   // one session per account
              ├─ $token = $user->createToken('admin-panel', ['*'], now()->addHours(24))
              ├─ $user->forceFill(['last_login_at' => now()])->save()
              └─→ 200 { data: { token, expiresAt, user: { id, name, email, role, avatarUrl, phone } } }

Every later call
        Authorization: Bearer <token>
        │
        ├─ unknown / revoked / expired token  →  401 { message: 'Unauthenticated.' }
        ├─ known token, role not allowed      →  403 { message: 'You do not have permission to perform this action.' }
        └─ known token, role allowed          →  the handler runs, with $request->user() set

POST /api/auth/logout
        └─ $request->user()->currentAccessToken()->delete()  →  200 { data: null, message: 'Signed out.' }
```

Notes that matter:

- `expiresAt` is part of the login response because the client enforces expiry
  too: it stores `sna_auth_token`, `sna_auth_user` and `sna_auth_expires_at`, sets
  a timer, and signs the user out with a toast the moment the clock runs out. Send
  it as ISO-8601 UTC, the same instant the token record carries.
- A `401` from **any** endpoint makes the frontend drop its stored session and
  redirect to the login screen, so never answer 401 for an authorisation failure —
  that is what `403` is for.
- The user object of the login response and of `GET /auth/profile` is the same
  shape. Return it from one API resource so the two cannot drift.
- `PUT /auth/password` answers `422` with `errors.currentPassword` when the
  current password is wrong, and requires at least 8 characters **with a letter
  and a digit** for the new one (`errors.newPassword`: "The newPassword must
  contain at least one letter and one digit."). Revoke every other token of that
  user on success.
- The same rule holds wherever a password is set (QA-64): `POST /admin/users`
  and a `PUT` or `PATCH /admin/users/:id` that carries `password` answer `422`
  on `password` ("The password must contain at least one letter and one
  digit.") for eight letters or eight digits. A password set that way — an
  administrator's reset — revokes **every** token of that account, except the
  caller's own when an administrator resets their own password.

### Signing in

```http
POST /auth/login
Accept: application/json
Content-Type: application/json

{
  "email": "admin@squaresnacres.com",
  "password": "Admin@123"
}
```

**200** `application/json`

```json
{
  "data": {
    "token": "3f9a1c7e5b2d8a6f4c0e9b3d7a5f1c8e2b6d4a0f9c3e7b5d",
    "expiresAt": "2026-01-15T09:30:00.000Z",
    "user": {
      "id": 1,
      "name": "Admin User",
      "email": "admin@squaresnacres.com",
      "role": "admin",
      "avatarUrl": null,
      "phone": "9880000010"
    }
  }
}
```

### Reading the session back

```http
GET /auth/profile
Accept: application/json
Authorization: Bearer <a sales token>
```

**200** `application/json`

```json
{
  "data": {
    "id": 3,
    "name": "Sales User",
    "email": "sales@squaresnacres.com",
    "role": "sales",
    "phone": "9880000012",
    "avatarUrl": null,
    "isActive": true,
    "lastLoginAt": "2026-01-15T09:30:00.000Z",
    "createdAt": "2026-01-15T09:30:00.000Z",
    "updatedAt": "2026-01-15T09:30:00.000Z"
  }
}
```

### Being refused

A role that may not do something gets `403` and this body. Never `401` — the
frontend drops the stored session on any `401` and sends the user back to the
login screen, so a 401 in place of a 403 signs a working user out.

```json
{
  "message": "You do not have permission to perform this action."
}
```

---

## Token lifetime

The mock expires a token after `MOCK_TOKEN_TTL_HOURS`, default **24 hours**.
Match it in Laravel:

```php
// config/sanctum.php
'expiration' => env('SANCTUM_TOKEN_TTL_MINUTES', 1440),   // 24 hours
```

Schedule `sanctum:prune-expired --hours=48` daily so the token table does not
grow without bound. Twenty-four hours is a deliberate compromise: long enough
that a working day needs one sign-in, short enough that a token copied out of a
laptop stops working overnight. Changing it is a one-line config change and
needs no frontend release — the client reads the expiry the server sends.

---

## The role matrix

`PERMISSIONS[area][action] = [roles]` in `src/config/rbac.js` is the source. The
admin panel asks `can('leads', 'export')`; the API must answer the same question
for the request it is serving.

| Area         | Action         | `admin` | `manager` | `sales` |
| ------------ | -------------- | ------- | --------- | ------- |
| `dashboard`  | `view`         | yes     | yes       | yes     |
| `properties` | `view`         | yes     | yes       | yes     |
| `properties` | `create`       | yes     | yes       | —       |
| `properties` | `edit`         | yes     | yes       | —       |
| `properties` | `delete`       | yes     | yes       | —       |
| `properties` | `bulk`         | yes     | yes       | —       |
| `properties` | `duplicate`    | yes     | yes       | —       |
| `masterData` | `view`         | yes     | yes       | —       |
| `masterData` | `create`       | yes     | yes       | —       |
| `masterData` | `edit`         | yes     | yes       | —       |
| `masterData` | `delete`       | yes     | yes       | —       |
| `leads`      | `view`         | yes     | yes       | yes     |
| `leads`      | `edit`         | yes     | yes       | yes     |
| `leads`      | `claim`        | —       | —         | yes     |
| `leads`      | `assign`       | yes     | yes       | —       |
| `leads`      | `delete`       | yes     | yes       | —       |
| `leads`      | `bulk`         | yes     | yes       | —       |
| `leads`      | `export`       | yes     | yes       | yes     |
| `articles`   | `view`         | yes     | yes       | —       |
| `articles`   | `create`       | yes     | yes       | —       |
| `articles`   | `edit`         | yes     | yes       | —       |
| `articles`   | `delete`       | yes     | yes       | —       |
| `articles`   | `bulk`         | yes     | yes       | —       |
| `content`    | `view`         | yes     | yes       | —       |
| `content`    | `create`       | yes     | yes       | —       |
| `content`    | `edit`         | yes     | yes       | —       |
| `content`    | `delete`       | yes     | yes       | —       |
| `content`    | `bulk`         | yes     | yes       | —       |
| `seo`        | `view`         | yes     | yes       | —       |
| `seo`        | `create`       | yes     | yes       | —       |
| `seo`        | `edit`         | yes     | yes       | —       |
| `seo`        | `delete`       | yes     | yes       | —       |
| `seo`        | `bulk`         | yes     | yes       | —       |
| `media`      | `view`         | yes     | yes       | —       |
| `media`      | `create`       | yes     | yes       | —       |
| `media`      | `edit`         | yes     | yes       | —       |
| `media`      | `delete`       | yes     | yes       | —       |
| `media`      | `bulk`         | yes     | yes       | —       |
| `settings`   | `view`         | yes     | yes       | —       |
| `settings`   | `edit`         | yes     | —         | —       |
| `users`      | `list`         | yes     | yes       | —       |
| `users`      | _every action_ | yes     | —         | —       |
| `profile`    | _every action_ | yes     | yes       | yes     |

### Admin routes

What the admin panel lets each role open. It is here so the API and the panel
can be checked against one another; the API's own answer is the table after it.

| Admin route             | `admin` | `manager` | `sales` |
| ----------------------- | ------- | --------- | ------- |
| `/admin/dashboard`      | yes     | yes       | yes     |
| `/admin/properties`     | yes     | yes       | yes     |
| `/admin/leads`          | yes     | yes       | yes     |
| `/admin/articles`       | yes     | yes       | —       |
| `/admin/pages`          | yes     | yes       | —       |
| `/admin/faqs`           | yes     | yes       | —       |
| `/admin/master-data`    | yes     | yes       | —       |
| `/admin/testimonials`   | yes     | yes       | —       |
| `/admin/team`           | yes     | yes       | —       |
| `/admin/partners`       | yes     | yes       | —       |
| `/admin/jobs`           | yes     | yes       | —       |
| `/admin/newsletter`     | yes     | yes       | —       |
| `/admin/media`          | yes     | yes       | —       |
| `/admin/seo`            | yes     | yes       | —       |
| `/admin/settings`       | yes     | yes       | —       |
| `/admin/settings/users` | yes     | —         | —       |
| `/admin/profile`        | yes     | yes       | yes     |

### Every admin endpoint

Generated by resolving each registry path through the same
`path + method → area.action` rules the mock uses, then reading the matrix
above. This is the table to implement against.

| Endpoint                                   | Permission             | `admin` | `manager` | `sales` |
| ------------------------------------------ | ---------------------- | ------- | --------- | ------- |
| `GET /admin/dashboard`                     | `dashboard.view`       | yes     | yes       | yes     |
| `GET /admin/properties`                    | `properties.view`      | yes     | yes       | yes     |
| `POST /admin/properties`                   | `properties.create`    | yes     | yes       | —       |
| `GET /admin/properties/:id`                | `properties.view`      | yes     | yes       | yes     |
| `PUT /admin/properties/:id`                | `properties.edit`      | yes     | yes       | —       |
| `PATCH /admin/properties/:id`              | `properties.edit`      | yes     | yes       | —       |
| `DELETE /admin/properties/:id`             | `properties.delete`    | yes     | yes       | —       |
| `POST /admin/properties/bulk`              | `properties.bulk`      | yes     | yes       | —       |
| `GET /admin/properties/check-slug`         | `properties.create`    | yes     | yes       | —       |
| `GET /admin/properties/slug/:slug`         | `properties.view`      | yes     | yes       | yes     |
| `POST /admin/properties/:id/duplicate`     | `properties.duplicate` | yes     | yes       | —       |
| `GET /admin/leads`                         | `leads.view`           | yes     | yes       | yes     |
| `GET /admin/leads/:id`                     | `leads.view`           | yes     | yes       | yes     |
| `PATCH /admin/leads/:id`                   | `leads.edit`           | yes     | yes       | yes     |
| `DELETE /admin/leads/:id`                  | `leads.delete`         | yes     | yes       | —       |
| `POST /admin/leads/:id/claim`              | `leads.edit`           | yes     | yes       | yes     |
| `POST /admin/leads/:id/notes`              | `leads.edit`           | yes     | yes       | yes     |
| `DELETE /admin/leads/:id/notes/:noteId`    | `leads.edit`           | yes     | yes       | yes     |
| `GET /admin/leads/export`                  | `leads.export`         | yes     | yes       | yes     |
| `POST /admin/leads/bulk`                   | `leads.bulk`           | yes     | yes       | —       |
| `GET /admin/localities`                    | `masterData.view`      | yes     | yes       | —       |
| `POST /admin/localities`                   | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/localities/:id`                | `masterData.view`      | yes     | yes       | —       |
| `PUT /admin/localities/:id`                | `masterData.edit`      | yes     | yes       | —       |
| `PATCH /admin/localities/:id`              | `masterData.edit`      | yes     | yes       | —       |
| `DELETE /admin/localities/:id`             | `masterData.delete`    | yes     | yes       | —       |
| `POST /admin/localities/bulk`              | `masterData.edit`      | yes     | yes       | —       |
| `GET /admin/localities/check-slug`         | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/cities`                        | `masterData.view`      | yes     | yes       | —       |
| `POST /admin/cities`                       | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/cities/:id`                    | `masterData.view`      | yes     | yes       | —       |
| `PUT /admin/cities/:id`                    | `masterData.edit`      | yes     | yes       | —       |
| `PATCH /admin/cities/:id`                  | `masterData.edit`      | yes     | yes       | —       |
| `DELETE /admin/cities/:id`                 | `masterData.delete`    | yes     | yes       | —       |
| `POST /admin/cities/bulk`                  | `masterData.edit`      | yes     | yes       | —       |
| `GET /admin/cities/check-slug`             | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/segments`                      | `masterData.view`      | yes     | yes       | —       |
| `POST /admin/segments`                     | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/segments/:id`                  | `masterData.view`      | yes     | yes       | —       |
| `PUT /admin/segments/:id`                  | `masterData.edit`      | yes     | yes       | —       |
| `PATCH /admin/segments/:id`                | `masterData.edit`      | yes     | yes       | —       |
| `DELETE /admin/segments/:id`               | `masterData.delete`    | yes     | yes       | —       |
| `POST /admin/segments/bulk`                | `masterData.edit`      | yes     | yes       | —       |
| `GET /admin/segments/check-slug`           | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/property-types`                | `masterData.view`      | yes     | yes       | —       |
| `POST /admin/property-types`               | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/property-types/:id`            | `masterData.view`      | yes     | yes       | —       |
| `PUT /admin/property-types/:id`            | `masterData.edit`      | yes     | yes       | —       |
| `PATCH /admin/property-types/:id`          | `masterData.edit`      | yes     | yes       | —       |
| `DELETE /admin/property-types/:id`         | `masterData.delete`    | yes     | yes       | —       |
| `POST /admin/property-types/bulk`          | `masterData.edit`      | yes     | yes       | —       |
| `GET /admin/property-types/check-slug`     | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/amenities`                     | `masterData.view`      | yes     | yes       | —       |
| `POST /admin/amenities`                    | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/amenities/:id`                 | `masterData.view`      | yes     | yes       | —       |
| `PUT /admin/amenities/:id`                 | `masterData.edit`      | yes     | yes       | —       |
| `PATCH /admin/amenities/:id`               | `masterData.edit`      | yes     | yes       | —       |
| `DELETE /admin/amenities/:id`              | `masterData.delete`    | yes     | yes       | —       |
| `POST /admin/amenities/bulk`               | `masterData.edit`      | yes     | yes       | —       |
| `GET /admin/amenities/check-slug`          | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/badges`                        | `masterData.view`      | yes     | yes       | —       |
| `POST /admin/badges`                       | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/badges/:id`                    | `masterData.view`      | yes     | yes       | —       |
| `PUT /admin/badges/:id`                    | `masterData.edit`      | yes     | yes       | —       |
| `PATCH /admin/badges/:id`                  | `masterData.edit`      | yes     | yes       | —       |
| `DELETE /admin/badges/:id`                 | `masterData.delete`    | yes     | yes       | —       |
| `POST /admin/badges/bulk`                  | `masterData.edit`      | yes     | yes       | —       |
| `GET /admin/badges/check-slug`             | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/developers`                    | `masterData.view`      | yes     | yes       | —       |
| `POST /admin/developers`                   | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/developers/:id`                | `masterData.view`      | yes     | yes       | —       |
| `PUT /admin/developers/:id`                | `masterData.edit`      | yes     | yes       | —       |
| `PATCH /admin/developers/:id`              | `masterData.edit`      | yes     | yes       | —       |
| `DELETE /admin/developers/:id`             | `masterData.delete`    | yes     | yes       | —       |
| `POST /admin/developers/bulk`              | `masterData.edit`      | yes     | yes       | —       |
| `GET /admin/developers/check-slug`         | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/banks`                         | `masterData.view`      | yes     | yes       | —       |
| `POST /admin/banks`                        | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/banks/:id`                     | `masterData.view`      | yes     | yes       | —       |
| `PUT /admin/banks/:id`                     | `masterData.edit`      | yes     | yes       | —       |
| `PATCH /admin/banks/:id`                   | `masterData.edit`      | yes     | yes       | —       |
| `DELETE /admin/banks/:id`                  | `masterData.delete`    | yes     | yes       | —       |
| `POST /admin/banks/bulk`                   | `masterData.edit`      | yes     | yes       | —       |
| `GET /admin/banks/check-slug`              | `masterData.create`    | yes     | yes       | —       |
| `GET /admin/articles`                      | `articles.view`        | yes     | yes       | —       |
| `POST /admin/articles`                     | `articles.create`      | yes     | yes       | —       |
| `GET /admin/articles/:id`                  | `articles.view`        | yes     | yes       | —       |
| `PUT /admin/articles/:id`                  | `articles.edit`        | yes     | yes       | —       |
| `PATCH /admin/articles/:id`                | `articles.edit`        | yes     | yes       | —       |
| `DELETE /admin/articles/:id`               | `articles.delete`      | yes     | yes       | —       |
| `POST /admin/articles/bulk`                | `articles.bulk`        | yes     | yes       | —       |
| `GET /admin/articles/check-slug`           | `articles.create`      | yes     | yes       | —       |
| `GET /admin/articles/:id/preview-token`    | `articles.view`        | yes     | yes       | —       |
| `GET /admin/article-categories`            | `articles.view`        | yes     | yes       | —       |
| `POST /admin/article-categories`           | `articles.create`      | yes     | yes       | —       |
| `GET /admin/article-categories/:id`        | `articles.view`        | yes     | yes       | —       |
| `PUT /admin/article-categories/:id`        | `articles.edit`        | yes     | yes       | —       |
| `PATCH /admin/article-categories/:id`      | `articles.edit`        | yes     | yes       | —       |
| `DELETE /admin/article-categories/:id`     | `articles.delete`      | yes     | yes       | —       |
| `POST /admin/article-categories/bulk`      | `articles.bulk`        | yes     | yes       | —       |
| `GET /admin/article-categories/check-slug` | `articles.create`      | yes     | yes       | —       |
| `GET /admin/article-tags`                  | `articles.view`        | yes     | yes       | —       |
| `POST /admin/article-tags`                 | `articles.create`      | yes     | yes       | —       |
| `GET /admin/article-tags/:id`              | `articles.view`        | yes     | yes       | —       |
| `PUT /admin/article-tags/:id`              | `articles.edit`        | yes     | yes       | —       |
| `PATCH /admin/article-tags/:id`            | `articles.edit`        | yes     | yes       | —       |
| `DELETE /admin/article-tags/:id`           | `articles.delete`      | yes     | yes       | —       |
| `POST /admin/article-tags/bulk`            | `articles.bulk`        | yes     | yes       | —       |
| `GET /admin/article-tags/check-slug`       | `articles.create`      | yes     | yes       | —       |
| `GET /admin/authors`                       | `articles.view`        | yes     | yes       | —       |
| `POST /admin/authors`                      | `articles.create`      | yes     | yes       | —       |
| `GET /admin/authors/:id`                   | `articles.view`        | yes     | yes       | —       |
| `PUT /admin/authors/:id`                   | `articles.edit`        | yes     | yes       | —       |
| `PATCH /admin/authors/:id`                 | `articles.edit`        | yes     | yes       | —       |
| `DELETE /admin/authors/:id`                | `articles.delete`      | yes     | yes       | —       |
| `POST /admin/authors/bulk`                 | `articles.bulk`        | yes     | yes       | —       |
| `GET /admin/authors/check-slug`            | `articles.create`      | yes     | yes       | —       |
| `GET /admin/faqs`                          | `content.view`         | yes     | yes       | —       |
| `POST /admin/faqs`                         | `content.create`       | yes     | yes       | —       |
| `GET /admin/faqs/:id`                      | `content.view`         | yes     | yes       | —       |
| `PUT /admin/faqs/:id`                      | `content.edit`         | yes     | yes       | —       |
| `PATCH /admin/faqs/:id`                    | `content.edit`         | yes     | yes       | —       |
| `DELETE /admin/faqs/:id`                   | `content.delete`       | yes     | yes       | —       |
| `POST /admin/faqs/bulk`                    | `content.bulk`         | yes     | yes       | —       |
| `GET /admin/testimonials`                  | `content.view`         | yes     | yes       | —       |
| `POST /admin/testimonials`                 | `content.create`       | yes     | yes       | —       |
| `GET /admin/testimonials/:id`              | `content.view`         | yes     | yes       | —       |
| `PUT /admin/testimonials/:id`              | `content.edit`         | yes     | yes       | —       |
| `PATCH /admin/testimonials/:id`            | `content.edit`         | yes     | yes       | —       |
| `DELETE /admin/testimonials/:id`           | `content.delete`       | yes     | yes       | —       |
| `POST /admin/testimonials/bulk`            | `content.bulk`         | yes     | yes       | —       |
| `GET /admin/team`                          | `content.view`         | yes     | yes       | —       |
| `POST /admin/team`                         | `content.create`       | yes     | yes       | —       |
| `GET /admin/team/:id`                      | `content.view`         | yes     | yes       | —       |
| `PUT /admin/team/:id`                      | `content.edit`         | yes     | yes       | —       |
| `PATCH /admin/team/:id`                    | `content.edit`         | yes     | yes       | —       |
| `DELETE /admin/team/:id`                   | `content.delete`       | yes     | yes       | —       |
| `POST /admin/team/bulk`                    | `content.bulk`         | yes     | yes       | —       |
| `GET /admin/team/check-slug`               | `content.create`       | yes     | yes       | —       |
| `GET /admin/partners`                      | `content.view`         | yes     | yes       | —       |
| `POST /admin/partners`                     | `content.create`       | yes     | yes       | —       |
| `GET /admin/partners/:id`                  | `content.view`         | yes     | yes       | —       |
| `PUT /admin/partners/:id`                  | `content.edit`         | yes     | yes       | —       |
| `PATCH /admin/partners/:id`                | `content.edit`         | yes     | yes       | —       |
| `DELETE /admin/partners/:id`               | `content.delete`       | yes     | yes       | —       |
| `POST /admin/partners/bulk`                | `content.bulk`         | yes     | yes       | —       |
| `GET /admin/pages`                         | `content.view`         | yes     | yes       | —       |
| `POST /admin/pages`                        | `content.create`       | yes     | yes       | —       |
| `GET /admin/pages/:id`                     | `content.view`         | yes     | yes       | —       |
| `PUT /admin/pages/:id`                     | `content.edit`         | yes     | yes       | —       |
| `PATCH /admin/pages/:id`                   | `content.edit`         | yes     | yes       | —       |
| `DELETE /admin/pages/:id`                  | `content.delete`       | yes     | yes       | —       |
| `POST /admin/pages/bulk`                   | `content.bulk`         | yes     | yes       | —       |
| `GET /admin/pages/check-slug`              | `content.create`       | yes     | yes       | —       |
| `GET /admin/pages/:id/preview-token`       | `content.view`         | yes     | yes       | —       |
| `GET /admin/header-menus`                  | `content.view`         | yes     | yes       | —       |
| `POST /admin/header-menus`                 | `content.create`       | yes     | yes       | —       |
| `GET /admin/header-menus/:id`              | `content.view`         | yes     | yes       | —       |
| `PUT /admin/header-menus/:id`              | `content.edit`         | yes     | yes       | —       |
| `PATCH /admin/header-menus/:id`            | `content.edit`         | yes     | yes       | —       |
| `DELETE /admin/header-menus/:id`           | `content.delete`       | yes     | yes       | —       |
| `POST /admin/header-menus/bulk`            | `content.bulk`         | yes     | yes       | —       |
| `GET /admin/header-menus/check-slug`       | `content.create`       | yes     | yes       | —       |
| `GET /admin/jobs`                          | `content.view`         | yes     | yes       | —       |
| `POST /admin/jobs`                         | `content.create`       | yes     | yes       | —       |
| `GET /admin/jobs/:id`                      | `content.view`         | yes     | yes       | —       |
| `PUT /admin/jobs/:id`                      | `content.edit`         | yes     | yes       | —       |
| `PATCH /admin/jobs/:id`                    | `content.edit`         | yes     | yes       | —       |
| `DELETE /admin/jobs/:id`                   | `content.delete`       | yes     | yes       | —       |
| `POST /admin/jobs/bulk`                    | `content.bulk`         | yes     | yes       | —       |
| `GET /admin/jobs/check-slug`               | `content.create`       | yes     | yes       | —       |
| `GET /admin/job-applications`              | `content.view`         | yes     | yes       | —       |
| `PATCH /admin/job-applications/:id`        | `content.edit`         | yes     | yes       | —       |
| `DELETE /admin/job-applications/:id`       | `content.delete`       | yes     | yes       | —       |
| `GET /admin/newsletter-subscribers`        | `content.view`         | yes     | yes       | —       |
| `DELETE /admin/newsletter-subscribers/:id` | `content.delete`       | yes     | yes       | —       |
| `GET /admin/newsletter-subscribers/export` | `content.view`         | yes     | yes       | —       |
| `GET /admin/media`                         | `media.view`           | yes     | yes       | —       |
| `POST /admin/media`                        | `media.create`         | yes     | yes       | —       |
| `GET /admin/media/:id`                     | `media.view`           | yes     | yes       | —       |
| `PUT /admin/media/:id`                     | `media.edit`           | yes     | yes       | —       |
| `PATCH /admin/media/:id`                   | `media.edit`           | yes     | yes       | —       |
| `DELETE /admin/media/:id`                  | `media.delete`         | yes     | yes       | —       |
| `POST /admin/media/bulk`                   | `media.bulk`           | yes     | yes       | —       |
| `GET /admin/redirects`                     | `seo.view`             | yes     | yes       | —       |
| `POST /admin/redirects`                    | `seo.create`           | yes     | yes       | —       |
| `GET /admin/redirects/:id`                 | `seo.view`             | yes     | yes       | —       |
| `PUT /admin/redirects/:id`                 | `seo.edit`             | yes     | yes       | —       |
| `PATCH /admin/redirects/:id`               | `seo.edit`             | yes     | yes       | —       |
| `DELETE /admin/redirects/:id`              | `seo.delete`           | yes     | yes       | —       |
| `POST /admin/redirects/bulk`               | `seo.bulk`             | yes     | yes       | —       |
| `POST /admin/redirects/import`             | `seo.create`           | yes     | yes       | —       |
| `GET /admin/redirects/export`              | `seo.view`             | yes     | yes       | —       |
| `GET /admin/seo/settings`                  | `seo.view`             | yes     | yes       | —       |
| `PUT /admin/seo/settings`                  | `seo.edit`             | yes     | yes       | —       |
| `GET /admin/seo/llms-preview`              | `seo.view`             | yes     | yes       | —       |
| `GET /admin/seo/overview`                  | `seo.view`             | yes     | yes       | —       |
| `GET /admin/settings`                      | `settings.view`        | yes     | yes       | —       |
| `PUT /admin/settings`                      | `settings.edit`        | yes     | —         | —       |
| `GET /admin/users`                         | `users.list`           | yes     | yes       | —       |
| `POST /admin/users`                        | `users.create`         | yes     | —         | —       |
| `GET /admin/users/:id`                     | `users.list`           | yes     | yes       | —       |
| `PUT /admin/users/:id`                     | `users.edit`           | yes     | —         | —       |
| `PATCH /admin/users/:id`                   | `users.edit`           | yes     | —         | —       |
| `DELETE /admin/users/:id`                  | `users.delete`         | yes     | —         | —       |
| `POST /admin/users/bulk`                   | `users.bulk`           | yes     | —         | —       |

---

## Seed accounts

| Name         | E-mail                      | Password (mock only) | Role      |
| ------------ | --------------------------- | -------------------- | --------- |
| Admin User   | `admin@squaresnacres.com`   | `Admin@123`          | `admin`   |
| Manager User | `manager@squaresnacres.com` | `Manager@123`        | `manager` |
| Sales User   | `sales@squaresnacres.com`   | `Sales@123`          | `sales`   |

`db.json` ships three accounts so the mock is usable out of the box:

| E-mail                      | Password      | Role      |
| --------------------------- | ------------- | --------- |
| `admin@squaresnacres.com`   | `Admin@123`   | `admin`   |
| `manager@squaresnacres.com` | `Manager@123` | `manager` |
| `sales@squaresnacres.com`   | `Sales@123`   | `sales`   |

They are **plaintext in the seed file and only there**. The mock compares them
directly because it is a development server on a developer's machine.

On import into MySQL, hash them:

```php
DB::table('admin_users')->get()->each(function ($user) {
    DB::table('admin_users')->where('id', $user->id)
        ->update(['password' => Hash::make($user->password)]);
});
```

and on the first deployment of a real environment, **rotate all three**:

```bash
php artisan tinker
>>> User::where('email', 'admin@squaresnacres.com')->update(['password' => Hash::make('<a new password>')]);
```

The three seed passwords are published in this handover package, in the
repository, and in the mock's README. Treat them as compromised from the moment
the API is reachable from anything but `localhost`. The go-live checklist of
`07_DEPLOYMENT.md` will not let you past that step.

`password` is never returned by any endpoint. It carries `secret: true` in the
model descriptor, and the API resource for a user must not include it — not even
hashed.

---

## Sales scoping

A sales user sees the leads **assigned to them or unassigned**, and nothing else.
The rule is enforced on the server; the frontend hides what it is not allowed to
show, but that is decoration.

```sql
-- every read, export and aggregate a sales user makes
SELECT *
  FROM leads
 WHERE deleted_at IS NULL
   AND (assigned_to = :user_id OR assigned_to IS NULL)
```

In Eloquent, one scope used by every lead query:

```php
public function scopeVisibleTo(Builder $query, User $user): Builder
{
    if ($user->role === 'sales') {
        return $query->where(fn ($q) => $q->where('assigned_to', $user->id)
                                          ->orWhereNull('assigned_to'));
    }

    return $query;   // admin and manager see everything
}
```

It applies to more than the list:

| Call                                    | What scoping means                                                                                                                                                                      |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /admin/leads`                      | the `WHERE` above, and `meta.total` counts only the visible rows                                                                                                                        |
| `GET /admin/leads/:id`                  | a lead outside the scope is **404**, never 403 — a sales user must not learn that a lead exists                                                                                         |
| `PATCH /admin/leads/:id`                | same 404; and a sales user may not set `assignedTo` to anybody but themselves                                                                                                           |
| `POST /admin/leads/:id/claim`           | allowed only while `assigned_to IS NULL`; sets it to the caller                                                                                                                         |
| `DELETE /admin/leads/:id`               | **403** for sales, whatever the scope says                                                                                                                                              |
| `GET /admin/leads/export`               | the same `WHERE`, so the CSV holds the visible rows only                                                                                                                                |
| `DELETE /admin/leads/:id/notes/:noteId` | a sales user may withdraw a note they wrote (`created_by = :user_id`); anybody else's is 403                                                                                            |
| `GET /admin/dashboard`                  | every **lead** figure is scoped — totals, the 30-day trend, the source and status breakdowns, the recent list and the follow-ups. Property, article and SEO figures stay global (§6.16) |

The one thing scoping must not do is leak through a count: a sales user who sees
`leadsTotal: 312` while their list shows 41 rows has been told how much of the
pipeline is hidden from them.

---

## Endpoints that need no token

Everything the public site calls. None of these may leak a private field — see
§5.10 of `01_API_CONTRACT.md` for the list of what is stripped.

| Endpoint                                | Purpose                                          |
| --------------------------------------- | ------------------------------------------------ |
| `GET /properties`                       | Public paginated property search with facets     |
| `GET /properties/featured`              | Featured properties, ordered by priority then recency |
| `GET /properties/slug/:slug`            | Property details by slug; 404 when inactive      |
| `GET /properties/:id/similar`           | Admin-selected similar properties, topped up to six by locality and type |
| `POST /properties/:id/view`             | Count one property view; debounced per IP per hour |
| `POST /properties/:id/documents/access` | The addresses of a listing’s files, for the token POST /leads answered a lead about it with |
| `GET /properties/suggestions`           | Type-ahead suggestions for the hero and header search |
| `GET /localities`                       | Localities with their active property count      |
| `GET /localities/slug/:slug`            | Locality guide page by slug                      |
| `GET /cities`                           | Cities the portal covers                         |
| `GET /developers`                       | Developers with their active property count      |
| `GET /developers/slug/:slug`            | Developer page by slug                           |
| `GET /segments`                         | Every segment, the inactive ones flagged — the layout of a listing filed under one depends on its kind |
| `GET /property-types`                   | Property types, optionally filtered by segment   |
| `GET /amenities`                        | Amenities, optionally filtered by category       |
| `GET /badges`                           | Property badges                                  |
| `GET /banks`                            | Home-loan partners for the finance section and the EMI calculator |
| `GET /articles`                         | Published articles, newest or most read first    |
| `GET /articles/slug/:slug`              | Article by slug; a matching preview token also returns drafts |
| `GET /articles/trending`                | The six most read published articles             |
| `GET /articles/:id/adjacent`            | The published articles either side of this one by publishedAt |
| `GET /article-categories`               | Article categories with their published article count |
| `GET /article-tags`                     | Article tags with their published article count  |
| `GET /authors`                          | Active authors, public fields only               |
| `GET /authors/slug/:slug`               | Author page by slug                              |
| `GET /faqs`                             | FAQs for the FAQ page, the home section and property pages |
| `GET /testimonials`                     | Active testimonials                              |
| `GET /team`                             | Team members shown on the About page             |
| `GET /partners`                         | Partner logos, optionally filtered by category   |
| `GET /pages`                            | Published pages that belong in the header or footer navigation |
| `GET /pages/slug/:slug`                 | Published CMS page by slug; a matching preview token also returns drafts |
| `GET /header-menus`                     | The active header menus, left to right, with their submenus and links |
| `GET /jobs`                             | Open job postings                                |
| `GET /jobs/slug/:slug`                  | Job posting by slug                              |
| `POST /jobs/:id/apply`                  | Apply for a job posting; honeypot and rate limited |
| `POST /leads`                           | Capture a lead from any form on the site; honeypot and rate limited |
| `POST /newsletter/subscribe`            | Subscribe an e-mail address; a duplicate answers 200 instead of 409 |
| `GET /settings`                         | The public subset of the site settings           |
| `GET /seo/settings`                     | The public subset of the SEO settings used by <Seo> and the sitemap |
| `GET /redirects`                        | Active redirects, resolved client-side by RedirectHandler |
| `GET /redirects/resolve`                | The active rule for one path, or 404; the only place hits are counted |
| `GET /sitemap.xml`                      | Sitemap index listing every sub-sitemap          |
| `GET /sitemap-properties.xml`           | Property URLs with lastmod, changefreq, priority and image entries |
| `GET /sitemap-localities.xml`           | Locality URLs                                    |
| `GET /sitemap-developers.xml`           | Developer URLs                                   |
| `GET /sitemap-articles.xml`             | Article, category, tag and author URLs           |
| `GET /sitemap-pages.xml`                | CMS page URLs                                    |
| `GET /robots.txt`                       | robots.txt from seoSettings.robotsTxt            |
| `GET /rss.xml`                          | RSS feed of the latest twenty published articles |
| `GET /llms.txt`                         | llms.txt from seoSettings.llmsTxt                |
| `POST /auth/login`                      | Exchange e-mail and password for a bearer token  |

## Endpoints any signed-in role may call

| Endpoint             | Minimum role | Purpose                                          |
| -------------------- | ------------ | ------------------------------------------------ |
| `POST /auth/logout`  | `user`       | Revoke the current token                         |
| `GET /auth/profile`  | `user`       | The signed-in user; 401 when the token is missing, expired or revoked |
| `PUT /auth/profile`  | `user`       | Update the signed-in user’s own name, phone and avatar |
| `PUT /auth/password` | `user`       | Change the signed-in user’s own password         |
