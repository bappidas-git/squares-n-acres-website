# Squares N Acres — backend developer guidelines

> Generated by `npm run generate:backend-guidelines`. **Do not edit any file in
> this folder** — the next run overwrites it. Everything hand-written lives in
> `docs/backend-notes/*.md` in the website repository.
> generatedFrom: 768de0a

Everything needed to build the Laravel + MySQL API that this website already
expects. The frontend is finished and it works today, against a mock server that
implements the contract completely. Your job is to answer the same way.

---

## What is in this folder

| File                       | What it is                                       | Size      |
| -------------------------- | ------------------------------------------------ | --------- |
| `README.md`                | this file — orientation, the switch-over, the parity checklist | this file |
| `01_API_CONTRACT.md`       | envelopes, errors, auth, pagination, filters, write semantics, slugs | 80 kB     |
| `02_AUTH_AND_RBAC.md`      | the token flow and who may call what, endpoint by endpoint | 46 kB     |
| `03_ENDPOINTS.md`          | every endpoint: parameters, Laravel rules, a captured example, errors, side effects | 1362 kB   |
| `04_DATA_MODELS.md`        | every collection and field, the relational mapping and the enumerations | 295 kB    |
| `schema.sql`               | MySQL 8 DDL — run it, then import the seed       | 48 kB     |
| `05_BUSINESS_RULES.md`     | the formulas: search, facets, leads, the dashboard, exports, guards | 88 kB     |
| `06_SEO_SITEMAP_ROBOTS.md` | the nine documents the API serves to crawlers    | 19 kB     |
| `07_DEPLOYMENT.md`         | Cloudways (one host or two), the self-managed Nginx alternative, switch-over, go-live | 40 kB     |
| `08_TESTING_AND_PARITY.md` | how to prove the two backends answer the same — and where not to | 22 kB     |
| `09_MEDIA_AND_EMAIL.md`    | Cloudinary’s server side, the lead e-mail, and the two planned additions | 15 kB     |
| `smoke/`                   | the smoke test with everything it needs — `node smoke/smoke-api.js`, no install | 245 kB    |
| `postman_collection.json`  | every endpoint as a request, with tests and saved responses | 1487 kB   |
| `postman_environment.json` | the local mock, with staging and production rows alongside | 2 kB      |
| `openapi.yaml`             | OpenAPI 3.1 for tooling and client generation    | 1629 kB   |
| `db.json`                  | the seed, byte-identical to the repository copy  | 1514 kB   |
| `seed-mapping.md`          | how to import `db.json` into MySQL               | 13 kB     |

---

## How the frontend talks to the API

There is **no adapter**. No transformation layer, no field mapper, no
compatibility shim — the components read the JSON the API sends, by the names
the API sends.

```
React component
   └─ a hook (useProperties, useLead, …)
        └─ a service (propertyService.list, …)
             └─ http.request(endpoints.properties.list, { params })
                  └─ axios  →  ${REACT_APP_API_URL}/properties?page=1&perPage=12
```

Three consequences worth internalising before you write a controller:

- **Every path the frontend can call is declared in one file**,
  `src/services/endpoints.js` — 274 entries across
  12 modules. `03_ENDPOINTS.md`, the Postman collection and
  `openapi.yaml` in this folder are all generated from it, so they cannot
  describe an endpoint that does not exist or miss one that does. A lint rule in
  the repository fails the build if an API path appears anywhere else.
- **The whole contract is camelCase** — request bodies, response bodies and
  query parameters alike. Your columns are `snake_case`; your API Resources are
  where the two meet, and `04_DATA_MODELS.md` has the column-by-column mapping.
- **The envelope is not optional.** `{ "data": … }` for one resource,
  `{ "data": [ … ], "meta": { … } }` for a list, `{ "data": null, "message": … }`
  for an action. A bare array breaks every list on the site. This is §5.2 of
  `01_API_CONTRACT.md` and it is the single most common parity failure.

---

## The switch-over

Pointing the live site at your API is **one line**:

```bash
# .env.production, on the build machine — one of the two
REACT_APP_API_URL=https://www.squaresnacres.com/api   # the site and the API on one host
REACT_APP_API_URL=https://api.squaresnacres.com/api   # the API on a host of its own
```

then `npm run build` and deploy the `build/` folder. There is no second step, no
feature flag and no code change. `07_DEPLOYMENT.md` has the full procedure on
Cloudways — one host (Layout A, recommended) or two (Layout B) — the
self-managed Nginx alternative, and the go-live checklist.

---

## Where to start

1. **`01_API_CONTRACT.md`** — envelopes, errors, auth, pagination, filters,
   write semantics, slugs. An hour here saves a week.
2. **`04_DATA_MODELS.md`** and **`schema.sql`** — the tables. Run the DDL, import
   `db.json` with `seed-mapping.md`, and you have a database the finished
   frontend renders in full.
3. **`03_ENDPOINTS.md`** — build endpoint by endpoint. Each section has the
   Laravel validation rules already written out.
4. **`05_BUSINESS_RULES.md`** — the formulas: the relevance sort, the facet
   counts, the lead pipeline, the dashboard aggregates. Read it before you
   finish the property list, not after.
5. **`08_TESTING_AND_PARITY.md`** — prove it matches.
6. **`07_DEPLOYMENT.md`** and **`09_MEDIA_AND_EMAIL.md`** — the hosting, the
   Cloudinary presets and the lead e-mail, before the go-live and not during it.

---

## Using Postman

1. Import `postman_collection.json` and `postman_environment.json`.
2. Select the **Squares N Acres — Local mock** environment.
3. Send `auth › auth › POST /auth/login`. Its test script writes `{{token}}`
   into the environment; every other request inherits it.
4. Run the whole collection. Each request asserts its status and its envelope,
   and the run creates, edits and deletes records — every one it creates it
   deletes again.

Every request carries the response captured from the mock as a saved example, so
you can see the shape of an answer before you have written it. To test your API,
switch the `baseUrl` row of the environment to your machine or to **staging**
and run the collection again: every test that passes against the mock must pass
against you. **Never run it against production** — it posts enquiries that
e-mail the desk and creates an account; production gets the read checks of the
smoke test below.

`openapi.yaml` describes the same 274 operations for tooling —
paste it into <https://editor.swagger.io> to browse it, or generate a client
from it.

---

## The parity checklist

The claim this package makes is that the frontend cannot tell the two backends
apart. In order:

- [ ] `schema.sql` runs clean on MySQL 8, and `db.json` imports per
      `seed-mapping.md` with the ids preserved.
- [ ] `node smoke/smoke-api.js --baseUrl=<your API>` passes — it walks every
      registry entry and checks statuses, envelopes, pagination, RBAC and two
      dozen behaviours a status code cannot describe. Node 20 and this folder
      are all it needs; in the website repository `npm run smoke -- <the same
      options>` is the same run. It writes only to your own machine unless
      `--allow-writes` is passed, and that is for **staging**: against
      production it runs the read checks alone (`smoke/README.md`).
- [ ] `node smoke/smoke-api.js --baseUrl=<your API> --compare=http://localhost:4000/api`
      prints an empty difference table — with `--email`/`--password` for your API
      and `--compareEmail`/`--comparePassword` for the mock once the passwords
      differ.
- [ ] The Postman collection runs green against your machine or staging —
      never against production.
- [ ] The module checklist in `08_TESTING_AND_PARITY.md` is walked through in a
      browser, with the site pointed at your API.
- [ ] The go-live checklist in `07_DEPLOYMENT.md` is complete — including
      rotating the three seed passwords.

---

## Endpoint support matrix

Everything here is **required for v1** unless the last column says otherwise: the
frontend calls all of it, and an endpoint that answers 404 is a screen that does
not work. The few marked optional degrade gracefully — the feature hides itself
rather than breaking.

| Endpoint                                   | Module     | Status              | Notes                                            |
| ------------------------------------------ | ---------- | ------------------- | ------------------------------------------------ |
| `GET /properties`                          | properties | **required for v1** | Public paginated property search with facets     |
| `GET /properties/counts`                   | properties | optional            | the home page falls back to one `GET /properties?perPage=1` a tile on 404 or 501 |
| `GET /properties/featured`                 | properties | **required for v1** | Featured properties, ordered by priority then recency |
| `GET /properties/slug/:slug`               | properties | **required for v1** | Property details by slug; 404 when inactive, unless `previewToken` is that listing’s share token |
| `GET /properties/:id/similar`              | properties | **required for v1** | Admin-selected similar properties, topped up to six by locality and type |
| `POST /properties/:id/view`                | properties | optional            | the counter simply does not move                 |
| `POST /properties/:id/documents/access`    | properties | **required for v1** | The addresses of a listing’s files, for the token POST /leads answered a lead about it with |
| `GET /properties/suggestions`              | properties | **required for v1** | Type-ahead suggestions for the hero and header search |
| `GET /localities`                          | masterData | **required for v1** | Localities with their active property count      |
| `GET /localities/slug/:slug`               | masterData | **required for v1** | Locality guide page by slug                      |
| `GET /cities`                              | masterData | **required for v1** | Cities the portal covers                         |
| `GET /developers`                          | masterData | **required for v1** | Developers with their active property count      |
| `GET /developers/slug/:slug`               | masterData | **required for v1** | Developer page by slug                           |
| `GET /segments`                            | masterData | **required for v1** | Every segment, the inactive ones flagged — the layout of a listing filed under one depends on its kind |
| `GET /property-types`                      | masterData | **required for v1** | Property types, optionally filtered by segment   |
| `GET /amenities`                           | masterData | **required for v1** | Amenities, optionally filtered by category       |
| `GET /badges`                              | masterData | **required for v1** | Property badges                                  |
| `GET /banks`                               | masterData | **required for v1** | Home-loan partners for the finance section and the EMI calculator |
| `GET /articles`                            | articles   | **required for v1** | Published articles, newest or most read first    |
| `GET /articles/slug/:slug`                 | articles   | **required for v1** | Article by slug; a matching preview token also returns drafts |
| `GET /articles/trending`                   | articles   | **required for v1** | The six most read published articles             |
| `GET /articles/:id/adjacent`               | articles   | **required for v1** | The published articles either side of this one by publishedAt |
| `GET /article-categories`                  | articles   | **required for v1** | Article categories with their published article count |
| `GET /article-tags`                        | articles   | **required for v1** | Article tags with their published article count  |
| `GET /authors`                             | articles   | **required for v1** | Active authors, public fields only               |
| `GET /authors/slug/:slug`                  | articles   | **required for v1** | Author page by slug                              |
| `GET /faqs`                                | content    | **required for v1** | FAQs for the FAQ page, the home section and property pages |
| `GET /testimonials`                        | content    | **required for v1** | Active testimonials                              |
| `GET /team`                                | content    | **required for v1** | Team members shown on the About page             |
| `GET /partners`                            | content    | **required for v1** | Partner logos, optionally filtered by category   |
| `GET /pages`                               | content    | **required for v1** | Published pages that belong in the header or footer navigation |
| `GET /pages/slug/:slug`                    | content    | **required for v1** | Published CMS page by slug; a matching preview token also returns drafts |
| `GET /header-menus`                        | content    | **required for v1** | The active header menus, left to right, with their submenus and links |
| `GET /jobs`                                | content    | **required for v1** | Open job postings                                |
| `GET /jobs/slug/:slug`                     | content    | **required for v1** | Job posting by slug                              |
| `POST /jobs/:id/apply`                     | content    | **required for v1** | Apply for a job posting; honeypot and rate limited |
| `POST /leads`                              | leads      | **required for v1** | Capture a lead from any form on the site; honeypot and rate limited |
| `POST /newsletter/subscribe`               | content    | **required for v1** | Subscribe an e-mail address; a duplicate answers 200 instead of 409 |
| `GET /settings`                            | settings   | **required for v1** | The public subset of the site settings           |
| `GET /seo/settings`                        | seo        | **required for v1** | The public subset of the SEO settings used by <Seo> and the sitemap |
| `GET /redirects`                           | seo        | **required for v1** | Active redirects, resolved client-side by RedirectHandler |
| `GET /redirects/resolve`                   | seo        | optional            | used by the smoke test; the SPA resolves client-side |
| `POST /redirects/:id/hit`                  | seo        | **required for v1** | Count a visitor RedirectHandler sent on by an active rule; 204, rate-limited, 404 for an unknown or inactive rule (prompt 51) |
| `POST /not-found`                          | seo        | **required for v1** | The 404 page reporting the address it was reached at; 204, rate-limited — the admin, the API, static files and redirected addresses are ignored (prompt 51) |
| `GET /health`                              | system     | **required for v1** | Liveness: `{ status: "ok", time }` — the first request of the smoke test |
| `GET /sitemap.xml`                         | seo        | **required for v1** | Sitemap index listing every sub-sitemap          |
| `GET /sitemap-properties.xml`              | seo        | **required for v1** | Property URLs with lastmod, changefreq, priority and image entries |
| `GET /sitemap-localities.xml`              | seo        | **required for v1** | Locality URLs                                    |
| `GET /sitemap-developers.xml`              | seo        | **required for v1** | Developer URLs                                   |
| `GET /sitemap-articles.xml`                | seo        | **required for v1** | Article, category, tag and author URLs           |
| `GET /sitemap-pages.xml`                   | seo        | **required for v1** | CMS page URLs                                    |
| `GET /robots.txt`                          | seo        | **required for v1** | robots.txt from seoSettings.robotsTxt            |
| `GET /rss.xml`                             | seo        | **required for v1** | RSS feed of the latest twenty published articles |
| `GET /llms.txt`                            | seo        | optional            | nothing on the site reads it; crawlers do        |
| `POST /auth/login`                         | auth       | **required for v1** | Exchange e-mail and password for a bearer token  |
| `POST /auth/refresh`                       | auth       | **required for v1** | Keep the current session: the same token, valid for a full lifetime from now — the answer a sign-in gives (prompt 51) |
| `POST /auth/logout`                        | auth       | **required for v1** | Revoke the current token                         |
| `GET /auth/profile`                        | auth       | **required for v1** | The signed-in user; 401 when the token is missing, expired or revoked |
| `PUT /auth/profile`                        | auth       | **required for v1** | Update the signed-in user’s own name, phone and avatar |
| `PUT /auth/password`                       | auth       | **required for v1** | Change the signed-in user’s own password         |
| `GET /admin/dashboard`                     | dashboard  | **required for v1** | Role-aware dashboard aggregates, trends and recent activity; follow-ups due with the overdue ones first and `overdueCount` |
| `GET /admin/properties`                    | properties | **required for v1** | List properties for the admin table              |
| `POST /admin/properties`                   | properties | **required for v1** | Create a property                                |
| `GET /admin/properties/:id`                | properties | **required for v1** | Read one property with every admin field         |
| `PUT /admin/properties/:id`                | properties | **required for v1** | Replace a property with the full record from the form |
| `PATCH /admin/properties/:id`              | properties | **required for v1** | Update the given fields of a property (toggles, order, SEO panel) |
| `DELETE /admin/properties/:id`             | properties | **required for v1** | Delete a property; 409 when it is still in use   |
| `POST /admin/properties/bulk`              | properties | **required for v1** | Apply one action to several properties: activate, deactivate, feature, unfeature, verify, unverify, delete — or availability {availability}, assignAgent {agentId}, setLocality {localityId}, setPropertyType {propertyTypeId}, setDeveloper {developerId} |
| `GET /admin/properties/check-slug`         | properties | **required for v1** | Check whether a property slug is free and suggest an alternative |
| `GET /admin/properties/slug/:slug`         | properties | **required for v1** | Read one property by slug with every admin field, active or not |
| `POST /admin/properties/:id/preview-token` | properties | **required for v1** | A 24-hour share token and URL that opens an inactive property on the site |
| `POST /admin/properties/:id/duplicate`     | properties | **required for v1** | Copy a property as an inactive draft with a fresh slug |
| `GET /admin/leads`                         | leads      | **required for v1** | Lead list for the CRM; scoped to own and unassigned leads for sales |
| `POST /admin/leads`                        | leads      | **required for v1** | Enter a lead by hand — a walk-in, a call, a portal lead; a sales user’s lead is their own, anyone else’s goes to the colleague named or is auto-assigned |
| `POST /admin/leads/:id/activities`         | leads      | **required for v1** | Log a call, a WhatsApp, a site visit, a meeting or another conversation on the lead’s timeline |
| `GET /admin/leads/:id`                     | leads      | **required for v1** | One lead with its notes and activity timeline    |
| `PATCH /admin/leads/:id`                   | leads      | **required for v1** | Change status, priority, assignee, follow-up or lost reason |
| `DELETE /admin/leads/:id`                  | leads      | **required for v1** | Delete a lead; forbidden for sales               |
| `POST /admin/leads/:id/claim`              | leads      | **required for v1** | Assign an unassigned lead to the signed-in user  |
| `POST /admin/leads/:id/notes`              | leads      | **required for v1** | Append a note to a lead and return the lead      |
| `DELETE /admin/leads/:id/notes/:noteId`    | leads      | **required for v1** | Delete one note from a lead                      |
| `GET /admin/leads/export`                  | leads      | **required for v1** | CSV export of the filtered lead list, scoped like the list endpoint |
| `POST /admin/leads/bulk`                   | leads      | **required for v1** | Change status, priority or assignee of several leads, or delete them |
| `GET /admin/localities`                    | masterData | **required for v1** | List localities for the admin table              |
| `POST /admin/localities`                   | masterData | **required for v1** | Create a locality                                |
| `GET /admin/localities/:id`                | masterData | **required for v1** | Read one locality with every admin field         |
| `PUT /admin/localities/:id`                | masterData | **required for v1** | Replace a locality with the full record from the form |
| `PATCH /admin/localities/:id`              | masterData | **required for v1** | Update the given fields of a locality (toggles, order, SEO panel) |
| `DELETE /admin/localities/:id`             | masterData | **required for v1** | Delete a locality; 409 when it is still in use   |
| `POST /admin/localities/bulk`              | masterData | **required for v1** | Apply one action to several localities           |
| `GET /admin/localities/check-slug`         | masterData | **required for v1** | Check whether a locality slug is free and suggest an alternative |
| `GET /admin/cities`                        | masterData | **required for v1** | List cities for the admin table                  |
| `POST /admin/cities`                       | masterData | **required for v1** | Create a city                                    |
| `GET /admin/cities/:id`                    | masterData | **required for v1** | Read one city with every admin field             |
| `PUT /admin/cities/:id`                    | masterData | **required for v1** | Replace a city with the full record from the form |
| `PATCH /admin/cities/:id`                  | masterData | **required for v1** | Update the given fields of a city (toggles, order, SEO panel) |
| `DELETE /admin/cities/:id`                 | masterData | **required for v1** | Delete a city; 409 when it is still in use       |
| `POST /admin/cities/bulk`                  | masterData | **required for v1** | Apply one action to several cities               |
| `GET /admin/cities/check-slug`             | masterData | **required for v1** | Check whether a city slug is free and suggest an alternative |
| `GET /admin/segments`                      | masterData | **required for v1** | List segments for the admin table                |
| `POST /admin/segments`                     | masterData | **required for v1** | Create a segment                                 |
| `GET /admin/segments/:id`                  | masterData | **required for v1** | Read one segment with every admin field          |
| `PUT /admin/segments/:id`                  | masterData | **required for v1** | Replace a segment with the full record from the form |
| `PATCH /admin/segments/:id`                | masterData | **required for v1** | Update the given fields of a segment (toggles, order, SEO panel) |
| `DELETE /admin/segments/:id`               | masterData | **required for v1** | Delete a segment; 409 when it is still in use    |
| `POST /admin/segments/bulk`                | masterData | **required for v1** | Apply one action to several segments             |
| `GET /admin/segments/check-slug`           | masterData | **required for v1** | Check whether a segment slug is free and suggest an alternative |
| `GET /admin/property-types`                | masterData | **required for v1** | List property types for the admin table          |
| `POST /admin/property-types`               | masterData | **required for v1** | Create a property type                           |
| `GET /admin/property-types/:id`            | masterData | **required for v1** | Read one property type with every admin field    |
| `PUT /admin/property-types/:id`            | masterData | **required for v1** | Replace a property type with the full record from the form |
| `PATCH /admin/property-types/:id`          | masterData | **required for v1** | Update the given fields of a property type (toggles, order, SEO panel) |
| `DELETE /admin/property-types/:id`         | masterData | **required for v1** | Delete a property type; 409 when it is still in use |
| `POST /admin/property-types/bulk`          | masterData | **required for v1** | Apply one action to several property types       |
| `GET /admin/property-types/check-slug`     | masterData | **required for v1** | Check whether a property type slug is free and suggest an alternative |
| `GET /admin/amenities`                     | masterData | **required for v1** | List amenities for the admin table               |
| `POST /admin/amenities`                    | masterData | **required for v1** | Create a amenity                                 |
| `GET /admin/amenities/:id`                 | masterData | **required for v1** | Read one amenity with every admin field          |
| `PUT /admin/amenities/:id`                 | masterData | **required for v1** | Replace a amenity with the full record from the form |
| `PATCH /admin/amenities/:id`               | masterData | **required for v1** | Update the given fields of a amenity (toggles, order, SEO panel) |
| `DELETE /admin/amenities/:id`              | masterData | **required for v1** | Delete a amenity; 409 when it is still in use    |
| `POST /admin/amenities/bulk`               | masterData | **required for v1** | Apply one action to several amenities            |
| `GET /admin/amenities/check-slug`          | masterData | **required for v1** | Check whether a amenity slug is free and suggest an alternative |
| `GET /admin/badges`                        | masterData | **required for v1** | List badges for the admin table                  |
| `POST /admin/badges`                       | masterData | **required for v1** | Create a badge                                   |
| `GET /admin/badges/:id`                    | masterData | **required for v1** | Read one badge with every admin field            |
| `PUT /admin/badges/:id`                    | masterData | **required for v1** | Replace a badge with the full record from the form |
| `PATCH /admin/badges/:id`                  | masterData | **required for v1** | Update the given fields of a badge (toggles, order, SEO panel) |
| `DELETE /admin/badges/:id`                 | masterData | **required for v1** | Delete a badge; 409 when it is still in use      |
| `POST /admin/badges/bulk`                  | masterData | **required for v1** | Apply one action to several badges               |
| `GET /admin/badges/check-slug`             | masterData | **required for v1** | Check whether a badge slug is free and suggest an alternative |
| `GET /admin/developers`                    | masterData | **required for v1** | List developers for the admin table              |
| `POST /admin/developers`                   | masterData | **required for v1** | Create a developer                               |
| `GET /admin/developers/:id`                | masterData | **required for v1** | Read one developer with every admin field        |
| `PUT /admin/developers/:id`                | masterData | **required for v1** | Replace a developer with the full record from the form |
| `PATCH /admin/developers/:id`              | masterData | **required for v1** | Update the given fields of a developer (toggles, order, SEO panel) |
| `DELETE /admin/developers/:id`             | masterData | **required for v1** | Delete a developer; 409 when it is still in use  |
| `POST /admin/developers/bulk`              | masterData | **required for v1** | Apply one action to several developers           |
| `GET /admin/developers/check-slug`         | masterData | **required for v1** | Check whether a developer slug is free and suggest an alternative |
| `GET /admin/banks`                         | masterData | **required for v1** | List banks for the admin table                   |
| `POST /admin/banks`                        | masterData | **required for v1** | Create a bank                                    |
| `GET /admin/banks/:id`                     | masterData | **required for v1** | Read one bank with every admin field             |
| `PUT /admin/banks/:id`                     | masterData | **required for v1** | Replace a bank with the full record from the form |
| `PATCH /admin/banks/:id`                   | masterData | **required for v1** | Update the given fields of a bank (toggles, order, SEO panel) |
| `DELETE /admin/banks/:id`                  | masterData | **required for v1** | Delete a bank; 409 when it is still in use       |
| `POST /admin/banks/bulk`                   | masterData | **required for v1** | Apply one action to several banks                |
| `GET /admin/banks/check-slug`              | masterData | **required for v1** | Check whether a bank slug is free and suggest an alternative |
| `GET /admin/articles`                      | articles   | **required for v1** | List articles for the admin table                |
| `POST /admin/articles`                     | articles   | **required for v1** | Create a article                                 |
| `GET /admin/articles/:id`                  | articles   | **required for v1** | Read one article with every admin field          |
| `PUT /admin/articles/:id`                  | articles   | **required for v1** | Replace a article with the full record from the form |
| `PATCH /admin/articles/:id`                | articles   | **required for v1** | Update the given fields of a article (toggles, order, SEO panel) |
| `DELETE /admin/articles/:id`               | articles   | **required for v1** | Delete a article; 409 when it is still in use    |
| `POST /admin/articles/bulk`                | articles   | **required for v1** | Apply one action to several articles             |
| `GET /admin/articles/check-slug`           | articles   | **required for v1** | Check whether a article slug is free and suggest an alternative |
| `GET /admin/articles/:id/preview-token`    | articles   | **required for v1** | A 24-hour preview token and URL for an unpublished article |
| `GET /admin/article-categories`            | articles   | **required for v1** | List article categories for the admin table      |
| `POST /admin/article-categories`           | articles   | **required for v1** | Create a article category                        |
| `GET /admin/article-categories/:id`        | articles   | **required for v1** | Read one article category with every admin field |
| `PUT /admin/article-categories/:id`        | articles   | **required for v1** | Replace a article category with the full record from the form |
| `PATCH /admin/article-categories/:id`      | articles   | **required for v1** | Update the given fields of a article category (toggles, order, SEO panel) |
| `DELETE /admin/article-categories/:id`     | articles   | **required for v1** | Delete a article category; 409 when it is still in use |
| `POST /admin/article-categories/bulk`      | articles   | **required for v1** | Apply one action to several article categories   |
| `GET /admin/article-categories/check-slug` | articles   | **required for v1** | Check whether a article category slug is free and suggest an alternative |
| `GET /admin/article-tags`                  | articles   | **required for v1** | List article tags for the admin table            |
| `POST /admin/article-tags`                 | articles   | **required for v1** | Create a article tag                             |
| `GET /admin/article-tags/:id`              | articles   | **required for v1** | Read one article tag with every admin field      |
| `PUT /admin/article-tags/:id`              | articles   | **required for v1** | Replace a article tag with the full record from the form |
| `PATCH /admin/article-tags/:id`            | articles   | **required for v1** | Update the given fields of a article tag (toggles, order, SEO panel) |
| `DELETE /admin/article-tags/:id`           | articles   | **required for v1** | Delete a article tag; 409 when it is still in use |
| `POST /admin/article-tags/bulk`            | articles   | **required for v1** | Apply one action to several article tags         |
| `GET /admin/article-tags/check-slug`       | articles   | **required for v1** | Check whether a article tag slug is free and suggest an alternative |
| `GET /admin/authors`                       | articles   | **required for v1** | List authors for the admin table                 |
| `POST /admin/authors`                      | articles   | **required for v1** | Create a author                                  |
| `GET /admin/authors/:id`                   | articles   | **required for v1** | Read one author with every admin field           |
| `PUT /admin/authors/:id`                   | articles   | **required for v1** | Replace a author with the full record from the form |
| `PATCH /admin/authors/:id`                 | articles   | **required for v1** | Update the given fields of a author (toggles, order, SEO panel) |
| `DELETE /admin/authors/:id`                | articles   | **required for v1** | Delete a author; 409 when it is still in use     |
| `POST /admin/authors/bulk`                 | articles   | **required for v1** | Apply one action to several authors              |
| `GET /admin/authors/check-slug`            | articles   | **required for v1** | Check whether a author slug is free and suggest an alternative |
| `GET /admin/faqs`                          | content    | **required for v1** | List FAQs for the admin table                    |
| `POST /admin/faqs`                         | content    | **required for v1** | Create a FAQ                                     |
| `GET /admin/faqs/:id`                      | content    | **required for v1** | Read one FAQ with every admin field              |
| `PUT /admin/faqs/:id`                      | content    | **required for v1** | Replace a FAQ with the full record from the form |
| `PATCH /admin/faqs/:id`                    | content    | **required for v1** | Update the given fields of a FAQ (toggles, order, SEO panel) |
| `DELETE /admin/faqs/:id`                   | content    | **required for v1** | Delete a FAQ; 409 when it is still in use        |
| `POST /admin/faqs/bulk`                    | content    | **required for v1** | Apply one action to several FAQs                 |
| `GET /admin/testimonials`                  | content    | **required for v1** | List testimonials for the admin table            |
| `POST /admin/testimonials`                 | content    | **required for v1** | Create a testimonial                             |
| `GET /admin/testimonials/:id`              | content    | **required for v1** | Read one testimonial with every admin field      |
| `PUT /admin/testimonials/:id`              | content    | **required for v1** | Replace a testimonial with the full record from the form |
| `PATCH /admin/testimonials/:id`            | content    | **required for v1** | Update the given fields of a testimonial (toggles, order, SEO panel) |
| `DELETE /admin/testimonials/:id`           | content    | **required for v1** | Delete a testimonial; 409 when it is still in use |
| `POST /admin/testimonials/bulk`            | content    | **required for v1** | Apply one action to several testimonials         |
| `GET /admin/team`                          | content    | **required for v1** | List team members for the admin table; each row carries `listingCount`, the listings naming the member as their advisor |
| `POST /admin/team`                         | content    | **required for v1** | Create a team member                             |
| `GET /admin/team/:id`                      | content    | **required for v1** | Read one team member with every admin field      |
| `PUT /admin/team/:id`                      | content    | **required for v1** | Replace a team member with the full record from the form |
| `PATCH /admin/team/:id`                    | content    | **required for v1** | Update the given fields of a team member (toggles, order, SEO panel) |
| `DELETE /admin/team/:id`                   | content    | **required for v1** | Delete a team member; 409 when it is still in use |
| `POST /admin/team/bulk`                    | content    | **required for v1** | Apply one action to several team members         |
| `GET /admin/team/check-slug`               | content    | **required for v1** | Check whether a team member slug is free and suggest an alternative |
| `GET /admin/partners`                      | content    | **required for v1** | List partners for the admin table                |
| `POST /admin/partners`                     | content    | **required for v1** | Create a partner                                 |
| `GET /admin/partners/:id`                  | content    | **required for v1** | Read one partner with every admin field          |
| `PUT /admin/partners/:id`                  | content    | **required for v1** | Replace a partner with the full record from the form |
| `PATCH /admin/partners/:id`                | content    | **required for v1** | Update the given fields of a partner (toggles, order, SEO panel) |
| `DELETE /admin/partners/:id`               | content    | **required for v1** | Delete a partner; 409 when it is still in use    |
| `POST /admin/partners/bulk`                | content    | **required for v1** | Apply one action to several partners             |
| `GET /admin/pages`                         | content    | **required for v1** | List pages for the admin table                   |
| `POST /admin/pages`                        | content    | **required for v1** | Create a page                                    |
| `GET /admin/pages/:id`                     | content    | **required for v1** | Read one page with every admin field             |
| `PUT /admin/pages/:id`                     | content    | **required for v1** | Replace a page with the full record from the form |
| `PATCH /admin/pages/:id`                   | content    | **required for v1** | Update the given fields of a page (toggles, order, SEO panel) |
| `DELETE /admin/pages/:id`                  | content    | **required for v1** | Delete a page; 409 when it is still in use       |
| `POST /admin/pages/bulk`                   | content    | **required for v1** | Apply one action to several pages                |
| `GET /admin/pages/check-slug`              | content    | **required for v1** | Check whether a page slug is free and suggest an alternative |
| `GET /admin/pages/:id/preview-token`       | content    | **required for v1** | A 24-hour preview token and URL for an unpublished page |
| `GET /admin/header-menus`                  | content    | **required for v1** | List header menus for the admin table            |
| `POST /admin/header-menus`                 | content    | **required for v1** | Create a header menu                             |
| `GET /admin/header-menus/:id`              | content    | **required for v1** | Read one header menu with every admin field      |
| `PUT /admin/header-menus/:id`              | content    | **required for v1** | Replace a header menu with the full record from the form |
| `PATCH /admin/header-menus/:id`            | content    | **required for v1** | Update the given fields of a header menu (toggles, order, SEO panel) |
| `DELETE /admin/header-menus/:id`           | content    | **required for v1** | Delete a header menu; 409 when it is still in use |
| `POST /admin/header-menus/bulk`            | content    | **required for v1** | Apply one action to several header menus         |
| `GET /admin/header-menus/check-slug`       | content    | optional            | nothing calls it: a menu’s slug is derived from its name and kept (422 on a clash) |
| `GET /admin/jobs`                          | content    | **required for v1** | List job postings for the admin table            |
| `POST /admin/jobs`                         | content    | **required for v1** | Create a job posting                             |
| `GET /admin/jobs/:id`                      | content    | **required for v1** | Read one job posting with every admin field      |
| `PUT /admin/jobs/:id`                      | content    | **required for v1** | Replace a job posting with the full record from the form |
| `PATCH /admin/jobs/:id`                    | content    | **required for v1** | Update the given fields of a job posting (toggles, order, SEO panel) |
| `DELETE /admin/jobs/:id`                   | content    | **required for v1** | Delete a job posting; 409 when it is still in use |
| `POST /admin/jobs/bulk`                    | content    | **required for v1** | Apply one action to several job postings         |
| `GET /admin/jobs/check-slug`               | content    | **required for v1** | Check whether a job posting slug is free and suggest an alternative |
| `GET /admin/job-applications`              | content    | **required for v1** | Applications received for the job postings       |
| `PATCH /admin/job-applications/:id`        | content    | **required for v1** | Move an application through the hiring statuses  |
| `DELETE /admin/job-applications/:id`       | content    | **required for v1** | Delete an application                            |
| `GET /admin/newsletter-subscribers`        | content    | **required for v1** | Newsletter subscribers                           |
| `PATCH /admin/newsletter-subscribers/:id`  | content    | **required for v1** | Mark a subscriber unsubscribed — or subscribed again; the status is all it changes (prompt 51) |
| `DELETE /admin/newsletter-subscribers/:id` | content    | **required for v1** | Remove a subscriber                              |
| `GET /admin/newsletter-subscribers/export` | content    | optional            | the list is visible on screen                    |
| `GET /admin/media`                         | media      | **required for v1** | List media items for the library grid; `meta.folders` is `[{ name, count }]` for the folders the other filters leave something in, `meta.unfiled` counts the files in none, `usage=unused` keeps the files nothing shows, and `q` also reads the address and the tags |
| `POST /admin/media`                        | media      | **required for v1** | Create a media item                              |
| `GET /admin/media/:id`                     | media      | **required for v1** | Read one media item with every admin field       |
| `PUT /admin/media/:id`                     | media      | **required for v1** | Replace a media item with the full record from the form |
| `PATCH /admin/media/:id`                   | media      | **required for v1** | Update the given fields of a media item (toggles, order, SEO panel) |
| `DELETE /admin/media/:id`                  | media      | **required for v1** | Delete a media record; 409 listing `usedIn` when the file is still in use, unless `force=true` |
| `POST /admin/media/bulk`                   | media      | **required for v1** | Apply one action to several media items; a delete is all or nothing — a 409 naming every file still in use (`data.refused`), unless `force=true`; `move` refiles them under `payload.folder` (`null` for none) and answers `missing` for ids that matched nothing |
| `POST /admin/media/folders/rename`         | media      | **required for v1** | Refile every media record of a folder (and of the folders inside it) under another name; 422 on `to` with `data.existing` when that name holds files, unless `merge: true`. Records only — Cloudinary paths do not change |
| `GET /admin/redirects`                     | seo        | **required for v1** | List redirects for the admin table               |
| `POST /admin/redirects`                    | seo        | **required for v1** | Create a redirect                                |
| `GET /admin/redirects/:id`                 | seo        | **required for v1** | Read one redirect with every admin field         |
| `PUT /admin/redirects/:id`                 | seo        | **required for v1** | Replace a redirect with the full record from the form |
| `PATCH /admin/redirects/:id`               | seo        | **required for v1** | Update the given fields of a redirect (toggles, order, SEO panel) |
| `DELETE /admin/redirects/:id`              | seo        | **required for v1** | Delete a redirect; 409 when it is still in use   |
| `POST /admin/redirects/bulk`               | seo        | **required for v1** | Apply one action to several redirects            |
| `POST /admin/redirects/import`             | seo        | optional            | the admin still adds redirects one at a time     |
| `GET /admin/redirects/export`              | seo        | optional            | the list is visible on screen                    |
| `GET /admin/seo/settings`                  | seo        | **required for v1** | The complete SEO settings singleton              |
| `PUT /admin/seo/settings`                  | seo        | **required for v1** | Replace the SEO settings; known keys are deep-merged |
| `GET /admin/seo/llms-preview`              | seo        | optional            | the SEO settings screen hides the preview button |
| `GET /admin/seo/overview`                  | seo        | **required for v1** | Lightweight SEO rows for the dashboard and the uniqueness checks |
| `GET /admin/seo/not-found`                 | seo        | **required for v1** | The addresses visitors reached that answered 404, one line per path, the most reached first (prompt 51) |
| `DELETE /admin/seo/not-found/:id`          | seo        | **required for v1** | Take a path off the 404 list — every day of it; a new visit puts it back (prompt 51) |
| `GET /admin/settings`                      | settings   | **required for v1** | The complete site settings singleton, including the lead branch |
| `PUT /admin/settings`                      | settings   | **required for v1** | Replace the site settings; known keys are deep-merged |
| `POST /admin/settings/test-lead-alert`     | settings   | **required for v1** | Send a test lead alert to the saved notification addresses; answers `sentTo` — 422 when none is saved, 502 when the mailer refuses |
| `GET /admin/users`                         | users      | **required for v1** | List users for the admin table                   |
| `POST /admin/users`                        | users      | **required for v1** | Create a user                                    |
| `GET /admin/users/:id`                     | users      | **required for v1** | Read one user with every admin field             |
| `PUT /admin/users/:id`                     | users      | **required for v1** | Replace a user with the full record from the form |
| `PATCH /admin/users/:id`                   | users      | **required for v1** | Update the given fields of a user (the Active switch, a password reset) |
| `DELETE /admin/users/:id`                  | users      | **required for v1** | Delete a user; their leads are unassigned        |
| `POST /admin/users/bulk`                   | users      | **required for v1** | Apply one action to several users                |

`GET /properties/counts` is optional in the sense the table gives the word: the
home page asks it two questions and, when it answers 404 or 501, falls back to
one `GET /properties?perPage=1` a tile — the numbers are the same, only slower
(`01_API_CONTRACT.md` → "Category counts"). Two further additions are specified
but not yet in the registry, so they are in none of the tables above: the
self-service password reset and the reCAPTCHA check, written up in
`09_MEDIA_AND_EMAIL.md`.

---

## Regenerating this package

From the website repository:

```bash
npm install
npm run mock                            # terminal 1 — examples are captured live
npm run generate:backend-guidelines     # terminal 2
npm run check:guidelines                # coverage check: registry ↔ docs ↔ Postman ↔ OpenAPI
```

The generator reads `src/services/endpoints.js`, `src/services/schemas/`,
`src/config/enums.js`, `mock-server/schemas/models.js`, `src/config/rbac.js`,
the hand-written notes in `docs/backend-notes/` and the live mock, then writes
every file in this folder — `smoke/` included, which `npm run
generate:smoke-bundle` also rebuilds alone. Before it writes anything it runs
the Postman collection against the mock and refuses a red test, and it refuses a
section of the notes no document places. It is deterministic: running it twice
against a fresh mock produces no diff, and the only line that changes between
commits is `generatedFrom` — which is why it fails outside a git checkout.

**Do not edit the generated files.** To change a sentence, edit
`docs/backend-notes/*.md`; to change a fact, edit the registry, the schema
descriptors or the model descriptors — and then the mock, the frontend and this
package all change together, which is the point.
