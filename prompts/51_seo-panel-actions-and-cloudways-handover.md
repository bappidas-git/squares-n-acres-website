# Prompt 51 — SEO panel actions that answer back, and a handover package that works on Cloudways

## 0. Before you start

- Read `prompts/00_MASTER_CONTEXT.md` fully, then `docs/PROJECT_STATE.md` (status is COMPLETE at 1.0.0 — this prompt is a post-release fix, not a new module) and `docs/DECISIONS.md`.
- Confirm prerequisites: prompts 01–48 are done and the two audits recorded as 49 and 50 in `docs/QA/` are committed; `git status` is clean; `npm install` has been run. Start `npm run mock` in the background; start `npm start` when you reach the manual QA.
- Never ask the human questions. If something is ambiguous, follow `00_MASTER_CONTEXT.md`; if still ambiguous, choose the simplest robust option and log it in `docs/DECISIONS.md`.
- If this prompt was partially executed in an earlier crashed session, continue from where `docs/PROJECT_STATE.md` and `git log` say it stopped; do not redo finished work.
- Do not weaken a lint rule, a test, `check:traces` or `check:endpoints` to get past a failure; fix the cause.

## 1. Context

Everything below was verified by reading the current code; re-verify each point before changing it.

**A. The three SEO panel buttons look dead.**

1. `src/components/seo/SeoPanel/parts/ScoreCard.jsx` — **Re-analyse** calls `reanalyse()` from `useSeoAnalysis.js`, which runs `analyze()` synchronously. Because the panel already re-analyses 400 ms after every keystroke (`ANALYSIS_DEBOUNCE_MS`), the click produces the identical result: the number does not move, nothing pulses, no toast is shown, and the cached site index (`useSiteSeoIndex.js`, module-level `cache`) that decides the three uniqueness tests is **not** refreshed. The button works and nobody can tell.
2. `ScoreCard.jsx` — **Auto-fill missing** calls `generateDefaults(entityType, entity, seoSettings, { context })` from `src/seo/autoGenerate.js` and writes only the fields that are empty (`seo.title || made.title`, …). On a record whose focus keyword, title, description and share image are already set — every seed record, and the category, page and article in the screenshots the human sent — it writes nothing and says nothing. There is no "overwrite" path in the UI at all (`generateDefaults` supports `{ overwrite: true }` but no caller passes it).
3. `src/components/seo/SeoPanel/SeoSummaryCard.jsx` — **Fix SEO** calls `onOpen(firstFailure(seo.analysis)?.field ?? null)`. The analysers (`src/seo/analyzers/basic.js`, `additional.js`, `titleReadability.js`, `contentReadability.js`) report `seo.title`, `seo.description`, `seo.focusKeyword`, `seo.og.imageUrl`, `slug`, `content`, `excerpt`, `images`, `faqs`, … as `field`. The two hosts handle that differently:
   - `src/pages/admin/articles/ArticleFormPage.jsx` — `focusField(path)` **always** calls `setActiveTab('content')` and looks the path up in `FIELD_TARGET`, which has no `seo.*` entries. The first failure of almost every article is a `seo.*` test ("The title does not carry the focus keyword" → `seo.title`), so the click keeps the Content tab open, focuses nothing, and looks broken. That is the screenshot the human sent.
   - `src/pages/admin/properties/property-form/usePropertyForm.js` — `focusField(path)` does switch to the SEO tab (`tabOfPath('seo.title')` → `seo` because the tab declares `fields: ['seo']`), but then `focusFieldElement` (`fieldFocus.js`) looks for the id `property-seo-title` while `SeoPanel` renders its controls with `fieldId(path)` = `seo-field-seo-title`. The tab opens; the field is never focused or scrolled to.
   - `SeoPanel.jsx` accepts `initialField` (used by `src/pages/admin/seo/SeoEditDialog.jsx`) but only reads it on mount (`useState` initialiser + `pendingFocus` ref); a host that is already showing the panel has no way to ask it to jump to a field later.

   None of this depends on the backend. The whole analysis runs in the browser (`src/seo/*`); the only server round trips are `GET /admin/seo/overview` (uniqueness tests) and the `PATCH` that stores the score. Going live with the Laravel API changes nothing about these buttons.

**B. The handover package assumes a hosting layout Cloudways does not offer.**

- `backend_developer_guidelines/` (generated by `scripts/generate-backend-guidelines.js` from `src/services/endpoints.js`, `mock-server/schemas/*`, `src/config/enums.js`, captured mock responses and the hand-written `docs/backend-notes/*.md`) is complete — 242 endpoints, contract, DDL, seed, Postman with tests, OpenAPI, business rules, SEO documents. But `07_DEPLOYMENT.md` (from `docs/backend-notes/07_deployment.md`) is written for a self-managed server: custom Nginx server blocks that proxy `/robots.txt`, `/sitemap*.xml`, `/rss.xml`, `/llms.txt` from the site host to the API host, `rsync` + release symlinks, `nginx -s reload`. The client will host on **Cloudways** (managed Nginx → Apache + PHP-FPM, Varnish, no custom Nginx server blocks, `.htaccess` only). Nobody has written down how the site and the API are laid out there, so a Cloudways deployment done from the current document will get `robots.txt` wrong (the static `public/robots.txt` placeholder — `User-agent: * / Disallow: /admin` — would be what Google reads), will have no SPA fallback for deep links, and may serve stale API data through Varnish.
- The package was generated from commit `3b420fb` (19 Sep); prompts 49/50 and today's fixes landed afterwards. It must be regenerated so it describes the code that ships.
- The parity checklist in the package README tells the backend developer to run `npm run smoke -- --baseUrl=…` — a command that only exists inside this repository. A developer who receives only the folder cannot run it.
- `docs/backend-notes/01_api_contract.md` → "Planned additions" specifies `GET /properties/counts` (one response instead of the 25 `GET /properties?…&perPage=1` calls `src/components/sections/home/useCategoryCounts.js` makes today — `docs/PROJECT_STATE.md` NEW-42). It is not in the registry, so the Laravel developer will not build it. Now — before the API is written — is the right moment to put it in.

## 2. Objective

When this prompt is finished: every SEO panel action visibly does something and says what it did (Re-analyse refreshes the site index, re-runs the tests and reports the result; Auto-fill fills what is empty, reports exactly what it filled, and offers an explicit overwrite; Fix SEO opens the right tab **and** the right field in every form that shows it); `GET /properties/counts` exists on the mock, in the registry, in the smoke test and in the package, and the home page uses it with the per-tile fallback intact; the handover package contains a Cloudways deployment guide (recommended single-application layout and the two-application alternative), a self-contained smoke-test bundle the backend developer can run without the repository, and is regenerated from the current code; the frontend build handles `robots.txt` correctly for both layouts; `docs/PROJECT_STATE.md` records prompt 51 and stays COMPLETE.

## 3. Scope

### Files to create

- `src/components/seo/SeoPanel/parts/__tests__/ScoreCard.test.jsx`
- `src/components/seo/SeoPanel/__tests__/SeoSummaryCard.test.jsx` (if one does not exist; extend it if it does)
- `src/pages/admin/articles/__tests__/ArticleFormPage.focus.test.jsx`
- `mock-server/routes/propertyCounts.js` (or a handler inside the existing properties router — match the router file layout you find)
- `scripts/postbuild.js` (robots.txt handling for the two hosting layouts; see 4.C)
- `scripts/generate-smoke-bundle.js` (or a section of `generate-backend-guidelines.js`) producing `backend_developer_guidelines/smoke/`
- `docs/DEPLOYMENT_CLOUDWAYS.md` (frontend-side guide for the human; the backend side lives in the notes below)
- `e2e/seo-actions.spec.js`

### Files to modify

- `src/components/seo/SeoPanel/parts/ScoreCard.jsx`
- `src/components/seo/SeoPanel/SeoPanel.jsx` (new `focusRequest` prop; `refreshSiteIndex` exposed through the panel context)
- `src/components/seo/SeoPanel/SeoPanelContext.js` (context typedef)
- `src/components/seo/SeoPanel/SeoSummaryCard.jsx`
- `src/components/seo/SeoPanel/SeoPanel.module.css` (gauge pulse, split-button/menu styles if needed)
- `src/pages/admin/articles/ArticleFormPage.jsx`
- `src/pages/admin/properties/property-form/usePropertyForm.js`, `tabs/SeoTab.jsx`, `StatusRail.jsx`, `fieldFocus.js` (only if needed)
- `src/pages/admin/pages/PageFormPage.jsx`, `src/pages/admin/master-data/LocalityFormPage.jsx`, `src/pages/admin/master-data/DeveloperFormPage.jsx`, `src/pages/admin/seo/SeoEditDialog.jsx` — verify each host's `onFocusField`/`initialField` wiring and apply the same `focusRequest` pattern where the host has a tab or a rail that can ask the panel to jump
- `src/config/adminCopy.js` (the new toast/tooltip copy lives with the other admin strings)
- `src/services/endpoints.js` (`properties.counts`), `src/services/propertyService.js`, `src/components/sections/home/useCategoryCounts.js` (+ its tests)
- `mock-server/routes/properties.js` (register the counts route **before** `/properties/:id`), `mock-server/schemas/*` (query descriptor for `by` and the §5.7 filters), `mock-server/routes/sitemap.js` (child-sitemap `<loc>` rule, 4.D)
- `scripts/smoke-api.js` (counts endpoint checks; `--baseUrl` unchanged)
- `scripts/generate-backend-guidelines.js` (render every section of `07_deployment.md`, copy the smoke bundle, update the README "What is in this folder" table and the parity checklist)
- `docs/backend-notes/01_api_contract.md` (move `GET /properties/counts` from "Planned additions" to the contract proper), `docs/backend-notes/05_business_rules.md` (counts formula), `docs/backend-notes/06_seo.md` (child-sitemap host rule, robots precedence), `docs/backend-notes/07_deployment.md` (Cloudways sections, 4.E), `docs/backend-notes/08_testing.md` (running the bundled smoke test)
- `package.json` (`postbuild`, `postbuild:ci`, `generate:smoke-bundle` scripts), `.env.production.example`, `README.md` (Deployment + Handover sections), `docs/RELEASE_CHECKLIST.md`, `docs/PROJECT_STATE.md`, `docs/DECISIONS.md`
- `backend_developer_guidelines/**` — regenerated only; never hand-edited

### Files to delete

None.

## 4. Detailed tasks

### 4.A — SEO panel actions

1. **`ScoreCard.jsx` — Re-analyse.** Replace the bare `onClick={reanalyse}` with an async handler:
   - set a local `busy` state (both buttons disabled, the refresh icon spins via a CSS class);
   - call `refreshSiteIndex()` (add it to the panel context: `SeoPanel.jsx` already gets `refresh` from `useSiteSeoIndex()` — expose it as `refreshSiteIndex`) and wait for it (make `refresh` return the `loadIndex()` promise);
   - call `reanalyse()`; it already returns the result of `run()` — read `score`, `testsPassed`, `testsTotal` from the return value;
   - toast through `useToast()` (`src/components/common/ToastProvider.jsx`): `Re-analysed — 51/100, 12 of 32 tests passed` when the score or the pass count changed, `Re-analysed — still 51/100, 12 of 32 tests passed` when nothing changed;
   - add a 600 ms `gaugePulse` class to the gauge (CSS keyframes in `SeoPanel.module.css`, respecting `prefers-reduced-motion`);
   - add `title`/`aria-label` tooltips from `adminCopy.js`: "Re-runs every test against the current values and the latest site-wide index (used by the uniqueness tests)".
2. **`ScoreCard.jsx` — Auto-fill missing.**
   - Compute `missing = ['title','description','focusKeyword','og.imageUrl'].filter(empty)` before generating. If `missing` is empty → toast (info): `Nothing to fill — the SEO title, description, focus keyword and share image are already set. Use "Regenerate all" to replace them.` and do not call `setSeo`.
   - Otherwise apply exactly as today and toast (success): `Filled: description, share image` (human labels, in that order). If `generateDefaults` produced an empty value for a missing field (e.g. no image on the record), name it in the toast: `Could not fill: share image (no image on this record)`.
   - Add a second action **Regenerate all** (a small `mdi:dots-vertical` menu next to the two buttons, or a third ghost button — pick the one that fits the card at 360 px and record it): opens the existing `ConfirmDialog` ("Replace the SEO title, description, focus keyword and share image with generated values? What you typed will be lost.") and on confirm calls `generateDefaults(..., { overwrite: true, context })`, writes all four, toasts `Regenerated: title, description, focus keyword, share image`.
   - Tooltip: "Fills only the SEO fields that are empty; never replaces what you typed".
   - Buttons disabled while `analysing`, while `busy`, and when `disabled` (already hidden then — keep that).
3. **`SeoPanel.jsx` — `focusRequest` prop.** Add `focusRequest?: { path: string, nonce: number }`. An effect keyed on `focusRequest?.nonce` calls the panel's own `focusField(path)` (which switches the sub-tab via `tabOfSeoField` and focuses `fieldId(path)`). Keep `initialField` working as before (it becomes `focusRequest` with nonce 0 internally, or stays separate — your choice, tested either way). Document the prop in the component's JSDoc.
4. **`SeoSummaryCard.jsx` — Fix SEO.** Keep `firstFailure`. Change the button label to `Fix SEO` when there is a failure, `Open SEO tab` when there are only warnings, and hide it when nothing is failing or warning **and** `onOpen` is absent. Pass `{ field, message }` to `onOpen` (message used by hosts for a follow-up toast: `Opened SEO › General — "The title does not carry the focus keyword."`).
5. **`ArticleFormPage.jsx`.** Rewrite `focusField(path)`: if `path` starts with `seo.` → `setActiveTab('seo')` and `setSeoFocusRequest({ path, nonce: Date.now() })`, passed to `<SeoPanel focusRequest={seoFocusRequest}>`; else the existing content-tab behaviour. The `onOpen` of the rail's `SeoSummaryCard` uses the same function, so a `seo.*` failure now lands on the SEO tab with the field focused, and a `content`/`excerpt`/`slug`/`faqs`/`featuredImage` failure lands on the Content tab as before. After the jump, toast the message once (info, 4 s).
6. **Property form.** In `usePropertyForm.js` `focusField(path)`: when `tabOfPath` answers `seo`, also set `seoFocusRequest` in the hook state (returned alongside `focusField`); `tabs/SeoTab.jsx` passes it to `<SeoPanel focusRequest={…}>`. Keep `focusFieldElement` for the non-SEO tabs. `StatusRail.jsx` keeps calling `focusField(field)`; verify the jump from the rail on all 16 tabs.
7. **Other hosts.** `PageFormPage.jsx`, `LocalityFormPage.jsx`, `DeveloperFormPage.jsx`, `SeoEditDialog.jsx`: confirm that a hint click inside the panel (`TestList` → `focusField`) reaches a host field where the field is outside the panel (`content`, `excerpt`, `heroImageUrl`, `images`, …) and the panel's own field where it is `seo.*`. Fix any host whose `onFocusField` ignores the path or opens the wrong tab. Where a host has no rail summary, no change beyond that.
8. **Tests.** `ScoreCard.test.jsx`: Re-analyse shows the "still" toast when nothing changed and the "Re-analysed — …" toast when the score changed (mock `analyze`), Auto-fill with nothing missing shows the "Nothing to fill" toast and does not call `onChange`, Auto-fill with a missing description calls `onChange` with only that field and toasts `Filled: description`, Regenerate all asks for confirmation and overwrites. `SeoSummaryCard.test.jsx`: label switches; `onOpen` receives `{ field: 'seo.title', message }`. `ArticleFormPage.focus.test.jsx`: `focusField('seo.title')` activates the `seo` tab and the panel receives a `focusRequest`; `focusField('excerpt')` activates `content` and focuses `#article-excerpt`. `SeoPanel` test: a changed `focusRequest.nonce` switches to the Social sub-tab for `seo.og.imageUrl` and focuses `#seo-field-seo-og-imageUrl` (or whatever `fieldId` yields — assert through `fieldId`).
9. **E2E** `e2e/seo-actions.spec.js` (Playwright, runs against the mock like the existing specs): sign in as admin → open `/admin/articles/edit/9` → click "Fix SEO" → expect the SEO tab active and the focused element id to equal `fieldId('seo.title')` → click "Re-analyse" → expect a toast containing "Re-analysed" → click "Auto-fill missing" → expect a toast starting with "Nothing to fill" or "Filled:". Repeat "Fix SEO" on `/admin/properties/edit/<seed id with a seo.* failure>`.

### 4.B — `GET /properties/counts`

1. Registry entry `endpoints.properties.counts` = `GET /properties/counts`, public, query `by` (comma-separated: `segment`, `propertyTypeId`, `listingType`, `constructionStatus`, `localityId`; unknown ignored) plus every §5.7 listing filter; response `{ data: { <dimension>: { <key>: count } }, meta: null }`, keys as strings, `isActive: true` scoping, `Cache-Control: public, max-age=300`. Register it in the mock **before** `GET /properties/:id` and `GET /properties/slug/:slug` so `counts` is never read as an id. Add the query descriptor to `mock-server/schemas/` so the generator documents it and the smoke test validates it.
2. `propertyService.counts(params)`; `useCategoryCounts.js` calls it once, maps the numbers onto the tiles, and **keeps the per-tile requests as the fallback** when the call fails with 404/501 (a backend that has not shipped it) — the fallback must stay covered by the existing tests. No other behaviour change on the home page.
3. `scripts/smoke-api.js`: assert the envelope, that `?by=segment` keys are the three segments, that `?by=propertyTypeId&listingType=rent` narrows, and that an unknown dimension is ignored. In `--compare` mode it is a read like any other.
4. Notes: move the endpoint from "Planned additions" into the body of `docs/backend-notes/01_api_contract.md` (keep the rationale), add the `GROUP BY` formula to `05_business_rules.md`.

### 4.C — `robots.txt` for the two hosting layouts (frontend side)

1. `scripts/postbuild.js`, wired as `"postbuild": "node scripts/postbuild.js"` and `"postbuild:ci": "node scripts/postbuild.js"` (npm runs `post<script>` hooks for both `build` and `build:ci`; `build:prerender` already runs `build` first). It reads `REACT_APP_API_URL` and `REACT_APP_SITE_URL` from the same env files CRA used (load `.env.production`/`.env` with the CRA precedence, or read `process.env` when set) and decides the layout:
   - **Same origin** (the origin of `REACT_APP_API_URL` equals the origin of `REACT_APP_SITE_URL`, e.g. `https://www.squaresnacres.com/api` and `https://www.squaresnacres.com`): delete `build/robots.txt` and print `robots.txt: removed from the build — the API serves it at /robots.txt on this host`. Apache serves an existing file before Laravel sees the request, so the placeholder must not be deployed.
   - **Two origins**: fetch `${REACT_APP_API_URL}/robots.txt` with a 5-second timeout; on success write it to `build/robots.txt` with every `Sitemap:` line rewritten to `${REACT_APP_API_URL}/sitemap.xml` (the static host has no sitemap); on failure write the shipped default (the same text `src/pages/admin/seo/settings-tabs/RobotsTab.jsx` uses as its "restore recommended" default, with `%siteurl%` resolved and the `Sitemap:` line pointing at the API) and print a warning. Print the layout it detected either way.
   - Never touch `public/robots.txt` (the committed development placeholder).
2. Unit-test the two branches and the `Sitemap:` rewrite in `scripts/__tests__/postbuild.test.js` (node:test, like the other script tests).
3. `.env.production.example`: replace the single API line with the two layouts, commented, same-origin first:

   ```bash
   # Layout A (recommended on Cloudways): one Laravel application serves the API
   # under /api and the React build for every other path — same origin, no CORS.
   REACT_APP_API_URL=https://www.squaresnacres.com/api
   REACT_APP_SITE_URL=https://www.squaresnacres.com
   # Layout B: the site on its own application/host, the API on api.<domain>.
   # REACT_APP_API_URL=https://api.squaresnacres.com/api
   ```

### 4.D — Sitemap index host rule (mock + notes)

The sitemap index's child `<loc>`s must be reachable wherever the index was fetched from. Rule for both backends: **child sitemap URLs are built from the request's own origin and path prefix** (fetched at `https://www.site.com/sitemap.xml` → `https://www.site.com/sitemap-properties.xml`; fetched at `https://api.host/api/sitemap.xml` → `https://api.host/api/sitemap-properties.xml`), while page `<loc>`s inside each sitemap keep using `seoSettings.siteUrl`. Implement it in `mock-server/routes/sitemap.js` (honour `X-Forwarded-Proto`/`X-Forwarded-Host` when present), assert it in the smoke test (fetch the index at the API base and check every child URL starts with that base), and document it in `docs/backend-notes/06_seo.md` (Laravel: `TrustProxies` for Cloudways' Nginx, `url()->current()` based, cache key per host). Also document the precedence problem: an existing `public/robots.txt`/`sitemap.xml` file in the Laravel `public/` folder shadows the route — the deploy step must not copy one there.

### 4.E — Cloudways deployment guide (backend notes → regenerated package) and `docs/DEPLOYMENT_CLOUDWAYS.md`

Write `docs/backend-notes/07_deployment.md` so that it contains, in addition to the existing Nginx material (keep it under a heading "Self-managed server (Nginx)"), the following sections — concrete commands, no hand-waving — and make sure `scripts/generate-backend-guidelines.js` renders **every** section of that file into `07_DEPLOYMENT.md` (it currently spreads `notesOf(notes, '07_deployment')` into the template; check the template prints unknown sections, and fix it if it prints only the ones it knows by name):

1. **"Hosting on Cloudways — choose a layout".** Layout A (recommended): one Cloudways *Laravel* application on the primary domain serves the API under `/api` **and** the React build for every other path. Why: same origin (no CORS, no cookies, no preflight), `robots.txt`/sitemaps/RSS/llms.txt are ordinary Laravel routes on the host Google crawls, deep links fall back to `index.html` through Laravel, one SSL certificate, one deployment. Layout B: two Cloudways applications — a *PHP/Custom* application holding the static build on `www.` and the Laravel application on `api.` — with the caveats: CORS must list the site origin; the static host needs the `.htaccess` SPA rewrite; `robots.txt` comes from the build (the `postbuild` script writes it and points its `Sitemap:` at the API host); child sitemaps and the sitemap index live on the API host; Search Console must verify both hosts. Say plainly that A is the one to pick unless the client insists on a separate API domain.
2. **"Layout A — server and application"**: Cloudways server (DigitalOcean/AWS/…), PHP 8.2 or 8.3, **MySQL 8.0** (if the server offers only MariaDB, `schema.sql` must be re-tested — JSON columns are `LONGTEXT` + `CHECK (JSON_VALID(...))` there, and the generated DDL must run clean; say which functions to watch), Composer 2, Node is **not** needed on the server (the build is produced elsewhere), Redis optional (`CACHE_DRIVER=file` works on one server), application webroot set to `public_html/public` (Cloudways → Application Settings → Webroot), Let's Encrypt SSL for `www.` and the apex, both domains mapped, primary = `www.`.
3. **"Layout A — Laravel application"**: `routes/api.php` carries every `/api/*` endpoint; `routes/web.php` carries `/robots.txt`, `/sitemap.xml`, `/sitemap-*.xml`, `/rss.xml`, `/llms.txt` (the same controllers as their `/api/` twins) **and** `Route::fallback()` that returns `public/index.html` (`response()->file(public_path('index.html'))`) for any `GET` that is not an API route and not an existing file; the default Laravel `public/.htaccess` (keep its "Handle Authorization Header" block — with PHP-FPM it is what keeps the `Bearer` token from being dropped; if every admin call answers 401 on Cloudways, that block is missing); `.htaccess` additions for HTTP→HTTPS and apex→`www` 301s, `Cache-Control: max-age=31536000, immutable` for `static/`, no-cache for `index.html`; `App\Http\Middleware\TrustProxies` set to `*` (Cloudways terminates TLS at Nginx); `config/cors.php` may stay restrictive (same origin), but list the origin anyway for a staging host; `post_max_size`/`upload_max_filesize` ≥ 8 MB (a property `PUT` with 30 images is a large JSON body); Sanctum token expiry = `SANCTUM_TOKEN_TTL_MINUTES`; the cron job `* * * * * php artisan schedule:run` (Cloudways → Application → Cron Job Management → Advanced) for scheduled article publishing and sitemap cache refresh; queue: `QUEUE_CONNECTION=database` with Supervisor (Cloudways → Server → Manage Services → Supervisor) or `sync` if no worker will be set up (lead e-mails then send inline — say what that costs); `php artisan storage:link` is unnecessary (no server-side uploads); `php artisan migrate --force`, then the seed import per `seed-mapping.md`, then `php artisan config:cache route:cache view:cache`.
4. **"Layout A — deploying the site into the Laravel application"**: on the build machine `npm ci && npm run build` (or `build:prerender`) with `.env.production` from 4.C; copy **the contents** of `build/` into the Laravel `public/` folder over SFTP/SSH (`rsync -a --delete --exclude index.php --exclude .htaccess --exclude robots.txt build/ public/` from a shell, or the SFTP client's folder upload) — `index.php` and `.htaccess` are Laravel's and stay; `robots.txt` was already removed by `postbuild`; `manifest.json`, `brand/`, `static/` and (if prerendered) the per-route `index.html` files come along; the prerendered per-route files are served by Apache before Laravel sees the request, so they work unchanged. Note that Cloudways' Git deployment can pull the Laravel repo, but the React build is an artefact and is uploaded, not built on the server.
5. **"Layout B — the static application"**: a Cloudways PHP application whose `public_html` holds the build; `.htaccess`:

   ```apache
   RewriteEngine On
   RewriteCond %{HTTPS} off [OR]
   RewriteCond %{HTTP_HOST} ^squaresnacres\.com$ [NC]
   RewriteRule ^ https://www.squaresnacres.com%{REQUEST_URI} [L,R=301]
   RewriteRule ^index\.html$ - [L]
   RewriteCond %{REQUEST_FILENAME} !-f
   RewriteCond %{REQUEST_FILENAME} !-d
   RewriteRule . /index.html [L]
   <FilesMatch "\.(js|css|png|jpg|webp|svg|woff2)$">
     Header set Cache-Control "public, max-age=31536000, immutable"
   </FilesMatch>
   <FilesMatch "index\.html$">
     Header set Cache-Control "no-cache"
   </FilesMatch>
   ```

   plus the CORS origins on the API, the `Sitemap:` line in the built `robots.txt`, and Search Console verification of both hosts.
6. **"Cloudways specifics that bite"**: **Varnish** — either disable it for the application (Application Settings → Varnish) or exclude `/api/` and `/admin` (URL exclusion rules); a cached `GET /api/properties` is how an edited price stays old for an hour, and a cached `POST` response is worse; **Bot protection** (the Malcare add-on) must not block the crawlers `robots.txt` allows — list the user agents to whitelist (`GPTBot`, `OAI-SearchBot`, `ClaudeBot`, `PerplexityBot`, `Google-Extended`, `Bingbot`, `Applebot`, `CCBot`, …); **Cloudflare/CDN** in front: bypass cache for `/api/*` and `/admin*`; PHP-FPM and the `Authorization` header (above); `.env` lives outside `public_html` on Cloudways — `APP_URL=https://www.squaresnacres.com`, `SITE_URL` the same; time zone `Asia/Kolkata` in `config/app.php` for the scheduler; log rotation; backups (Cloudways automated backups + a manual one before every deploy that migrates); staging application with the blanket-disallow `robots.txt` and `noindex` (`seoSettings.defaults.robots` off) until launch.
7. **"Switch-over on Cloudways"**: the same one-line rule, restated for both layouts, plus the exact upload steps and the smoke/compare commands, and the rollback (re-upload the previous `build/` folder — keep the last three builds zipped with their commit hashes; database rollback rules unchanged).
8. **"Go-live checklist — Cloudways"**: the existing 16 items adapted (item 10's `try_files` becomes "the `Route::fallback`/`.htaccess` rewrite"; add Varnish, bot protection, cron, `Authorization` header, `robots.txt` fetched on the live host shows the admin-edited content and the `Sitemap:` line, `https://www.squaresnacres.com/sitemap.xml` and `/api/sitemap.xml` both answer and their child URLs open).

`docs/DEPLOYMENT_CLOUDWAYS.md` (for the human, frontend side, short): the two layouts in one paragraph each, the `.env.production` lines, `npm run build`, what to upload where, the `postbuild` behaviour, and a link to the package's `07_DEPLOYMENT.md` for the server side. Link it from `README.md` → Deployment, and from `docs/RELEASE_CHECKLIST.md`.

### 4.F — A self-contained smoke bundle in the package

1. `scripts/generate-smoke-bundle.js` (called by the guidelines generator; also `npm run generate:smoke-bundle`) writes `backend_developer_guidelines/smoke/` containing: `package.json` (`"type": "module"` or CJS to match the script, `engines.node >= 20`, **no dependencies** — the smoke script uses Node's own `fetch`), `smoke-api.js`, and copies of everything it imports from the repository (`src/services/endpoints.js`, `src/config/enums.js`, any schema/descriptor module) with their import paths rewritten to the bundle layout. If a module it imports drags in React/CRA-only code, split that module first so the registry stays plain Node. Add `README.md` inside `smoke/` with the three commands: `node smoke-api.js --baseUrl=https://…/api`, `--compare=<mock url>`, `--verbose`.
2. Prove it: the generator (or a test in `scripts/__tests__/`) copies `backend_developer_guidelines/smoke/` to a temp folder outside the repository and runs `node smoke-api.js --baseUrl=http://localhost:4000/api` there; it must pass without `npm install`.
3. Package `README.md` (generated) — "What is in this folder" gets the `smoke/` row; the parity checklist's `npm run smoke …` lines become `node smoke/smoke-api.js …` with the repository command mentioned as the equivalent. `08_testing.md` notes updated accordingly.

### 4.G — Regenerate and reconcile the package

1. With the mock running on fresh seed data (`npm run mock:reset`, then `npm run mock`), run `npm run generate:backend-guidelines`. Confirm the `generatedFrom:` hash is the current `HEAD` (commit your code changes first, generate, then commit the package in a second commit so the hash is honest), that `db.json` in the package is byte-identical to the root copy, that `03_ENDPOINTS.md`/`openapi.yaml`/`postman_collection.json` contain `GET /properties/counts`, that `07_DEPLOYMENT.md` contains every Cloudways section, that `06_SEO_SITEMAP_ROBOTS.md` carries the child-sitemap host rule and the precedence note, and that `smoke/` is present. `npm run check:guidelines` must pass against the regenerated folder.
2. Run `npm run smoke` against the mock and `npm run smoke -- --compare=http://localhost:4000/api --baseUrl=http://localhost:4000/api` (a self-compare must print an empty difference table).
3. `README.md` (root) → "Handover package": state in one paragraph what to hand over (the whole `backend_developer_guidelines/` folder, zipped, plus read access to the repository if the developer wants to run the mock themselves), and where the base URL is changed (`.env.production` → `REACT_APP_API_URL`, then `npm run build`, then upload `build/`).

## 5. Data contract touched

- New public endpoint `GET /properties/counts` (registry `properties.counts`, mock, smoke, package). No other endpoint changes. Child-sitemap `<loc>` host rule for `GET /sitemap.xml` (mock + notes) — an output rule, not a new route.
- `db.json`: unchanged.
- Env vars: none added. `.env.production.example` rewritten for the two layouts.
- npm scripts: `postbuild`, `postbuild:ci`, `generate:smoke-bundle`.

## 6. UI/UX requirements

- Toasts use the existing `ToastProvider` variants (success/info/warning), 4 s, one at a time; wording exactly as in 4.A (copy in `adminCopy.js`).
- The score card must still fit at 360 px with three actions: on narrow widths the third action collapses into the `⋮` menu or wraps to a second row — no horizontal overflow, no truncated labels.
- Disabled/busy states: outline button shows a spinning refresh icon; buttons are not clickable twice.
- Gauge pulse is subtle (scale 1 → 1.04 → 1) and skipped under `prefers-reduced-motion`.
- "Fix SEO" jump: the focused control is centred in view (`scrollIntoView({ block: 'center' })`), and the failing test row in `TestList` gets a 1.5 s highlight (existing token colours only; no hex literals).
- No console warnings (act(), keys, MUI props) during any of these interactions.

## 7. Edge cases that must work

- Re-analyse while `GET /admin/seo/overview` fails (sales role gets 403): the index stays empty, the uniqueness tests stay `skip`, the toast still reports the score — never an error toast for a 403 here.
- Auto-fill on a record with no images and no content (new property with only a title): fills title + focus keyword, reports the two it could not fill.
- Regenerate all on a read-only form (`disabled`): the whole action row is hidden (already the case) — verify.
- Fix SEO when the first failure is `slug` (Content tab in the article form; Basics tab in the property form) and when it is `seo.og.imageUrl` (SEO tab → Social sub-tab).
- Fix SEO twice on the same failure: the second click still jumps (the nonce changes).
- `SeoEditDialog` opened from the SEO dashboard's Issues tab with `initialField` still opens on the right sub-tab and field.
- `GET /properties/counts?by=` (empty) → `data: {}`; `by=unknown` → `data: {}`; counts respect `isActive` and every §5.7 filter; a 404 from a backend without the endpoint makes the home page fall back silently to the per-tile requests.
- `postbuild` with no `build/` folder (someone runs it by hand) exits 0 with a message; with the API unreachable in layout B it writes the default and warns; with `REACT_APP_SITE_URL` unset it treats the layout as two-origin and says so.
- Regenerating the package twice without code changes produces a byte-identical folder except the `generatedFrom:` line if `HEAD` moved.

## 8. Acceptance criteria

- [ ] Clicking **Re-analyse** on the article, property, page, category, locality and developer forms and in the SEO dashboard's edit dialog always shows a toast with the score and pass count, spins the icon while working, and refreshes the site index.
- [ ] Clicking **Auto-fill missing** always answers: "Filled: …", "Could not fill: …" or "Nothing to fill — …"; **Regenerate all** exists, confirms, and overwrites.
- [ ] Clicking **Fix SEO** on the article form with a `seo.*` first failure opens the SEO tab, the right sub-tab, and focuses the field; with a content failure it opens the Content tab and focuses the field. The property form does the same from its rail on every tab.
- [ ] Hint clicks inside the panel (`TestList` "Fix") work in every host, for panel-owned and host-owned fields alike.
- [ ] `GET /properties/counts` is in the registry, the mock, the smoke test, `03_ENDPOINTS.md`, the Postman collection and `openapi.yaml`; the home page issues one counts request instead of 25 and still works when the endpoint answers 404.
- [ ] `npm run build` and `npm run build:ci` run `postbuild`; with same-origin env values `build/robots.txt` is absent; with two-origin values it exists and its `Sitemap:` line points at the API.
- [ ] The sitemap index served by the mock at `/api/sitemap.xml` lists children under `/api/`, and the smoke test asserts it.
- [ ] `backend_developer_guidelines/07_DEPLOYMENT.md` contains the Cloudways sections 4.E.1–4.E.8; `06_SEO_SITEMAP_ROBOTS.md` the host rule and the precedence note; `01_API_CONTRACT.md` the counts endpoint in the contract body; `README.md` the `smoke/` row and the updated parity checklist; `smoke/` runs from outside the repository with no install.
- [ ] `generatedFrom:` equals the commit the package was generated from, and the package `db.json` is byte-identical to the root `db.json`.
- [ ] `docs/DEPLOYMENT_CLOUDWAYS.md` exists and is linked from `README.md` and `docs/RELEASE_CHECKLIST.md`; `.env.production.example` shows both layouts.
- [ ] `npm run check:all` (which includes `check:guidelines` and `check:env`) and `npm run smoke` pass; `npm run e2e` passes including `seo-actions.spec.js` (or the spec is written and the reason it could not run is recorded, as for the earlier specs).
- [ ] No console errors or warnings on the six admin forms and the home page.
- [ ] `docs/PROJECT_STATE.md` records prompt 51, stays `Status: COMPLETE`, and its endpoint list includes `GET /properties/counts`; NEW-42 moves from "Deferred (post-1.0)" to closed.

## 9. Verification

1. `npm run check:all` (lint, test:ci, test:mock, test:scripts, build:ci, check:traces, validate:seed, check:contrast, check:guidelines, check:env) — all green. `check:guidelines` and `check:env` are the two most likely to catch a slip here: run them again after 4.G.
2. `npm run mock:reset && npm run mock` (terminal 1); `npm run smoke` and `npm run smoke -- --baseUrl=http://localhost:4000/api --compare=http://localhost:4000/api` (terminal 2) — both green, empty difference table.
3. `npm run build:ci` with `.env.production` holding the layout-A values → `build/robots.txt` must not exist; then with the layout-B values → it exists with the API `Sitemap:` line. Restore `.env.production` to whatever it was (it is git-ignored; do not commit it).
4. `npm run generate:backend-guidelines` (mock running) → `git status` shows only the package changed; open the five files named in 4.G.1 and confirm the content; copy `backend_developer_guidelines/smoke/` to `%TEMP%\sna-smoke` and run `node smoke-api.js --baseUrl=http://localhost:4000/api` there.
5. `npm start` and, in a 1280 px window and a 390 px window: `/admin/articles/edit/9` → Fix SEO → SEO tab opens, SEO title focused, toast shown; Re-analyse → toast; Auto-fill missing → "Nothing to fill" toast; ⋮ → Regenerate all → confirm → fields replaced, toast; Ctrl+Z is not expected — "Cancel" on the form discards. Repeat on `/admin/properties/edit/1` (rail → Fix SEO), `/admin/pages/edit/1`, `/admin/articles/categories` (Edit → panel), `/admin/localities/edit/1`, `/admin/builders/edit/1` (use the real admin routes of the repo), `/admin/seo` → Issues → Fix.
6. Home page: Network tab shows one `GET /properties/counts` and no `perPage=1` requests; stop the counts route (temporarily return 404 in the mock) → the tiles still show numbers via the fallback; restore.
7. `npm run e2e` (or record why it cannot run in this environment, exactly as the earlier QA prompts did).
8. `npm run check:traces` → 0 findings.

## 10. Update project state

- Append to `docs/PROJECT_STATE.md`: "Prompt 51 — SEO panel actions and Cloudways handover": files added/changed, the new endpoint, the `postbuild` behaviour, the smoke bundle, the regenerated package hash; close NEW-42; keep `Status: COMPLETE`; version stays `1.0.0` unless you also tag `v1.0.1` (do: bump `package.json` to `1.0.1`, tag `v1.0.1` at the final commit).
- Append to `docs/DECISIONS.md`: D-51a the three-action score card and its copy; D-51b `focusRequest` over `initialField`-on-mount; D-51c layout A recommended on Cloudways and why; D-51d child-sitemap host rule; D-51e `postbuild` owns `robots.txt` in the build; D-51f the package carries its own smoke bundle; D-51g `GET /properties/counts` promoted from "planned" to contract.

## 11. Commit

Three commits, in this order, Conventional Commits:

1. `feat(seo): score-card actions that report what they did, and Fix SEO that lands on the field`
2. `feat(api): GET /properties/counts, postbuild robots.txt, Cloudways deployment notes and the smoke bundle`
3. `docs(handover): regenerate backend_developer_guidelines from <hash>` (package only), then `git tag v1.0.1`.

Working tree clean afterwards; `.env.production` not committed.

## 12. Guardrails

- Do not touch: `db.json` (seed), `src/seo/analyzers/*` scoring weights, the public property/article pages, `prompts/*`.
- Do not add dependencies. Node's own `fetch`, `node:test` and the existing libraries are enough.
- Do not hand-edit anything under `backend_developer_guidelines/` — change the notes or the generator and regenerate.
- Do not leave TODO/FIXME comments, `console.log` calls, commented-out code, lorem ipsum, or HOM/H.O.M traces.
- Do not reduce or remove existing functionality; the per-tile counts fallback, `initialField`, and the Nginx deployment material all stay.
