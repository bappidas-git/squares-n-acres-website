# Prompt 10 — Full Bangalore seed data (`db.json`) and seed guide

## 0. Before you start
- Read `prompts/00_MASTER_CONTEXT.md` fully (especially §6 Data model, §6.15 mapping, §6.17 enums, §9.5/§9.6/§9.8 SEO defaults, §10 "Seed quality", §14 Content placeholders policy), then `docs/PROJECT_STATE.md`, `docs/DECISIONS.md` and `docs/SEED_GUIDE.md`.
- Confirm prerequisites: prompts 01–09 are done; working tree clean; `npm install` run; `npm run mock` + `npm run smoke` pass on the starter seed.
- Never ask the human questions. If something is ambiguous, follow `00_MASTER_CONTEXT.md`; if still ambiguous, choose the simplest robust option and log it in `docs/DECISIONS.md`.
- If this prompt was partially executed in an earlier crashed session, continue from where `docs/PROJECT_STATE.md` and `git log` say it stopped; do not redo finished work.

## 1. Context
`db.json` is the starter seed of prompt 06 (6 properties, 6 localities, 3 articles, 2 pages, …) in the target shape; `scripts/validate-seed.js` enforces the model, ids, references, covers, `seo` shapes and the trace regexes; `mock-server/schemas/models.js` defines defaults; `src/services/endpoints.js` `example` values reference `lakeview-heights-3-bhk-whitefield`, `karnataka-rera-guide-for-homebuyers`, `whitefield`, `aurelia-estates`, `about`, `real-estate-advisor-bengaluru`, and seed ids `1` for most collections. The smoke script depends on those examples. Real Bangalore localities may be used; developers, banks, partners, people and projects must be fictional.

## 2. Objective
When this prompt is finished `db.json` is the complete, realistic, internally consistent dataset of `00_MASTER_CONTEXT.md` §10 "Seed quality": 36+ properties, 20 localities, 1 city, 17 property types, 40+ amenities, 8 badges, 8 developers, 6 banks, 12+ HTML articles, 4 categories, 15 tags, 3 authors, 20 FAQs, 8 sample testimonials, 6 placeholder team members, 6 partners, 15 CMS pages, 4 jobs, 3 applications, 45 leads, 12 subscribers, 3 redirects, media records, fully populated `siteSettings`/`seoSettings`; it passes `npm run validate:seed`, `npm run smoke` and `npm run check:traces`; `docs/SEED_GUIDE.md` documents how it was built and how to extend it. Every property/article/page/locality/developer has a `seo` object whose `title/description/focusKeyword` are filled (scores stay `null` until the SEO engine analyses them in prompt 36/37).

## 3. Scope
### Files to create
- `scripts/seed/` (optional generator helpers: `scripts/seed/build-seed.js` that assembles `db.json` from `scripts/seed/data/*.js` modules — **recommended** so the seed is reproducible; if you write `db.json` by hand instead, still keep it valid and deterministic)
### Files to modify
- `db.json`, `docs/SEED_GUIDE.md`, `docs/PROJECT_STATE.md`, `docs/DECISIONS.md`, `package.json` (script `seed:build` if the generator is used)
### Files to delete
- none
### May also touch
- `scripts/validate-seed.js` (only to add checks, never to weaken)

## 4. Detailed tasks
1. **Generator (recommended).** `scripts/seed/build-seed.js` writes `db.json` deterministically (no `Math.random` — use a seeded PRNG, e.g. mulberry32 with a fixed seed) from data modules: `data/cities.js`, `localities.js` (20 entries: name, slug, zone, `shortDescription`, `description` (HTML, 150–300 words: character, connectivity (metro line/ORR/airport), social infra, price band), `heroImageUrl` picsum seed, lat/lng (real approximate coordinates), `pincodes`, `highlights` ×4, `connectivity` ×4 (`{label:'Kempegowda Airport', value:'38 km'}`), `avgPricePerSqft` realistic, `priceTrendNote`, `isFeatured` for 8, `order`, `seo`), `propertyTypes.js` (17, §6.3), `amenities.js` (≥ 40, §6.4 list, `mdi:` icons), `badges.js` (8, colour tokens), `developers.js` (8 fictional, `description` HTML 100–200 words, logos picsum `/200/80`, `establishedYear`, `headquarters` "Bengaluru", counts, `reraIds` ["PRM/KA/RERA/…/placeholder"], `highlights`, 4 featured), `banks.js` (6 fictional: rates 8.35–9.25, `maxTenureYears 30`, `maxLtvPercent 80–90`, `processingFeeNote` "Indicative; confirm with the bank", `features` ×3, `applyUrl` null, `logoUrl` picsum), `properties.js` (see task 2), `articles.js` (task 3), `articleTaxonomy.js`, `authors.js`, `faqs.js` (20 across all 8 categories, HTML answers, 6 `showOnHome`), `testimonials.js` (8, `isSample:true`, names "Sample — A. Rao"), `team.js` (6, `name: 'Team Member 1'`, `designation` placeholders, `showOnAbout`), `partners.js` (6 fictional, categories mixed), `pages.js` (task 4), `jobs.js` (4: Real Estate Advisor (slug `real-estate-advisor-bengaluru`), Digital Marketing Specialist, Property Analyst, Customer Relations Manager (part-time); HTML descriptions; responsibilities/requirements lists), `jobApplications.js` (3), `leads.js` (task 5), `subscribers.js` (12), `redirects.js` (3: `/old-properties→/properties`, `/blog→/insights/articles`, `/flats-in-whitefield→/localities/whitefield`), `settings.js` (siteSettings + seoSettings), `media.js` (derived from every image URL). Ids are assigned sequentially per collection; `createdAt/updatedAt` spread over the last 180 days deterministically; `seo` objects built by a helper `makeSeo({ title, description, focusKeyword, slug })`.
2. **Properties (36+; ids 1–40).** Cover the matrix: residential sale apartments in 8 localities (pre-launch ×3, under-construction ×5, ready-to-move ×6, resale ×3), villas (sale ×4: ready/under-construction), independent house/row house/penthouse/duplex/studio/builder floor (1 each, sale), residential plots ×3 (Devanahalli, Sarjapur, Kanakapura; `plotArea` + `plotLength/plotWidth`, `area.areaUnit` sqft, no bedrooms, approvals BIAAPA/BMRDA/BDA), farm land ×1 (acre), rent: apartments ×4 (Koramangala, HSR, Bellandur, Hebbal), villa ×1, independent house ×1, PG/co-living ×1, lease: office space ×2, co-working ×1, retail shop ×1, warehouse ×1, commercial plot ×1 (sale). Property id 1 = `lakeview-heights-3-bhk-whitefield` (3 BHK apartment, ready-to-move, Whitefield, `isFeatured`, `isVerified`, full data). Every property: title pattern "<Project name> – <BHK/type> <in Locality>", `projectName`, realistic `pricing` (per §10 price bands; `pricePerSqft` consistent with area; `priceRangeMin/Max` for projects with multiple unit configurations; rent listings `rentPerMonth` + `securityDeposit` = 5–10 months + `maintenanceChargesMonthly`; lease listings `rentPerMonth` per month and `otherCharges` "Lock-in 3 years"), `area` per segment, `configuration` per segment (plots/commercial: no bedrooms; commercial `specifications` group `other` "Washrooms 4"), `unitConfigurations` for projects (2–4 configs with floor plan images + prices), `floorPlans` (images picsum `/900/600`, `pdfUrl` null except 6 properties with a placeholder PDF URL `https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf`), `images` 5–8 (`alt` = "<title> – <living room|exterior|kitchen|bedroom|amenities|balcony view|master plan|clubhouse>"), `videoUrl` for 6 properties (`https://www.youtube.com/watch?v=dQw4w9WgXcQ` is **not** allowed — use a neutral placeholder YouTube id such as `https://www.youtube.com/watch?v=ysz5S6PUM-U`), `virtualTourUrl` null except 3 (any https placeholder), `brochureUrl` for 20 (the dummy PDF), `documents` 2–4 (types brochure/approval/legal/price-list; `leadGated` true except one `price-list` per 5 properties), `amenityIds` 8–18 (commercial ones use the commercial category), `badgeIds` 0–3 (consistent: "Ready to Move" only on ready-to-move; "RERA Approved" only when `reraRegistered`), `specifications` (project group + 3–5 others), `constructionSpecs` 6–10 rows for apartments/villas, `nearbyPlaces` 5–8 (real Bangalore landmarks near that locality, categories from the enum, `distanceKm` 0.5–12, `travelTimeMin`), `location` (address placeholder "Survey No. 12, <Locality> Main Road", `pincode` real for the locality, lat/lng near the locality centre, `showExactLocation` true for 60 %), `project` (developer, totals, `launchDate`, `approvals`, `landmarkProject` for 4), `constructionTimeline` for pre-launch/under-construction (5 milestones with `completed/in-progress/upcoming`, dates, `imageUrl` for completed), `constructionProgressPercent`, `faqs` ≥ 3 (price, possession, RERA, amenities, locality), `similarPropertyIds` 2–4 valid ids, `sectionVisibility` all true (set `video/virtualTour` false where no URL; `unitConfigurations` false for single units), `agent` (team member id or manual placeholder; `showOnListing` for half), `seo` (title ≤ 60 chars with locality + type, description 120–160 chars, focus keyword "3 bhk apartment in whitefield" style, `secondaryKeywords` ×2), `isActive` true for 34, false for 2 drafts, `isFeatured` ×10, `isVerified` ×20, `priorityOrder` 0–10, `viewCount` 40–2 400, `enquiryCount` 0–35 (consistent with the leads count per property), `publishedAt`, `createdBy` 1.
3. **Articles (12; ids 1–12).** Slugs/titles: `karnataka-rera-guide-for-homebuyers` "Karnataka RERA: A Complete Guide for Bengaluru Homebuyers" (id 1), `bbmp-a-khata-vs-b-khata-explained` "BBMP A Khata vs B Khata: What Every Buyer Should Know", `stamp-duty-and-registration-charges-in-karnataka` "Stamp Duty and Registration Charges in Karnataka (2026 Guide)", `home-loan-eligibility-and-foir-explained` "Home Loan Eligibility in India: FOIR, Credit Score and EMI Planning", `whitefield-locality-guide` "Living in Whitefield: Locality Guide for Buyers and Renters", `sarjapur-road-investment-guide` "Sarjapur Road: Why Investors Keep Coming Back", `north-bengaluru-airport-corridor` "North Bengaluru and the Airport Corridor: Growth Story", `nri-property-buying-checklist-bengaluru` "NRI Property Buying in Bengaluru: Checklist and Common Mistakes", `rental-yields-by-locality-bengaluru` "Rental Yields Across Bengaluru: Where Renters Pay More", `plot-buying-checklist-bda-bmrda-biaapa` "Buying a Plot in Bengaluru: BDA, BMRDA and BIAAPA Approvals Explained", `under-construction-vs-ready-to-move` "Under-construction vs Ready-to-move: Which Should You Choose?", `first-time-homebuyer-checklist-bengaluru` "First-time Homebuyer Checklist for Bengaluru". Each: 800–1 500 words of **clean semantic HTML** (`<p>`, `<h2>`, `<h3>`, `<ul>/<ol>`, one `<table>` in at least 3 articles, `<blockquote>` in 2, one `<figure><img loading="lazy" alt><figcaption>` in each using picsum), one embedded property block placeholder `<div data-sna-block="properties" data-ids="1,3"></div>` in 4 articles, one CTA block placeholder `<div data-sna-block="cta" data-title="Need help choosing?" data-text="Talk to a Squares N Acres advisor." data-button-label="Talk to us" data-lead-source="article"></div>` in every article, internal links (`/localities/whitefield`, `/buy/ready-to-move`, other articles) ≥ 2 and one external dofollow link (`https://rera.karnataka.gov.in/`, `https://bbmp.gov.in/`, `https://kaverionline.karnataka.gov.in/`), `excerpt` ≤ 300, `featuredImage` picsum `/1200/630` with alt, `categoryId`, `tagIds` 2–4, `authorId`, `status` published for 10, `scheduled` (future `publishedAt`) for 1, `draft` for 1, `publishedAt` spread over 120 days, `isFeatured` ×3, `relatedArticleIds` ×2–3, `relatedPropertyIds` ×2, `faqs` ×3, `tableOfContents` true, `seo` filled (focus keyword = the topic phrase), `viewCount` 50–3 000. Figures (stamp duty %, 80C limits, escrow %) written as "as of the time of writing — verify with the authority" (D84). The `contentText/wordCount/readingTimeMinutes` fields are computed by the generator with the same helper as the mock (`mock-server/lib/html.js`).
4. **Pages (15).** Exactly the seed pages of §6.10 with their block lists; placeholder copy per §14 (About story/mission/vision marked `[Placeholder — client to provide]`; `stats` blocks with empty `items`; Contact `leadForm` with a `subject` select (general/property/buyer-assistance/partnership/careers/other), Sell/Let fields (`propertyType` select from residential types, `location` text required, `askingPrice` text, `description` textarea), Home-loan `leadForm` with `monthlyIncome`/`desiredLoanAmount` selects (values from the old HomeLoan page: below-50k/50k-1L/1L-2L/2L-5L/above-5L and below-30L/30L-50L/50L-1Cr/1Cr-2Cr/above-2Cr), Legal `expandableCards` ×6 (Title Verification, Agreement Drafting, RERA Compliance, Property Registration, Due Diligence, Dispute Resolution) with `html` details, Interior `packages` ×3 (Essential/Premium/Luxury with "[indicative]" price labels) + `gallery` ×8 picsum + `features` rooms ×6 (Living Room, Bedroom, Kitchen, Bathroom, Balcony, Study Room), Flexible workspace `features` ×6 (Hot Desks, Dedicated Desks, Private Offices, Meeting Rooms, Virtual Offices, Day Passes) + `leadForm` `workspaceType` select including `day-pass`, Direct lease `features` ×4 (Retail Shops, Office Spaces, Showrooms, F&B Outlets) + `leadForm` `spaceType`/`areaRequired`, Careers `features` culture ×3 + `jobs` + `features` perks ×6 + `leadForm` source `careers` ("Didn't find a role?"), Awareness `facts` ×6 (rewritten generically: "70% of collections must go to an escrow account under RERA" etc., marked "verify"), `expandableCards` ×6 (RERA, Registration, Home loans, Tax benefits, Stamp duty, Documents checklist), `quiz` ×5 (from the old page, `answerIndex`, explanations), `checklist` ×10, `leadForm` with `interest` select (buying/selling/renting/home-loan/legal/general); Privacy/Terms/Disclaimer `richText` placeholders. `showInHeader`/`headerMenu`: buyer-assistance ×3 (`buyer-assistance`), about/careers/partnership/sell-let/flexible-workspace/direct-lease-retails (`company`), awareness (`insights`); `showInFooter` + `footerColumn` for company pages and legal pages (`company`), buyer-assistance (`services`), awareness (`insights`). `seo` filled. `home` page `features` ("Why choose Squares N Acres": Expert local guidance, Verified listings, Transparent process, End-to-end support) and `steps` ("How it works": Search, Shortlist & visit, Negotiate & verify, Close & move in).
5. **Leads (45).** Spread over the last 90 days (deterministic), every `LEAD_SOURCES` value used at least once, statuses distributed (new 14, contacted 9, qualified 6, site-visit 5, negotiation 3, converted 5, lost 3), priorities mixed, `assignedTo` sales user (id 3) for 15, manager for 5, unassigned for the rest, `followUpAt` (8 in the future, 3 overdue), `notes` 0–3 with `createdBy/createdByName`, `activities` consistent with status history (created → status-changed → note-added …), `requirement` filled for post-requirement/hero-search leads, `utm` for 10, `meta` for the 4 financial-assessment/bank-eligibility leads (`score`, `occupation`, `monthlyIncome`, `existingEmi`, `creditScore`, `downPayment`, `hasCoApplicant`, `bank`), `consent` true, `propertyId` for property sources (matching `enquiryCount` sums), `articleId` for `article`, `pageSlug`/`pageUrl` for page sources, `lostReason` for lost. Names Indian, phones synthetic valid, e-mails `@example.com`.
6. **Settings.** `siteSettings` per §6.13 with placeholders (§14): `general.siteName 'Squares N Acres'`, `tagline 'Your trusted partner for Bengaluru property'`, `logoUrl/iconUrl` Cloudinary, `siteUrl 'https://www.squaresnacres.com'`, `contactEmail 'info@squaresnacres.com'`, `contactPhone '+91 98XXX XXXXX'`, `whatsappNumber '919800000000'` (placeholder digits), `whatsappDefaultMessage 'Hi Squares N Acres, I am interested in a property.'`, address placeholder, `mapEmbedUrl null`, `latitude 12.9716`, `longitude 77.5946`, `workingHours` ×2, `reraNumber 'To be provided'`, `hero` (title "Find your next home in Bengaluru", subtitle "Verified apartments, villas, plots and commercial spaces — with expert guidance at every step.", picsum background `/1920/1080`, mobile `/900/1200`, `searchTabs` all 5, `stats []`, `badges []`), `navigation` defaults, `social` empty strings, `footer` (`aboutText`, `columns` ×2 custom (e.g. "Buy" links to status routes, "Services" links to buyer-assistance pages), `disclaimer` "Listings are subject to availability. [Client legal text]", `copyrightText '© %year% Squares N Acres. All rights reserved.'`, `showNewsletter true`, `showGallery false`, `galleryImageUrls []`), `newsletter` copy, `integrations` empty strings, `leads { notificationEmails: ['info@squaresnacres.com'], autoAssign: 'none', defaultPriority: 'medium' }`. `seoSettings` per §6.14 with the §9.5 templates, §9.8 robots, `knowledgeGraph` (type RealEstateAgent, placeholders, `areaServed ['Bengaluru']`, `sameAs []`), `defaults.ogImageUrl` = the Cloudinary OG URL, sitemap defaults, `llmsTxt ''`.
7. **Media.** One record per distinct image/PDF/video URL used anywhere (`provider 'external'`, `type`, `alt`, `folder` = collection name, `tags`), plus the two brand URLs (`cloudinary`).
8. **Validation additions (`scripts/validate-seed.js`):** enforce the quality rules of §10 (active property: ≥ 5 images, alt everywhere, one cover, ≥ 8 amenities, ≥ 3 faqs, description ≥ 300 chars, `seo.title/description/focusKeyword` non-empty; rent listings have `rentPerMonth` and no `pricing.price`; plots have `plotArea` and no `configuration.bedrooms`; commercial has no `bedrooms`; pre-launch/under-construction have `possessionDate` + timeline; ready-to-move/resale have `ageOfPropertyYears`; badge consistency; `enquiryCount` equals the number of leads with that `propertyId` — or ≥, decide ≥ and document), article rules (≥ 800 words for published, excerpt ≤ 300, featured image alt, category/author exist), page rules (block ids unique, `faq` blocks reference existing FAQ ids), counts (≥ 36 properties, 20 localities, ≥ 40 amenities, ≥ 12 articles, 15 pages, 45 leads). Print the stats table.
9. `docs/SEED_GUIDE.md`: structure, id ranges, how to regenerate (`npm run seed:build` then `npm run validate:seed` then `npm run mock:reset`), naming/placeholder rules (§14), how to add a property/article/page, what is fictional vs placeholder, the credentials (mock only).
10. Run `npm run validate:seed`, `npm run mock:reset`, `npm run mock`, `npm run smoke` (must pass with the new data — fix examples in `endpoints.js` **only** if an `example` id changed; the reserved slugs must exist), `npm run check:traces`.

## 5. Data contract touched
`db.json` fully rewritten (all collections). npm script: `seed:build` (if the generator is used). No endpoint changes.

## 6. UI/UX requirements
N/A (the React app still uses old shapes until prompt 11; verify it does not crash on `/`).

## 7. Edge cases that must work
- Deterministic output: running `npm run seed:build` twice yields an identical `db.json` (diff empty).
- Every `similarPropertyIds`, `relatedArticleIds`, `relatedPropertyIds`, `faqIds`, `memberIds`, `ids` inside page blocks reference existing records; `assignedTo` references active users.
- Exactly one property is `slug: lakeview-heights-3-bhk-whitefield` and it is active; article id 1 slug `karnataka-rera-guide-for-homebuyers` is published; locality `whitefield`, developer `aurelia-estates`, page `about`, job `real-estate-advisor-bengaluru` exist.
- The two inactive properties have valid data (they are drafts, not broken records).
- Scheduled article `publishedAt` is at least 30 days in the future relative to the generation date (record the generation date in `docs/SEED_GUIDE.md`; the validator warns when it becomes past).
- No real developer/bank/partner/person names; no HOM strings; no placehold/dzbiw7t4i/gumlet; `check:traces` passes.
- JSON file size stays < 4 MB.

## 8. Acceptance criteria
- [ ] `npm run validate:seed` passes and its stats table shows: properties ≥ 36 (34 active), localities 20, cities 1, propertyTypes 17, amenities ≥ 40, badges 8, developers 8, banks 6, articles ≥ 12, articleCategories 4, articleTags 15, authors 3, faqs 20, testimonials 8, teamMembers 6, partners 6, pages 15, jobOpenings 4, jobApplications 3, leads 45, newsletterSubscribers 12, redirects 3, adminUsers 3.
- [ ] `npm run smoke` passes against the new seed (all registry examples resolve).
- [ ] `npm run check:traces` passes; `grep -c "picsum.photos" db.json` > 200.
- [ ] Every property/article/page/locality/developer has non-empty `seo.title`, `seo.description`, `seo.focusKeyword`.
- [ ] Article HTML is semantic (no `<div>` soup; `<h2>`/`<h3>` present; ≥ 800 words for published).
- [ ] `docs/SEED_GUIDE.md` updated; `npm run lint`, `npm run test:ci`, `npm run build:ci`, `npm run test:mock` pass.
- [ ] One commit; clean tree.

## 9. Verification
```
npm run seed:build          (if generator)
npm run validate:seed
npm run check:traces
npm run mock:reset
npm run mock                (second terminal)
npm run smoke
npm run test:mock
npm run lint
npm run test:ci
npm run build:ci
```
Manual QA: `GET http://localhost:4000/api/properties?localityId=1&bedrooms=3` → Whitefield 3-BHKs only; `GET /api/articles?categorySlug=legal-rera` → RERA/khata/stamp-duty articles; `GET /api/pages/slug/buyer-assistance/home-loan` (slug contains a slash — confirm the route handles `slug/:slug(*)` — if it does not, fix `routes/pages.js` to accept slashes); `GET /api/admin/dashboard` (admin token) shows non-zero `leadsByDay` and `topProperties`.

## 10. Update project state
- `docs/PROJECT_STATE.md`: prompt 10 report (counts, generation date), Known issues: additional defect 26 closed, BUG-11 data side prepared; next prompt: 11.
- `docs/DECISIONS.md`: D26, D27, D76, D79, D80, D84, generator + seeded PRNG decision, `enquiryCount ≥ leads` rule.

## 11. Commit
`git add -A && git commit -m "feat(seed): full Bangalore dataset (properties, localities, articles, pages, leads, settings) with validator rules"`

## 12. Guardrails
- Do not touch: React source, `theme.js`, `global.css`, mock routes (except the pages slug-with-slash fix if needed).
- Do not add dependencies other than: none.
- Do not leave TODO/FIXME comments, console.log calls, commented-out code, lorem ipsum, or HOM traces; no real brand/people names.
- Do not reduce or remove existing functionality.
