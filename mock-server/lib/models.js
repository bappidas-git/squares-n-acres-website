/**
 * The collection descriptors, re-exported for the mock server.
 *
 * `mock-server/schemas/models.js` describes every `db.json` collection
 * (00_MASTER_CONTEXT.md §6); this module only shortens the relative path for
 * middleware and route files.
 */

module.exports = require('../schemas/models');
